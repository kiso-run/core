# kiso-run/core — v0.9 Development Plan

## Execution Rules

**Hard rule:** Kiso is still internal and unreleased.
- No backward compatibility work required.
- Prefer total cleanup over compatibility layers.
- When a structure is wrong, replace it cleanly.

## Phase 1 — Code deduplication (M1236-M1238)

### M1236 — Unify fallback switching in `_run_text_role` ✅

**Problem:** The `LLMStallError` and `LLMError` (timeout) handlers in
`_run_text_role` have near-identical 6-line fallback-switching blocks.
If one changes, the other gets forgotten.

**Fix:** Extract the common fallback-switch logic into a single block
with a boolean condition. Keep the divergent behavior (stall breaks if
no fallback, timeout retries same model) but eliminate the copy-paste.

**Tasks:**
- [x] Unified into single `except LLMError` with `should_fallback` boolean
- [x] 3983 unit tests pass

### M1237 — Fix stale config template comments ✅

**Problem:** 3 config template comments don't match MODEL_DEFAULTS:
- briefer: comment says flash-lite, code says flash
- classifier: comment says flash-lite, code says flash
- messenger: comment says qwen3.5-flash, code says deepseek-v3.2

**Fix:** Align comments to match MODEL_DEFAULTS.

**Tasks:**
- [x] Aligned all 11 template comments to match MODEL_DEFAULTS
- [x] 81 config tests pass

### M1238 — Extract text stats regex to conftest constants ✅

**Problem:** The multilingual chars/lines regex patterns are duplicated
in `test_multitool.py` and `test_preset_workflows.py`. If a new
language needs support, both files need independent updates — exactly
the bug we hit twice (M1223 fixed one file, M1228 caught the other).

**Fix:** Define `CHARS_COUNT_RE` and `LINES_COUNT_RE` as compiled
patterns in `tests/functional/conftest.py` and import in both test
files.

**Tasks:**
- [x] Added `CHARS_COUNT_RE` and `LINES_COUNT_RE` to `tests/functional/conftest.py`
- [x] Replaced inline patterns in `test_multitool.py` and `test_preset_workflows.py`
- [x] Tests collect cleanly

## Phase 2 — Documentation update (M1239-M1243)

6 of 7 key docs are stale after v0.8's 137 milestones. The project
structure, model defaults, and deterministic guardrails are all
undocumented or wrong.

### M1239 — Update architecture.md and README.md package structure ✅

**Problem:** Both files show `brain.py` and `store.py` as monolithic
files. In reality: `brain/` is a package (planner.py, reviewer.py,
curator.py, common.py, text_roles.py, etc.), `store/` is a package
(plans.py, knowledge.py, sessions.py, etc.), `api/` exists (runtime.py,
sessions.py, knowledge.py, etc.), `worker/` has message_flow.py,
review_flow.py, state.py, replan.py extracted.

**Fix:** Update the project tree in both files. Show packages with their
key modules. Add `api/` which is missing entirely from README.

**Tasks:**
- [x] Updated README.md project tree with brain/, store/, api/, worker/ packages
- [x] architecture.md has no structure section (conceptual doc) — no change needed

### M1240 — Update model-selection.md to match current defaults ✅

**Problem:** 6 of 11 model defaults are wrong. The doc shows old models
(glm-4.7, qwen3.5-flash, flash-lite for curator/briefer/classifier)
that were changed throughout v0.8.

**Fix:** Rewrite the "Default Configuration" section to match
MODEL_DEFAULTS in config.py. Update rationale paragraphs. Remove
references to models no longer used as defaults.

**Tasks:**
- [x] Full rewrite: defaults, rationale, cost table, fallback section
- [x] Removed stale alternative configurations and old benchmark entries

### M1241 — Update flow.md with v0.8 guardrails

**Problem:** flow.md describes the execution flow but omits 4 key
guardrails added in v0.8:
1. msg-first removed — plans start with action tasks, not announce msgs
2. Codegen guardrail — validate_plan rejects exec after leading tool
   unless goal says run/test
3. Circular replan — strategy fingerprint threshold lowered to 2
4. Timeout→fallback — LLM timeout triggers fallback model switch

**Fix:** Add a "Plan Validation Guardrails" section documenting the
deterministic checks in validate_plan. Update the replan loop section
with the new circular detection threshold. Add timeout→fallback to
the LLM retry section.

**Tasks:**
- [ ] Add plan validation guardrails section
- [ ] Update replan loop description
- [ ] Update LLM retry/fallback description

### M1242 — Update tools.md with browser URL validation

**Problem:** tools.md doesn't mention that the browser tool rejects
`file://` URLs. A user trying to use browser on local files would be
confused by the rejection.

**Fix:** Add a note in the browser tool section (or a general "Tool
Validation" section) documenting that browser requires http/https URLs.

**Tasks:**
- [ ] Add browser URL validation note to tools.md

### M1243 — Fix config.md fallback model

**Problem:** config.md says `planner_fallback_model` defaults to
`google/gemini-2.5-flash-lite` but the code uses `minimax/minimax-m2.7`.

**Fix:** Update the default value in config.md.

**Tasks:**
- [ ] Fix fallback model default in config.md
