# kiso-run/core — v0.10 Development Plan

## Headline

**Convergence to Agent Skills + MCP, with Kiso-defined runtime policy,
full lifecycle parity with Claude Code / openclaw, and single-key
onboarding.**

Today kiso has four overlapping extension primitives — recipes,
wrappers, prompt modules, and MCP — with fuzzy routing rules
between them. The briefer (`kiso/brain/common.py:1382`) has to
disambiguate per task, the planner prompt has to explain the
difference, and contributors have to learn four concepts where
two would suffice. On top of that, the planner emits six task
types (`exec`, `wrapper`, `msg`, `search`, `mcp`, `replan`) —
two of which (`wrapper`, `search`) are internal capability
layers that duplicate what MCP already provides.

v0.10 collapses the model to **two orthogonal standards** (plus
exec as the fallback):

- **Agent Skills** (`agentskills.io`) — standard skill packages
  centered on `SKILL.md`, optionally bundling
  scripts/references/assets. They describe *how to think* about a
  class of problem. Replaces recipes, but not as a planner-only
  primitive.
- **MCP** (`modelcontextprotocol.io`) — the standardized protocol
  for *what to call*. Replaces wrappers **and the built-in search
  task type** as the capability surface.
- **Exec** — Pure shell commands. Unchanged.

The planner task taxonomy collapses from six to four:

```
before:  exec | wrapper | msg | search | mcp | replan
after:   exec | mcp     | msg | replan
```

- **exec** — unstructured shell command (universal fallback)
- **mcp** — structured call to any capability server
- **msg** — respond to the user
- **replan** — control flow: re-plan after investigation

Prompt modules (`<!-- MODULE: xxx -->` blocks in `planner.md`,
hardcoded list in `kiso/brain/common.py:1049-1053`) are NOT in
this convergence — they are prompt fragments, not capabilities,
and stay where they are (though their contents are rewritten in
M1505 to reflect the new taxonomy).

**Kiso/core does not keep a privileged in-process tool layer.**
Capability gaps in the public MCP ecosystem (codegen, web search,
transcription, OCR, local document parsing) are filled by
**standalone MCP servers published under the `kiso-run` GitHub
org and distributed via `uvx --from git+...@<tag>`**. They are
maintained on the same release cadence as `kiso/core` itself but
live in their own repos, identical in protocol status to any
third-party MCP server from the user's point of view.

**No PyPI.** Track B servers are consumed directly from their
GitHub repos via `uvx` git support: `uvx --from
git+https://github.com/kiso-run/aider-mcp@v0.1.0 kiso-aider-mcp`.
`uv` caches the git clone; release tags (`v0.1.0`, `v0.2.0`, ...)
are the stability boundary. The `kiso-<name>-mcp` identifier is
the entry-point name declared in each repo's `pyproject.toml` —
it is not a PyPI package.

What *is* still Kiso/core's job, and it is substantial:

- load standard skill packages and project only the role-relevant
  parts into prompts
- manage MCP install/bootstrap UX (`add/install/test/env/logs`)
- maintain trust policy, provenance, health checks, and migration
  affordances for users
- own the runtime policy gap that neither standard fills:
  per-session context, secret scoping, role visibility, schema
  validation, install-from-URL, trust tiers

In other words: **the standards stay standard; Kiso defines the
runtime policy around them, and the runtime policy is the
product.**

## Single-key philosophy

The default preset is designed so that **`OPENROUTER_API_KEY` is
the only mandatory secret** for full capability. All
kiso-maintained MCP servers that need an LLM or search backend
route through OpenRouter:

- **codegen** (aider) → LLM via OpenRouter
- **web search** → Perplexity Sonar via OpenRouter
- **transcription** → Gemini multimodal via OpenRouter
- **OCR** → Gemini vision via OpenRouter

Servers that need no API key at all (filesystem, fetch, browser,
docreader) work out of the box. `GITHUB_TOKEN` is the only
*optional* extra for the `github` server.

This collapses the onboarding from three required keys
(`OPENROUTER_API_KEY` + `GEMINI_API_KEY` + a search API key)
to **one**. The single-key constraint is a design invariant for
the default preset — any future server added to the preset must
either need no key or route through OpenRouter. This invariant
is enforced by CI (M1503).

## Claude Code / openclaw parity baseline

v0.10 must match the lifecycle UX of comparable generalist
agents. Concretely, every one of the following flows must work
end-to-end on a fresh install with only `OPENROUTER_API_KEY`:

1. **Install a skill from a link.** `kiso skill install
   --from-url <github|zip|raw SKILL.md>` lands the skill,
   validates frontmatter, warns on `scripts/` or `allowed-tools`,
   and makes it selectable by the briefer on the next message.
2. **Install an MCP from a link.** `kiso mcp install --from-url
   <pulsemcp|github|npm:|pypi:|server.json>` (already works
   today) and the new server is exposed to the planner.
3. **Planner proposes install when missing.** Goal mentions a
   domain with no skill or MCP covering it → planner produces
   msg-only plan proposing the install command → user approves
   → exec install → replan picks up the new capability.
4. **Briefer selects the right skill.** Skill metadata alone is
   enough; no vector retrieval needed because
   `activation_hints` pre-filter scales the catalog.
5. **Planner sees the right instructions.** Skill `## Planner`
   section is injected; `## Worker` stays out of planner
   context.
6. **Worker executes with skill context.** `## Worker` section
   reaches the exec translator prompt.
7. **Reviewer checks against `expect` plus skill `## Reviewer`**
   heuristics, without demoting `expect` as the primary
   criterion.
8. **MCP sessions stay isolated.** Two concurrent sessions
   hitting the same MCP server get isolated workspace contexts;
   `${session:workspace}` resolves per-call, not at daemon
   boot.
9. **Secrets stay scoped.** `kiso mcp env set <name> KEY VAL`
   writes a credential that is actually delivered to the
   subprocess at spawn, never bleeds across servers, and never
   appears in telemetry.
10. **Broken plugin is recoverable.** Unhealthy MCP server or
    missing skill surfaces to the planner with a concrete
    remediation step (reinstall/re-auth/report), not a silent
    failure.

Each line above maps to one or more of the milestones below.
Nothing in this list is deferred to v0.11. The intent is that at
v0.10 close, a new user can recreate the Claude Code experience
on kiso by pointing at a SKILL.md URL and an MCP server URL with
no manual file editing.

## Standards boundary

This plan follows two external standards and draws a hard line
between "standard" and "Kiso policy":

- **Agent Skills standard** (`agentskills.io`) defines the skill
  package shape: `SKILL.md`, YAML frontmatter, optional
  `scripts/`, `references/`, and other bundled assets. Standard
  frontmatter fields include `name`, `description`, `license`,
  `compatibility`, `metadata`, `allowed-tools`. The standard also
  defines naming conventions (lowercase, hyphens, 1-64 chars).
- **MCP standard** (`modelcontextprotocol.io`) defines the
  client/server protocol, lifecycle handshake, transports (stdio,
  streamable HTTP), and three primitives: **Tools** (executable
  functions), **Resources** (data sources), and **Prompts**
  (templates). Kiso already implements Streamable HTTP
  (2025-06-18) at `kiso/mcp/http.py:58`.

These standards do **not** decide:

- which internal Kiso role sees which skill content
- how Kiso selects skills for a request
- how Kiso installs or bootstraps a server package on the local
  machine
- how Kiso stores trust/provenance/health information
- how per-session context (workspace, secrets, user role) reaches
  long-lived MCP servers
- how capability discovery degrades on cold start

v0.10 therefore introduces a **Kiso runtime profile** on top of
both standards:

- **Kiso Skill Profile** — a backward-compatible convention for
  role-scoped consumption of a standard `SKILL.md`, with
  deterministic `activation_hints` pre-filtering.
- **Kiso MCP Runtime Policy** — a consistent install/bootstrap/
  health/trust/session layer around standard MCP servers.

## Prerequisites from v0.9

This plan assumes the v0.9 Phase 19 milestones (M1367-M1372)
have landed. In particular:

- **M1367** — `npx` is part of the runtime baseline. `uvx` is
  also available because `uv` is already installed for kiso
  itself, no extra work.
- **M1368** — planner routes Node tooling through `npx`, not
  global installs.
- **M1370** — the briefer already sees MCP methods as a
  first-class category in its input pool.
- **M1374** — pre-`list_methods` auth hook in MCP transports.
  v0.10 extends this into session-aware client initialization
  (M1512).

## Non-goals (explicit)

- **No vector retrieval / embeddings for skills.** Skill
  discovery remains metadata-driven through the briefer plus a
  deterministic `activation_hints` pre-filter (M1538). At >100
  installed skills the pre-filter keeps the briefer budget sane;
  embeddings remain off-scope.
- **No registry, no catalog runtime.** Kiso sees only what is
  installed locally in `mcp.json` and `~/.kiso/skills/`. If a
  capability is not installed, it does not exist for kiso. The
  planner prompt already contains this rule for MCP
  (`kiso/roles/planner.md:72-75`) and extends it to skills in
  M1505. `docs/recommended-mcps.md` and
  `docs/recommended-skills.md` compensate by listing community
  options that users can install manually.
- **No marketplace, no service.** The "ecosystem" is curated
  Markdown pages in the docs.
- **No tools in `kiso/core`.** Not as wrappers, not as builtin
  modules, not as in-tree MCP servers. Anything that does work
  for the user lives in a separate repo.
- **No pretending that MCP standardizes installation.** It
  doesn't. Kiso must still own the user-facing install/bootstrap
  workflow around standard servers.
- **No pretending that Agent Skills standardizes role routing.**
  It doesn't. Kiso must still decide how skills are projected into
  planner/worker/reviewer/messenger prompts.
v0.10 is the last cycle before public release. **Nothing that is
legitimately part of the MCP + Skills lifecycle is deferred to
v0.11.** That includes MCP Resources, MCP Prompts, MCP Sampling,
sandboxing parity, connector simplification, and behavior
injection into the messenger role. All land in v0.10 via
milestones in Phase 10.

## Design principles

1. **Standards first, Kiso profile second.** Never fork the skill
   or MCP standards when a local convention will do.
2. **One capability surface.** User-facing capabilities come from
   MCP, not from an internal wrapper subsystem or built-in task
   types.
3. **Role-scoped instruction flow.** Skills are not planner-only;
   each role sees only the slice relevant to it.
4. **CLI-managed lifecycle.** Users should not have to hand-edit
   config files to install, test, bootstrap, or inspect MCP
   servers or skills.
5. **Soft migration before hard deletion.** Remove wrappers from
   runtime routing first; delete compatibility shims only after
   the MCP path is feature-complete.
6. **Single key.** The default preset runs entirely on
   `OPENROUTER_API_KEY`. CI enforces that any kiso-maintained
   MCP in the preset needing a backend routes through OpenRouter.
7. **Per-session correctness over daemon-wide shortcuts.** Where
   the standards are session-agnostic and kiso is multi-session,
   kiso's runtime policy resolves per-session. No session bleed.
8. **Honest regressions.** If a feature is lost in the migration,
   it is named explicitly and either ported or documented as
   recovered elsewhere. No silent drop.
9. **No grace periods, no backward compatibility.** Kiso/core is
   pre-release. Every deprecation is a clean cut: no
   "accepted-but-ignored" schema fields, no dual-name env var
   fallbacks, no migration shims that survive past the PR that
   introduced them. If a rename or removal is the right call, it
   lands in one commit without a compatibility path. Existing dev
   configs, dev databases, and dev tests that relied on removed
   behavior get updated in the same commit — or deleted.

## Execution order (no-gap invariant)

**Invariant: zero user-visible capability gap at any commit on
`main`.** Every capability that works before a milestone continues
to work after it lands. The sequencing below respects this —
wrappers lose planner visibility only after their MCP replacements
ship and are registered in the default preset.

The Phase numbering below (Phase 1 … Phase 12) groups milestones
conceptually. The execution order is **different** from the phase
numbering, because Track B (sibling-repo rewrites) must land before
Track A breaks wrapper visibility in core.

### Sequence

**Phase α — MCP replacements ship** (Track B, sibling repos)

1. **M1507** — `kiso-run/aider-mcp` rewrite + `v0.1.0` tag ✅
2. **M1509** — `kiso-run/transcriber-mcp` rewrite + `v0.1.0` tag ✅
3. **M1510** — `kiso-run/ocr-mcp` rewrite + `v0.1.0` tag ✅
4. **M1511** — `kiso-run/docreader-mcp` rewrite + `v0.1.0` tag ✅
5. **M1508** — `kiso-run/search-mcp` new + `v0.1.0` tag ✅

During this phase core is unchanged. Wrappers continue to work via
the existing `wrapper` task type. At the end of phase α, all five
MCP servers are published on GitHub and consumable via
`uvx --from git+...@v0.1.0`, standalone.

**Phase β — Core wires the MCP replacements** (default preset)

6. **M1503** — `default.mcp.json` with the five GitHub URLs + tags, ✅
   `kiso init --preset`, standards docs, trust CI, single-key CI.

After β, a fresh install with only `OPENROUTER_API_KEY` can use
every wrapper-replaced capability via MCP (`kiso init --preset
default` → chat "trascrivimi questo audio" → transcriber-mcp path
runs end-to-end). Wrappers **also** still work — both paths are
green. This is the redundant safe state required before γ.

**Phase γ — Schema convergence** (safe to break wrapper visibility)

7. **M1502** — Briefer schema convergence: drop
   `wrappers`/`exclude_recipes`, add `skills`. Wrappers become
   invisible to the planner. Capabilities continue via MCP (every
   wrapper-replaceable capability has an MCP server registered in
   β). ✅
8. **M1540** — Thread skill role slices into planner / worker /
   reviewer prompts. 🟡 *(builders + unit tests shipped; caller
   wiring + integration test deferred — see milestone body.)*
9. **M1538** — `activation_hints` pre-filter (deterministic,
   pre-briefer). ✅
10. **M1539** — Per-user `[users.<name>.mcp]` role filtering. ✅
11. **M1541** — Recipe → skill migration script + `docs/recipes.md`
    → `docs/skills.md` rewrite. ✅ *(script shipped; docs rename
    deferred to M1523 docs sweep.)*

**Phase δ — Wrapper retirement** (safe, dead code)

12. **M1504** — Retire wrapper code. Wrappers have been invisible
    to the planner since γ; δ deletes the files, task type, CLI
    surface, and wrapper tests. 🟡 *(part 1 CLI retirement
    complete; task-type + source-file deletion pending a fresh
    session — see M1504 body.)*

**Phase ε — Remaining Track A surface**

13. **M1505** — `planner.md` routing rewritten as
    `skills_and_mcp`.
14. **M1506** — `kiso skill list/info/add/remove` CLI.
15. **M1512-M1519** — MCP lifecycle completeness (per-session
    pool, schema validation, install-from-URL, trust,
    `needs_install` for skills, per-user role filtering,
    cold-start, `x-kiso-consumes` / `mcp_recovery`).
16. **M1520** — Wire `## Messenger` skill sections into the
    messenger role.
17. **M1521-M1523** — Final cleanup (env-var unification, registry
    concept removal, docs sweep).

**Phase ζ — Completeness & launch**

18. **M1524-M1529** — Behavior injection, connector retirement,
    sandbox parity, MCP Resources / Prompts / Sampling.
19. **M1530-M1533** — Launch readiness (`kiso doctor`,
    end-to-end pipeline test, security audit, `install.sh`
    rewrite).
20. **M1535-M1537** — Quality of life (error-surface enrichment,
    `kiso session export/import`, OpenRouter cost tracking).

### Invariant checks between phases

- **After α**: `uvx --from git+https://github.com/kiso-run/<name>-mcp@v0.1.0`
  works for all five servers, standalone, against a minimal
  `mcp.json` config.
- **After β**: on a fresh install with only `OPENROUTER_API_KEY`,
  `kiso init --preset default` + chat "trascrivimi questo audio"
  runs end-to-end via the MCP path. Wrappers also still available.
- **After γ**: same chat test, now via the new briefer schema.
  Wrappers installed via `kiso wrapper install` still exist on
  disk but are invisible to the planner — chat still uses the MCP
  path. Test suite green.
- **After δ**: `kiso wrapper` CLI gone; only MCP + skills remain.
  All capabilities that used to work still work via MCP.
- **After ε-ζ**: public-release readiness.

### Why this order and not "Track A first"

Executing Track A first (M1502 before Track B) creates a temporary
capability gap: wrappers invisible but MCP replacements not yet
available. Even in a dev cycle with no releases between commits,
this gap prevents meaningful end-to-end testing after M1502. The
sibling repos have **no dependency** on Track A work — they use
the standard `mcp` Python SDK — so there is no reason to defer
them. Track B first, always.

---

## Trust rule for the default preset

The default preset shipped with `kiso init --preset default`
contains **only MCP servers maintained by trustworthy upstreams**:

- **Tier 1**: organizations with public release pipelines and
  verifiable track records (Anthropic `@modelcontextprotocol/*`,
  Microsoft `@playwright/mcp`, GitHub `@github/*`).
- **Tier 2**: `kiso-run/*-mcp` servers maintained by the same
  team that maintains `kiso/core`. Listed at the same trust
  level only because the kiso project itself is the upstream.

Explicitly excluded from the default preset:
- Single-developer experiments, however popular.
- Forks of enterprise servers.
- Servers requiring paid API keys *for basic operation* (free
  tier OK; required paid key not OK in default).
- Community servers from public registries (`mcp.so`,
  `glama.ai`, `smithery.ai`) without org backing.

These can still be **recommended in docs** under
`docs/recommended-mcps.md` with explicit disclaimers about
provenance and maintenance status — but they are not in the
default preset, ever.

Every future PR that modifies `kiso/presets/default.mcp.json` must
demonstrate that the new entry passes this rule. The rule lives
in the `Done when` of M1503 and in the docs page itself.

For **user-installed** MCP servers and skills (outside the
preset), kiso maintains a **trust store** at `~/.kiso/trust.json`
managed via `kiso mcp trust add/remove/list` and `kiso skill
trust add/remove/list` (M1515). Untrusted sources are not
blocked; they install with a loud warning that the user must
acknowledge. This mirrors `pip install` and `npx` UX.

## Kiso Skill Profile

Kiso consumes **standard Agent Skills** from `~/.kiso/skills/`.
The canonical package form is:

```text
~/.kiso/skills/<name>/
├── SKILL.md
├── scripts/...
├── references/...
└── assets/...
```

Single-file skills remain acceptable for lightweight cases, but
directory skills are the default because they match the standard
best and allow bundled assets.

`SKILL.md` remains standard Markdown with YAML frontmatter.

**Standard fields** (defined by `agentskills.io`):
- `name` (required, 1-64 chars lowercase+hyphens)
- `description` (required, 1-1024 chars)
- `license`, `compatibility`, `metadata`, `allowed-tools`
  (optional)

**Kiso extension fields** (optional, backward-compatible):
- `when_to_use` — natural-language activation hint for the briefer
- `audiences` — list of role names that should see this skill
  (subset of `planner`, `worker`, `reviewer`, `messenger`;
  defaults to all roles when absent)
- `activation_hints` — structured pre-filter: `{applies_to:
  ["...", "..."], excludes: ["...", "..."]}` evaluated
  deterministically before the briefer runs. Equivalent to the
  current `applies_to`/`excludes` in recipes.
- `version` — semantic version string

The loader accepts all frontmatter keys permissively. Standard
fields are preserved as-is; Kiso extension fields enable richer
runtime behavior when present.

**Naming convention** (per the standard): lowercase, hyphens
only, 1-64 characters. The loader validates this.

The body may optionally contain role-scoped headings:
`## Planner`, `## Worker`, `## Reviewer`, `## Messenger`.
If those headings are absent, the body defaults to
**planner-only guidance** for backward compatibility with
recipe-like skills.

This gives Kiso a portable default:

- a generic skill from the wider ecosystem still works
- a Kiso-authored skill can be richer without breaking the
  standard
- a migrated recipe (planner-only body) still works unchanged

## Role visibility model

### Briefer

The briefer sees **skill metadata only**, never full skill bodies:

- name
- description
- `when_to_use` when present
- `audiences` summary (for scoping hints)

The briefer returns a **positive selection**:

- `skills: ["python-debug", "research-verify"]`

It no longer returns `exclude_recipes`.

Before the briefer runs, an `activation_hints` pre-filter (M1538)
deterministically removes skills whose `applies_to` does not
match the current message OR whose `excludes` matches. This
keeps briefer prompt size bounded regardless of how many skills
the user has installed.

### Planner

The planner sees:

- briefer-selected skills
- only the `## Planner` section for each selected skill, or the
  full body when no role sections exist
- available MCP methods with their descriptions and input
  schemas (M1513)

This is the main replacement for today's recipe flow.

### Worker / exec translator

The worker sees:

- task detail
- system environment
- preceding task outputs
- only the `## Worker` sections for skills relevant to the
  current task

This replaces today's narrow `Recipe Contracts` section
(`kiso/brain/text_roles.py:449-466`, `_add_section(context_parts,
"Recipe Contracts", recipe_contracts_text)`) with an explicit
role-scoped mechanism. Worker skill sections are injected into
the exec translator prompt under a new `## Skills (worker
guidance)` header, adjacent to the existing `## System
Environment`, `## Workspace Files`, `## Preceding Task
Outputs`, and `## Task` headers.

### Reviewer

The reviewer still treats `expect` as the primary success
criterion, but may also receive:

- only the `## Reviewer` sections for skills relevant to the
  current task

Injected into the reviewer prompt
(`kiso/brain/reviewer.py:250-290`) under a new `## Skills
(reviewer heuristics)` header, after `## Expected Outcome` and
before `## Actual Output`. The section docstring inside the
prompt explicitly frames these as heuristics supporting
`expect`, never overriding it.

### Messenger

Messenger receives only `## Messenger` sections for skills
relevant to the current response. Wired in M1520. Injected into
the messenger prompt alongside `MemoryPack` context, under a
new `## Skills (output style)` header.

## Secret management for MCP

MCP servers receive secrets via two channels, both owned by
Kiso's runtime policy (since the MCP standard defines none):

1. **`env` block in `mcp.json`** — static per-server env vars.
   Supports `${env:VAR}` expansion at parse time (existing
   behavior from `kiso/mcp/config.py:204-221`).
2. **Per-server credential files** at `~/.kiso/mcp/<name>.env`
   with mode `0600`, managed via `kiso mcp env set/unset/show`.
   **Today these files are written by CLI but never read by the
   spawner** — a silent integration gap. M1512 wires the spawner
   to merge these files into the subprocess environment at
   client initialization, closing the gap.

Per-session ephemeral secrets (analog of wrapper
`session_secrets`, `kiso/wrappers.py:88-90`) are handled by
M1512's per-session client pool: when a session contributes a
secret that a specific MCP method declares as required, Kiso
spawns a session-scoped client instance with that secret in its
environment, and the client is evicted on session end.

`OPENROUTER_API_KEY` is the only secret kiso-maintained servers
need. It flows as follows:
- user env → `KISO_LLM_API_KEY` (already set for LLM calls)
- preset `[mcp.aider].env` contains `{OPENROUTER_API_KEY =
  "${env:KISO_LLM_API_KEY}"}` (canonical rename at preset
  install time)
- spawner merges `~/.kiso/mcp/aider.env` on top (user override
  possible)

## Health monitoring for MCP

Kiso applies two tiers of health monitoring:

- **kiso-maintained servers** (`kiso-run/*-mcp`): each exposes a
  `doctor` tool that checks dependencies and credentials. The
  CLI can invoke it via `kiso mcp test <name>` which will
  automatically call `doctor` when the server declares it.
- **Third-party servers**: no `doctor` contract exists. Kiso
  relies on the existing **circuit breaker** in
  `kiso/mcp/manager.py:248-261` (3 failures in 60 seconds →
  server marked unhealthy, retry after cooldown). The MCP client
  also retries once on `MCPTransportError` before failing the
  task.

The `kiso mcp test` command works on both tiers: for
kiso-maintained servers it calls `doctor`; for third-party
servers it sends `initialize` + `tools/list` + shutdown and
reports success/failure (existing behavior at
`cli/mcp.py:386-420`).

To eliminate the cold-start briefer gap (empty
`list_methods_cached_only` until first explicit call), M1518
adds **background catalog warm-up** on daemon startup: an async
task iterates configured servers and primes
`method_cache` non-blockingly. The briefer on a first message
that races the warm-up sees a partial catalog rather than an
empty one, and by the second message the cache is fully warm.

## `needs_install` flow after wrapper removal

Today the planner sets `needs_install=true` to propose a wrapper
installation (msg-only → user approves → exec install → replan).
After v0.10, the same UX applies to both MCP servers and
skills:

- The planner detects that a capability is missing from the
  available MCP methods OR that no installed skill covers the
  current domain.
- It sets `needs_install=["<name-or-url>"]` and emits a `msg`
  task proposing:
  - `kiso mcp install --from-url <url>` (if MCP), or
  - `kiso skill install --from-url <url>` (if skill)
- The user approves.
- The planner re-plans with the new capability available.

The underlying `needs_install` field and the install-approved
routing suppression (M1234) are unchanged. Only the *target* of
the install extends from wrappers to MCP servers AND skills
(M1516).

Since kiso maintains no registry, the planner never invents
URLs — it asks the user for the concrete source. The hard rule at
`kiso/roles/planner.md:74` ("Kiso does NOT maintain a registry of
MCP servers") is preserved verbatim for MCP and mirrored for
skills in M1505.

## Per-session MCP client pool

A critical architectural shift in v0.10. Today `MCPManager`
(`kiso/mcp/manager.py:67-85`) keys its client pool by
`server_name` only, and `${env:VAR}` substitution in
`kiso/mcp/config.py:204-221` happens at parse time. A default
preset entry like `"${WORKSPACE}"` in `args` is therefore baked
into the `MCPServer` dataclass at daemon boot and cannot change
per session — a correctness bug for any multi-session daemon
running a session-scoped server like `@modelcontextprotocol/server-filesystem`.

M1512 reworks the pool:

- Pool key becomes `(server_name, scope_key)` where `scope_key`
  is `"_global"` by default and `session_id` when the server's
  config references any `${session:*}` token or opts into
  per-session via `[mcp.<name>] scope = "session"`.
- `kiso/mcp/config.py` gains `${session:workspace}` and
  `${session:id}` placeholders. These are NOT expanded at parse
  time; they are preserved verbatim and resolved per-call inside
  `MCPManager._get_or_spawn(name, scope)` using the caller's
  session context.
- `MCPManager.call_method(name, method, args, *, session: str
  | None = None)` gains a session parameter. `kiso/worker/mcp.py`
  passes `ctx.session`.
- Idle eviction: a per-session client unused for
  `mcp_session_idle_timeout` seconds (default 1800s, configurable
  in `config.toml`) is gracefully shut down and evicted.
- Pool bound: at most `mcp_max_session_clients_per_server`
  clients per server (default 32, configurable). LRU eviction
  above the bound. Health state is per `(server, scope_key)`.

Servers that do not reference `${session:*}` continue to pool
globally with no behavioral change.

## Architecture after v0.10

```
kiso-run/                              ← GitHub org
├── core/                              ← runtime (this repo)
│   ├── kiso/brain/                    ← planner, briefer, ...
│   ├── kiso/worker/                   ← exec, MCP client, ...
│   ├── kiso/mcp/                      ← MCP client + install/bootstrap policy
│   │   ├── config.py                  ← ${session:*} substitution (M1512)
│   │   ├── manager.py                 ← per-session pool (M1512)
│   │   ├── warmup.py                  ← NEW (catalog warm-up, M1518)
│   │   └── trust.py                   ← NEW (trust store, M1515)
│   ├── kiso/skill_loader.py           ← NEW (M1500)
│   ├── kiso/skill_runtime.py          ← NEW (M1501)
│   ├── kiso/skill_install.py          ← NEW (from-URL resolver, M1514)
│   ├── kiso/skill_trust.py            ← NEW (trust tiers, M1515)
│   ├── kiso/presets/default.mcp.json  ← NEW (M1503)
│   ├── cli/skill.py                   ← NEW (M1506, extended in M1514-M1515)
│   ├── docs/standards.md              ← NEW (M1503)
│   ├── docs/default-preset.md         ← NEW (M1503)
│   ├── docs/recommended-mcps.md       ← NEW (M1503)
│   └── docs/recommended-skills.md     ← NEW (M1515)
│
├── aider-mcp/                         ← MCP server, our maintenance, uvx+git
├── search-mcp/                        ← MCP server, our maintenance, uvx+git
├── transcriber-mcp/                   ← MCP server, our maintenance, uvx+git
├── ocr-mcp/                           ← MCP server, our maintenance, uvx+git
└── docreader-mcp/                     ← MCP server, our maintenance, uvx+git
```

Files removed from `kiso/core` in v0.10:

- `kiso/wrappers.py`
- `kiso/registry.py`
- `kiso/recipe_loader.py`
- `kiso/worker/wrapper.py`
- `kiso/worker/search.py` (search handler, replaced by MCP)
- `kiso/roles/searcher.md` (killed outright — search-mcp in
  M1508 calls Sonar directly, no prompt migration needed)
- `cli/wrapper.py`
- `cli/recipe.py`
- `~/.kiso/wrappers/` directory tree (init no longer creates it)
- `~/.kiso/recipes/` directory tree (init no longer creates it)
- Stale entries in `registry.json` on the `main` branch (file
  is emptied to `{}` or removed)

Net target: **at least 1500 lines deleted** from `kiso/core`,
zero net additions of privileged tool code. This is a goal, not
a correctness criterion — if the net is -1300 lines because
Phase 7 adds runtime policy code, that's acceptable.

Files explicitly retained in `kiso/core`:

- `cli/mcp.py` (extended in M1512, M1515)
- `kiso/mcp/install.py` (unchanged)
- `kiso/mcp/manager.py` (reworked in M1512)
- `kiso/mcp/auth.py` (extended in M1512 for session-scoped
  credentials)
- any small metadata/health/provenance helpers needed to make
  MCP usable without manual file editing

## MCP configuration and install UX

Kiso adopts **standard `mcp.json` semantics** as the user-facing
shape, but keeps a first-class CLI for manipulating it. The
canonical user flows remain:

- `kiso mcp add`
- `kiso mcp install --from-url <url>`
- `kiso mcp test`
- `kiso mcp env set/unset/show`
- `kiso mcp logs`
- `kiso mcp trust add/remove/list` (NEW, M1515)

Manual editing remains possible, but it is not the recommended
path.

Beside the MCP config itself, Kiso maintains local metadata
files under `~/.kiso/mcp/` for:

- provenance (`installed_via`, source URL, trust tier) —
  `~/.kiso/mcp/<name>.provenance.json` (M1515)
- credentials (`<name>.env`, mode 0600) — wired to spawner in
  M1512
- stderr logs (`<name>.err.log`)
- OAuth tokens (`credentials/<name>.json`, mode 0600) — existing
  behavior

These metadata files are **not** part of the MCP standard; they
are purely Kiso runtime policy.

## Skill CLI and install UX

Parallel command surface for skills:

- `kiso skill list` (M1506)
- `kiso skill info <name>` (M1506)
- `kiso skill add <path>` (M1506) — copy a local directory
- `kiso skill install --from-url <url>` (M1514) — download,
  validate, install
- `kiso skill remove <name>` (M1506)
- `kiso skill test <name>` (M1515) — frontmatter + referenced
  file integrity check
- `kiso skill trust add/remove/list` (M1515)

URL forms supported by `kiso skill install --from-url` (M1514):

- `https://github.com/<owner>/<repo>` — clones repo; expects
  either a top-level `SKILL.md` or a `skills/<name>/SKILL.md`
  subdirectory; copies the matching skill dir into
  `~/.kiso/skills/`.
- `https://github.com/<owner>/<repo>/tree/<ref>/path/to/skill`
  — installs the pointed subdirectory.
- `https://raw.githubusercontent.com/.../SKILL.md` — fetches a
  single-file skill; creates `~/.kiso/skills/<derived-name>/SKILL.md`.
- `https://agentskills.io/skills/<slug>` — fetched through the
  agentskills.io public manifest endpoint (if/when available).
  Until then, translates to the backing github.com URL.
- Zip URL (`*.zip`) — downloads, unpacks, expects a directory
  with `SKILL.md` at root or one level deep.
- Bare local path — `kiso skill add` equivalent; kept for
  composability.

Pre-install checks (M1515):
- trust tier lookup against `~/.kiso/trust.json` + hardcoded
  Tier 1 list
- presence of `scripts/` → warning (requires `--yes` or
  interactive confirmation if untrusted)
- `allowed-tools` frontmatter referencing `Bash(*)`, `Write(*)`,
  or network tools → warning
- frontmatter naming validation per the standard
- SKILL.md size sanity check (> 50KB is flagged)

---

# Track A — `kiso/core` cleanup

Thirty-two milestones inside this repo, organized in twelve phases.
Order matters: skill loader → role projection → briefer schema →
preset/docs → wrapper retirement → planner prompt → skill CLI →
lifecycle completeness → messenger completion → final cleanup →
completeness & consolidation → launch readiness → quality of life.
v0.10 is the last cycle before public release; nothing
legitimately part of the MCP + Skills lifecycle — or of being a
production-grade launch target — is deferred past this cycle.

## Phase 1 — Skill foundation (M1500-M1501, M1540-M1541)

### M1500 — `kiso/skill_loader.py` and standard skill discovery ✅

**Why.** Skills replace recipes as the instruction primitive.
They need a loader for **standard Agent Skills**, not a custom
markdown format.

**Approach.** New `~/.kiso/skills/` root. Primary shape is a
directory `<name>/` containing `SKILL.md` plus optional
`scripts/`, `references/`, and `assets/`. A single-file fallback
may be accepted for lightweight local skills, but directory
skills are the standard-first path.

The loader returns `Skill` dataclasses containing:

- **standard fields**: `name`, `description`, `license`,
  `compatibility`, `metadata`, `allowed_tools`
- **Kiso extension fields**: `when_to_use`, `audiences`,
  `activation_hints` (structured: `{applies_to: [...],
  excludes: [...]}`), `version`
- the raw body
- bundled root path
- parsed role sections when present (`planner`, `worker`,
  `reviewer`, `messenger`)

**Tasks**:
- [x] `kiso/skill_loader.py` with `discover_skills()`, TTL cache
      (30s parity with `recipe_loader`), `invalidate_skills_cache()`.
- [x] `Skill` dataclass with standard + Kiso extension fields.
- [x] Standard `SKILL.md` parser using `yaml.safe_load` (pyyaml
      added as a dependency).
- [x] Validate skill naming convention per Agent Skills standard:
      lowercase, hyphens only, 1-64 characters. Invalid names
      rejected with a warning log.
- [x] Role section extractor: split body on H2 headings matching
      `^## (Planner|Worker|Reviewer|Messenger)$` (case-insensitive),
      return dict keyed by lowercased role name.
- [x] Unit tests: 28 tests covering directory skill, single-file
      fallback, malformed frontmatter, cache invalidation, naming
      validation, role-section extraction, frontmatter fields.
- [x] `kiso/config.py` exposes `SKILLS_DIR = KISO_DIR / "skills"`
      constant.
- [x] `kiso/main.py` ensures the skills dir is precreated at init.
- [x] Commit & push.

**Done when**:
- A skill at `~/.kiso/skills/foo/SKILL.md` (or lightweight
  fallback file form) is discovered, parsed, and returned by
  `discover_skills()`.
- All standard frontmatter fields are preserved; Kiso extensions
  are parsed when present.
- Non-conforming skill names are rejected or warned.
- Bundled assets are accessible via `Skill.bundled_root`.
- Role sections are extracted into `Skill.role_sections` dict.

---

### M1501 — Kiso Skill Profile: role-scoped projection module ✅

**Status**: Module implemented (✅ `kiso/skill_runtime.py`, 21 unit
tests green).

The remaining work originally bundled here was split out into
focused follow-up milestones (2026-04-18 restructure):

- **M1540** — Thread skill role slices into planner / worker /
  reviewer prompt builders. This was originally folded into M1502
  "because it requires coordinated updates to briefer mocks"; after
  re-analysis M1502 can ship the new briefer schema without the
  prompt threading (planner reads the new `skills` field as metadata
  only, same shape as today's `wrappers`). M1540 then enriches those
  sections with role-scoped skill bodies.
- **M1541** — `kiso-migrate-recipes-to-skills` conversion script +
  `docs/recipes.md` → `docs/skills.md` rewrite + integration test
  that a skill with all four role sections routes correctly in a
  live planner run.

**Why.** Recipes today are planner-only. Skills in v0.10 must be
usable across roles without inventing a non-standard package
format.

**Approach.** Introduce a small `skill_runtime` layer that reads
standard skills and projects them by audience:

- `metadata_for_briefer(skill) -> dict` — name, description,
  `when_to_use`, `audiences` summary; no body
- `instructions_for_planner(skill) -> str`
- `instructions_for_worker(skill) -> str`
- `instructions_for_reviewer(skill) -> str`
- `instructions_for_messenger(skill) -> str` — defined; wired
  in M1520

Body parsing is convention-based:

- if role headings exist, use them
- otherwise default the whole body to planner guidance

Prompt placement contract (this is the part M1501 must not leave
ambiguous):

| Role prompt builder | Injection point (existing section) | New section header |
|---|---|---|
| `build_planner_messages()` (`kiso/brain/planner.py:1259-1289`) | after `## Available Recipes`, before/replacing `## Wrappers` | `## Skills (planner guidance)` |
| `build_exec_translator_messages()` (`kiso/brain/text_roles.py:449-466`) | replacing the `Recipe Contracts` section entirely, between `## Preceding Task Outputs` and `## Task` | `## Skills (worker guidance)` |
| `build_reviewer_messages()` (`kiso/brain/reviewer.py:250-290`) | after `## Expected Outcome`, before `## Actual Output` | `## Skills (reviewer heuristics)` |
| Messenger prompt builder (`kiso/brain/text_roles.py` messenger path) | after `MemoryPack` context block (wired in M1520) | `## Skills (output style)` |

Recipe migration happens on top of this profile, so converted
recipe-like skills continue to work unchanged. Recipe
`applies_to`/`excludes` selectors are migrated to the Kiso
extension field `activation_hints` (same semantic, structured).
Recipe `runtime_contract` inference
(`kiso/recipe_loader.py:225-242`) output maps to a synthetic
`## Worker` section like:

```markdown
## Worker
Prefer a valid JSON object when producing structured output.
```

or the `key_value` variant, mirroring the existing
`build_recipe_runtime_contracts_text()` output.

**Tasks (delivered by this milestone)**:
- [x] Add `kiso/skill_runtime.py` with the five projection
      functions.
- [x] Define heading-based role section extraction (implemented
      in M1500; consumed here).
- [x] Respect the `audiences` frontmatter field: if absent,
      project to all roles; if present, skip projection for
      roles not listed.
- [x] Define messenger projection API (no runtime wiring yet;
      wired in M1520).
- [x] Unit tests for each projection function (21 tests green).
- [x] Commit & push.

**Tasks moved to M1540** (role-scoped prompt threading):
- Thread planner role slices into `build_planner_messages()`
- Thread worker role slices into `build_exec_translator_messages()`
  (removes the explicit `Recipe Contracts` section)
- Thread reviewer role slices into `build_reviewer_messages()`
- Integration test: skill with all four role sections routes
  correctly to each role prompt

**Tasks moved to M1541** (recipe→skill migration):
- Conversion script `kiso-migrate-recipes-to-skills`
- Run migration against fixture set of recipe files
- Update docs from recipes to skills terminology
  (`docs/recipes.md` → `docs/skills.md`)

**Done when** (for this milestone, narrowed):
- `kiso/skill_runtime.py` exposes the five projection APIs with
  unit-test coverage.
- The `audiences` frontmatter field is honored.
- Messenger projection API exists (wiring in M1520).

The broader "Done when" criteria (end-to-end role routing,
migration completeness) are covered by M1540 and M1541.

---

### M1540 — Thread skill role slices into planner/worker/reviewer prompts ✅

**Why.** `kiso/skill_runtime.py` (M1501) exposes
`instructions_for_planner/worker/reviewer` but no prompt builder
consumes them yet. Until this milestone, a skill with `## Worker`
or `## Reviewer` sections has its body silently ignored outside the
planner path. This wires the runtime into the three role prompts.

M1540 runs **after** M1502 (briefer schema convergence) so the
planner is already reading `briefing["skills"]` in the new schema
and listing skill metadata. M1540 upgrades that listing from
names-and-descriptions to role-scoped bodies.

**Approach.** Keep the injection contract from M1501's placement
table:

| Role prompt builder | Injection point | Section header |
|---|---|---|
| `build_planner_messages()` (`kiso/brain/planner.py:1259-1289`) | after `## Available Recipes`, before/replacing `## Wrappers` | `## Skills (planner guidance)` |
| `build_exec_translator_messages()` (`kiso/brain/text_roles.py:449-466`) | replacing the `Recipe Contracts` section entirely, between `## Preceding Task Outputs` and `## Task` | `## Skills (worker guidance)` |
| `build_reviewer_messages()` (`kiso/brain/reviewer.py:250-290`) | after `## Expected Outcome`, before `## Actual Output` | `## Skills (reviewer heuristics)` |

The worker injection removes the existing `Recipe Contracts`
section handling in `build_exec_translator_messages` — its content
is now carried by worker skill sections produced by
`skill_runtime.instructions_for_worker()`. Legacy recipe runtime
contracts are already being migrated to synthetic `## Worker`
sections in M1541, so the content is preserved across the
transition.

**Status (2026-04-20)**: caller wiring landed. `build_planner_messages`
now accepts an `out_state` dict and exposes the briefer-selected
`Skill` objects; `run_planner` attaches them to the plan as
`plan["_selected_skills"]`. `_run_planning_loop` threads them into
`_execute_plan`, which seeds a new `_PlanCtx.selected_skills` field.
`run_worker` (exec translator) and `_review_task` / `run_reviewer`
forward the list down to `build_exec_translator_messages` and
`build_reviewer_messages`, closing the end-to-end path. The
reviewer system prompt still states `expect` is the sole criterion
so a skill `## Reviewer` body remains heuristic, never
authoritative. The `Recipe Contracts` section is gone with the
recipe subsystem retirement (M1504). Live-integration test with
messenger wiring stays paired with M1520.

**Tasks**:
- [x] Thread planner slices into `build_planner_messages()`
      using `skill_runtime.instructions_for_planner()` for each
      skill selected by the briefer (or listed at full catalog
      when briefer is disabled).
- [x] Thread worker slices into `build_exec_translator_messages()`
      (builder-level opt-in). Removal of the explicit `Recipe
      Contracts` section and the corresponding caller wiring
      still pending — deferred below.
- [x] Thread reviewer slices into `build_reviewer_messages()`
      (builder-level opt-in).
- [x] Unit test: planner prompt contains `## Skills (planner
      guidance)` with the selected skill's planner body.
- [x] Unit test: worker prompt contains `## Skills (worker
      guidance)`; a skill with no `## Worker` section yields no
      worker injection for that skill. *(Replacement of the
      `Recipe Contracts` section deferred with the caller
      wiring.)*
- [x] Unit test: reviewer prompt contains `## Skills (reviewer
      heuristics)` AFTER `## Expected Outcome` and BEFORE
      `## Actual Output`; `## Expected Outcome` remains the
      primary criterion.
- [x] Caller wiring: plumb `run_planner` → `_run_planning_loop`
      → `run_worker` / `run_review` so `selected_skills` actually
      reach the worker and reviewer prompts in production.
      `Recipe Contracts` already removed with M1504.
- [x] Reviewer contract assertion: `build_reviewer_messages`
      keeps the modular `Sole criterion is `expect`` clause in
      the system prompt whenever a skill `## Reviewer` body is
      present, so the skill heuristic stays subordinate to
      `expect`.
- [ ] *(deferred → M1520)* Integration test: a skill with all
      four role sections reaches planner / worker / reviewer /
      messenger in a live planner run. Blocks on the messenger
      wiring landed in M1520.
- [x] Commit & push.

**Done when**:
- A skill with `## Worker` / `## Reviewer` sections reaches those
  roles through the role-scoped projections.
- A skill without role sections behaves like a recipe today
  (planner-only).
- Worker skill sections replace the `Recipe Contracts` section in
  the exec translator prompt.
- Reviewer skill sections do not demote `## Expected Outcome` as
  the primary judgement criterion.

---

### M1541 — Recipe → Skill migration script and docs rewrite ✅

**Why.** M1500/M1501 established skills as the v0.10 instruction
primitive, but the existing recipes in `~/.kiso/recipes/*.md` need
an automatic migration path. `docs/recipes.md` is also still live
and must be replaced with a skills-centric doc that matches the
new model.

**Approach.** A CLI tool `kiso-migrate-recipes-to-skills` reads
`~/.kiso/recipes/*.md`, produces `~/.kiso/skills/<name>/SKILL.md`
with `applies_to`/`excludes` mapped to the Kiso extension field
`activation_hints`, body under `## Planner`, and synthesized
`## Worker` section when `runtime_contract` is detected. The tool
is idempotent and preserves existing user skills (never
overwrites).

Docs: rename `docs/recipes.md` → `docs/skills.md`, rewrite content
around the Agent Skills model.

**Tasks**:
- [x] `kiso-migrate-recipes-to-skills` CLI (installed via
      `pyproject.toml` console script).
- [x] Mapping: `applies_to` + `excludes` → `activation_hints:
      {applies_to: [...], excludes: [...]}`.
- [x] Body placement: original recipe body goes under
      `## Planner`.
- [x] Runtime contract synthesis: when
      `build_recipe_runtime_contracts_text()` infers a contract,
      emit a `## Worker` section mirroring the existing output
      format (JSON-object / key_value variants).
- [x] Idempotent: re-running the tool leaves existing skills
      untouched; only creates skills for recipes that have no
      corresponding `~/.kiso/skills/<name>/SKILL.md`.
- [x] Fixture test: run migration against a fixture set of
      recipe files; verify the generated skills parse cleanly
      via `discover_skills()` and reach the planner.
- [ ] *(deferred to M1523 docs sweep)* Rename
      `docs/recipes.md` → `docs/skills.md`; rewrite for the
      skills model (directory layout, frontmatter,
      activation_hints, role sections).
- [x] Commit & push.

**Done when**:
- Every shipped recipe has a corresponding skill generated by
  the migration script.
- Running the script a second time makes no changes
  (idempotent).
- `docs/skills.md` exists, covers the Agent Skills model, and
  `docs/recipes.md` no longer exists.

---

## Phase 2 — Briefer schema convergence (M1502, M1538, M1539)

### M1502 — Briefer schema convergence ✅

**Status (2026-04-19)**: landed. BRIEFER_SCHEMA now has
`skills`/`mcp_methods` (no `wrappers`/`exclude_recipes`).
`_CONTEXT_POOL_SECTIONS` replaces `wrappers` with `skills`. Post-filter
validates skill names against the catalog. `briefer.md` rewritten.
Planner consumer reads `briefing["skills"]` and injects a `## Skills`
section; the `## Wrappers` section is gone (wrappers become invisible
to planner). Exclude-recipes mechanism retired; M1541 completes
recipe→skill migration.

Test suite: 4578 passed across ~1200 briefer-mock migrations. 4
pre-existing aider-validator failures unrelated to M1502. No
regressions.

**Scope narrowed (2026-04-18).** Originally M1502 bundled three
concerns: the briefer JSON schema change, the `activation_hints`
pre-filter, and per-user MCP role filtering. These were split into
M1502 / M1538 / M1539 so each ships as a focused TDD cycle:

- **M1502** (this milestone) — JSON schema + context pool +
  post-filter + `briefer.md` rewrite + test migration
- **M1538** — `activation_hints` pre-filter (deterministic,
  pre-briefer)
- **M1539** — Per-user MCP method role filtering

**Why.** Today the briefer JSON output
(`kiso/brain/common.py:1023-1032`) has 8 fields covering 4
categories. After convergence the briefer reasons about two
orthogonal primitives: skills and MCP methods. Its output
schema simplifies accordingly. The fewer categories the briefer
has to disambiguate, the fewer routing errors.

**Approach.**

- New briefer schema: `skills`, `mcp_methods`, `modules`,
  `context`, `output_indices`, `relevant_tags`,
  `relevant_entities`. Drop `wrappers` and `exclude_recipes`.
- Update `kiso/brain/common.py:1023` (response schema),
  1263-1280 (context pool sections: replace `wrappers` and
  `recipes` with `skills`), 1480-1496 (post-filter to drop
  hallucinated skill names), and the consumers in
  `kiso/brain/planner.py:1259-1289` and `1291-1338` (fallback
  path). The planner consumer reads `briefing["skills"]` and
  injects a `## Skills` section listing skill names +
  descriptions (metadata-only; M1540 enriches with role-scoped
  bodies).
- Feed the briefer only **skill metadata**, not full skill
  bodies.
- Rewrite `kiso/roles/briefer.md` rules: skills + MCP categories,
  positive skill selection, no recipe exclusion model.
- `kiso/worker/message_flow.py:184-186` (messenger briefer call)
  already forces wrappers/modules empty — update to force
  `skills=[]` where the caller handles skills separately.

**Tasks**:
- [ ] Update briefer JSON schema (`common.py:1023`).
- [ ] Update context pool sections tuple (`common.py:1263-1280`).
- [ ] Update post-filter to validate against the skill catalog
      (`common.py:1480-1496`), using `_filter_briefer_names()`
      pattern at `common.py:1419-1443`.
- [ ] Update planner consumer (`planner.py:1259-1289`,
      `1291-1338`) to read `briefing["skills"]` and inject a
      `## Skills` section with names + descriptions.
- [ ] Rewrite `kiso/roles/briefer.md`.
- [ ] Update messenger briefer call site
      (`kiso/worker/message_flow.py:184-186`).
- [ ] Migrate every existing test that mocks the briefer to the
      new schema. Grep for `exclude_recipes` and `wrappers` keys
      in `tests/` and update in the same PR.
- [ ] Unit test: post-filter drops hallucinated skill names,
      keeps catalog names.
- [ ] Commit & push.

**Done when**:
- Briefer returns the new schema.
- All mocked-briefer tests updated; suite green.
- `wrappers` and `exclude_recipes` fields no longer appear
  anywhere in the briefer codepath.
- The planner prompt contains a `## Skills` section populated
  from `briefing["skills"]` (metadata-only; M1540 enriches with
  role-scoped bodies).

---

### M1538 — `activation_hints` pre-filter (deterministic, pre-briefer) ✅

**Why.** With 50+ skills installed the briefer prompt bloats
linearly. The `activation_hints` pre-filter is the deterministic
gate that keeps the skill catalog small and relevant. It is the
scalability mechanism that makes "no embeddings" viable for
real-world ecosystems.

**Approach.** Implement
`skill_runtime.filter_by_activation_hints(skills, message,
is_replan)` with word-boundary tokenization matching the recipe
rules (`kiso/recipe_loader.py:135-183`), so migrated recipes
behave identically. Wire it into `build_planner_messages()`
before the context pool is built. Runs regardless of whether the
briefer is enabled (~1ms).

**Approach details.**

- `applies_to` hints: skill is eligible only if the message
  matches at least one selector (word-boundary match).
- `excludes` hints: skill is eliminated if the message matches.
- Empty `activation_hints` → always eligible (backward-compat
  with skills that don't declare hints).
- Multi-word selectors match as ordered token sequences.
- `is_replan=True` → bypass the filter (the planner may need
  skills that weren't implied by the original message).

Add `briefer_skill_filter_threshold` setting parallel to
`briefer_wrapper_filter_threshold` (default 10). When the
post-pre-filter catalog exceeds the threshold, the briefer does
the final selection; below, all eligible skills are passed
through.

**Tasks**:
- [x] Implement `skill_runtime.filter_by_activation_hints()`
      with the semantics above and full unit coverage.
- [x] Wire pre-filter into `build_planner_messages()` before
      briefer invocation.
- [x] Add `briefer_skill_filter_threshold` setting to
      `kiso/config.py` (SETTINGS_DEFAULTS).
- [x] Unit test: applies_to match keeps the skill.
- [x] Unit test: excludes match removes the skill.
- [x] Unit test: empty hints → always eligible.
- [x] Unit test: multi-word selector matches ordered tokens.
- [x] Unit test: `is_replan=True` bypasses the filter.
- [x] Unit test: behaves identically to recipe
      `applies_to`/`excludes` on a migrated skill fixture.
- [x] Unit test: 100-skill catalog + matching message ends up
      under 2KB of briefer input.
- [x] Commit & push.

**Done when**:
- `activation_hints` pre-filter runs before briefer.
- Catalog size reduces deterministically on representative
  messages.
- Unit tests cover matching, non-matching, and edge cases
  (empty hints, multi-word selectors, replan bypass).
- Installed-skill catalog size of 100 in fixture stays under
  2KB of briefer input after pre-filter + description-only
  metadata.

---

### M1539 — Per-user MCP method role filtering ✅

**Why.** Wrappers had per-user role filtering
(`kiso/wrappers.py:225-250`,
`build_planner_wrapper_list(user_role, user_wrappers, ...)`).
That is the one wrapper-side feature worth preserving for the
MCP catalog so admins can expose only a subset of MCP methods to
a given user.

**Approach.**

- Add `[users.<name>.mcp]` config section to `kiso/config.py`
  (parsing in the User dataclass alongside
  `[users.<name>.wrappers]`).
- Semantics mirror `[users.<name>.wrappers]`:
  - `"*"` means all methods available.
  - A list means allowlist by qualified `server:method` name.
  - Absent defaults to all for admin role, none for user role.
- Apply the filter to `format_mcp_catalog()` output before the
  briefer sees it (briefer-input assembly time).
- Non-existent methods in the allowlist are dropped silently
  with a warning log.

**Tasks**:
- [x] Add `[users.<name>.mcp]` parsing in
      `kiso/config.py:370-405`.
- [x] Add MCP catalog role filter at briefer input assembly.
- [x] Unit test: admin with no config sees all methods.
- [x] Unit test: user with no config sees no methods.
- [x] Unit test: restricted user with allowlist sees only the
      listed methods.
- [x] Unit test: allowlist entry for a non-existent method is
      dropped silently with a warning log.
- [x] Commit & push.

**Done when**:
- `[users.<name>.mcp]` is parsed and validated.
- Per-user role filtering on the MCP catalog works (admin sees
  all, restricted user sees only allowed methods).
- Dropped allowlist entries log a warning.

---

## Phase 3 — Default preset and standards docs (M1503)

### M1503 — `default.mcp.json`, `kiso init --preset`, standards docs, trust CI, single-key CI ✅

**Status (2026-04-19)**: shipped. Nine servers in
[`kiso/presets/default.mcp.json`](../kiso/presets/default.mcp.json),
`kiso init --preset <name>` + `--force` flag, four new docs pages
(`docs/standards.md`, `docs/default-preset.md`,
`docs/recommended-mcps.md`, `docs/recommended-skills.md`), and two
CI-enforced rules encoded as tests in
`tests/test_presets_default.py`. Trust + single-key rules reject
bad preset PRs before they reach main.

35 preset/init tests green; CLI tests (203) unaffected.
End-to-end smoke: `KISO_HOME=/tmp/x kiso init` produces a valid
config.toml with nine `[mcp.<name>]` sections, each parsing
cleanly through `kiso.mcp.config.parse_mcp_section`.

**Why.** With no registry and no wrapper install flow, new users
need a working MCP configuration and a good CLI UX out of the
box. "Edit `mcp.json` manually" is acceptable as an escape hatch,
not as the primary experience. Users and contributors also need
clear documentation of which standards Kiso follows and what is
Kiso-specific policy. And the single-key and trust rules must be
enforced mechanically, not only by convention.

The preset is bootstrap-only. After `init`, kiso never reads
the preset file again — it reads the user's `mcp.json`, which
the user is free to modify. There is no "preset upgrade" path;
when we ship a new preset version it affects only fresh
installs.

**Approach.**
- Create `kiso/presets/default.mcp.json` with the nine servers
  listed in the "Default preset" section below. All nine entries
  ship as regular, enabled entries — no `enabled: false` stubs,
  no placeholder comments. If a Track B package (M1507-M1511) is
  not yet released at the moment M1503 is ready to merge, M1503
  waits for the relevant Track B milestone. The preset is not
  shipped half-functional.
- `kiso init --preset <name>` command. Default behavior: `kiso
  init` with no flag still applies `default` preset.
- `kiso init --preset none` for users who want an empty
  `mcp.json`.
- Document `OPENROUTER_API_KEY` as the sole mandatory key in the
  post-init message. Mention `GITHUB_TOKEN` as optional.
- Keep `kiso mcp add/install/test/env/logs` as supported and
  documented first-class commands.
- Add `docs/standards.md` explaining:
  - Kiso adopts the **Agent Skills standard**
    (`agentskills.io`) for skill packaging
  - Kiso adopts the **MCP standard**
    (`modelcontextprotocol.io`) as the protocol for capabilities
  - Links to both standards
  - What is standard vs what is "Kiso Skill Profile" / "Kiso MCP
    Runtime Policy"
  - How to contribute a compatible skill or MCP server
- Add `docs/default-preset.md` describing each server, its
  upstream, and its trust tier.
- Add `docs/recommended-mcps.md` listing community MCP options
  that are NOT in the default, with disclaimers.
- Add `docs/recommended-skills.md` listing community skill
  sources that are NOT in the default, with disclaimers. Created
  here so it can be referenced by M1505 planner prompt and
  M1515 trust docs.
- Add two CI checks, each a test:
  1. **Trust rule**: fails if `default.mcp.json` contains a
     server whose `command` is not on the allowlist of approved
     upstreams (`npx` with `@modelcontextprotocol/*`,
     `@playwright/*`, `@github/*` pkgs; `uvx` with `kiso-*-mcp`
     pkgs).
  2. **Single-key rule**: fails if any preset entry has an
     `env` block referencing an env var other than
     `OPENROUTER_API_KEY` or `GITHUB_TOKEN`, or if a preset
     entry's `args` contains a reference to
     `${env:PERPLEXITY_API_KEY}` / `${env:BRAVE_API_KEY}` /
     `${env:GEMINI_API_KEY}`. The single-key invariant is
     encoded by string match for simplicity.

The trust allowlist distinguishes four kinds of command:
`npx` with specific scoped pkgs (enterprise), `uvx --from
git+https://github.com/kiso-run/*-mcp@*` (kiso-run Tier 2),
`uvx <bare pkg>` is NOT allowed in the default preset (avoids
PyPI-name collisions with community single-dev projects).

**Tasks**:
- [ ] `kiso/presets/default.mcp.json` written (nine servers).
- [ ] `init --preset` flag implementation in `cli/__init__.py`.
- [ ] Post-init message listing `OPENROUTER_API_KEY` as sole
      mandatory key, `GITHUB_TOKEN` as optional.
- [ ] Confirm `kiso mcp` commands operate on the canonical config
      path and remain documented.
- [ ] `docs/standards.md` written.
- [ ] `docs/default-preset.md` written.
- [ ] `docs/recommended-mcps.md` written.
- [ ] `docs/recommended-skills.md` created (initial empty
      template; populated in M1515).
- [ ] CI check / test for trust rule allowlist.
- [ ] CI check / test for single-key rule.
- [ ] Functional test: `kiso init --preset default` produces the
      expected `mcp.json` and the briefer can see all nine
      servers as MCP methods (assumes M1370 landed).
- [ ] Commit & push.

**Done when**:
- A fresh `kiso init` produces the default preset `mcp.json`.
- A user can complete the common flows without manually editing
  `mcp.json`.
- The trust-rule and single-key CI checks pass for the shipped
  default and each fails on a deliberately-bad PR.
- All four docs pages exist and are linked from a docs index.
- `docs/standards.md` clearly explains Kiso's relationship to
  Agent Skills and MCP, with links to both standards.

## Phase 4 — Wrapper and search retirement (M1504)

### M1504 — Retire wrappers + search type in one shot; no shims, no grace path ✅

**Status (2026-04-19)**: CLI surface retired (commit `2ac241f`).
`kiso wrapper …` and `kiso recipe …` return argparse `invalid
choice`; the cli/ source, preset wrapper auto-install, and five
test files are deleted or updated. 4520 unit tests green, no new
failures.

**Remaining for part 2** — the original "single commit" framing
turned out to underestimate how deeply the wrapper/recipe/registry
logic is threaded into `kiso/brain/common.py` (install-mode
classification, `FAILURE_CLASS_SEMANTIC_WRAPPER`, briefer
context-pool wiring), `kiso/brain/planner.py`
(`validate_plan` type branches, `build_planner_wrapper_list`,
replan routing) and the test surface (155 `"type": "wrapper"` /
`"type": "search"` mocks across 13 files, most in `test_brain.py`
and `test_worker.py`). Rather than push a 3k+ line single-commit
PR and risk a half-done red suite, part 2 is split into four
sequentially-green sub-commits. Each lands independently on
`main` with the full test suite green before the next starts.

**Part 2a — retire the `search` task type** ✅ (smallest / most
isolated):

- [x] Delete `kiso/worker/search.py`, `kiso/roles/searcher.md`,
      `tests/test_search.py`, `tests/test_searcher.py`.
- [x] Drop `TASK_TYPE_SEARCH`, `_handle_search_task`, the
      `_TASK_HANDLERS[search]` entry, and all `TASK_TYPE_SEARCH`
      branches in `validate_plan` / `_GROUPABLE_TYPES` /
      `_DATA_TYPES`; drop the search-specific
      `_is_plugin_discovery_search` helper and
      `_PLUGIN_DISCOVERY_RE`.
- [x] Delete `build_searcher_messages`, `run_searcher`,
      `SearcherError` from `kiso/brain/text_roles.py`; remove the
      `searcher` entry from `kiso/brain/roles_registry.py`.
- [x] Remove `searcher` from `_MODEL_METADATA`, `MODEL_DEFAULTS`,
      `CONFIG_TEMPLATE`; config validator rejects
      `models.searcher = "..."` with a clear upgrade message
      pointing to `kiso-run/search-mcp`.
- [x] Migrate `"type": "search"` mocks across `test_brain.py`,
      `test_worker.py`, `test_store.py`, `tests/conftest.py`,
      `tests/integration/conftest.py`, `tests/_cli_user_helpers.py`,
      `tests/test_config.py`, `tests/test_import_surfaces.py`,
      `tests/test_prompts.py` (drop search-specific assertions;
      re-type to `exec` where the test probes a non-search
      behavior).
- [x] Add `tests/test_search_retired.py` with module/file/
      config/role-registry surface invariants so the retirement
      stays pinned.
- [x] Full unit suite green (4434 passed, 10 skipped, 6
      deselected as pre-existing aider-validator failures already
      red on `main`).
- [x] Commit & push.

**Part 2b — retire `recipe_loader` + recipes** ✅:

- [x] Delete `kiso/recipe_loader.py`, `tests/test_recipes.py`,
      `tests/test_recipe_loader.py`.
- [x] Vendor the minimal recipe parser needed by
      `migrate_recipes.py` inline (the migration tool is a
      one-shot transitional piece).
- [x] Remove `from kiso.recipe_loader import …` from
      `kiso/brain/common.py`, `kiso/brain/planner.py`,
      `kiso/worker/loop.py`, `cli/plugin.py`,
      `tests/functional/test_advanced_flows.py`,
      `tests/functional/conftest.py`, `tests/test_brain.py`,
      `tests/test_rename_completeness.py`,
      `tests/test_plugin_taxonomy.py`.
- [x] Drop `recipes` / `_raw_recipes` context-pool keys, the
      `Available Recipes` planner section,
      `build_recipe_runtime_contracts_text` references (worker
      `recipe_contracts_text` parameter removed from
      `build_exec_translator_messages` and `run_worker`), the
      `recipe_contracts_text`/`recipe_runtime_contracts` fields
      on `_PlanCtx` and the `_merge_recipe_expected_outputs`
      helper + `recipe_hints` contract field.
- [x] Drop the `exclude_recipes` legacy briefer field from
      `tests/test_briefer.py::_briefing`; delete the
      `TestRecipesInBriefer` and `TestF38RecipeDrivenPlanning`
      test classes (functional).
- [x] Add `tests/test_recipes_retired.py` with module /
      briefer-pool / migrate-recipes invariants so the
      retirement stays pinned.
- [x] Full suite green (4403 passed, 10 skipped, 6 pre-existing
      aider-validator failures deselected).
- [x] Commit & push.

**Part 2c — retire the `wrapper` task type + subsystem** (the
largest piece). After a first execution attempt showed the
wrapper logic is more deeply interwoven than 2a/2b (install-mode
classifier in `common.py`, replan routing in `planner.py`,
`_PlanCtx.installed_wrappers` lookup path in `loop.py`, ~100
test mocks), 2c is itself split into three sequentially-green
slices. Each lands on `main` with the full unit suite green
before the next starts.

**Part 2c-i — delete wrapper-dedicated test files** (checkpoint
commit, keeps suite green without code cuts):

- Delete wrapper-dedicated tests (the ones that exercise
  kiso.wrappers / kiso.wrapper_repair / kiso.registry /
  kiso.worker.wrapper directly and have no meaningful
  non-wrapper replacement): `tests/test_wrappers.py`,
  `tests/test_wrapper_repair.py`,
  `tests/test_wrapper_lifecycle.py`,
  `tests/test_wrapper_replan.py`, `tests/test_registry.py`,
  `tests/docker/test_wrapper_venv.py`.
- Production code is untouched. The ~90 remaining
  `"type": "wrapper"` mocks that test non-wrapper mechanisms
  (plan-shape validation, grouping, install-approval flow, etc.)
  migrate together with the subsystem cuts in **2c-ii** — they
  are coupled to the code removals and moving them separately
  would just be a drive-by rename that risks breaking
  test_brain.py/test_worker.py for two commits in a row.
- Full unit suite green (4218 passed, 10 skipped, 5
  pre-existing aider-validator failures deselected; test count
  drops from 4403 because the 185 wrapper-dedicated tests are
  gone).

**Part 2c-ii — source-code retirement + test-mock migration** ✅
(actual cuts + the coupled mock rewrites):

- [x] TDD red: add `tests/test_wrappers_retired.py` pinning
  module absence, task-type removal, validator rejection,
  `_TASK_HANDLERS` shape, and `kiso.brain` re-export removal.
- [x] Delete `kiso/wrappers.py`, `kiso/wrapper_repair.py`,
  `kiso/registry.py`, `kiso/worker/wrapper.py`.
- [x] Drop `TASK_TYPE_WRAPPER` constant + `TASK_TYPES` entry +
  `_TASK_HANDLERS[wrapper]` + `_handle_wrapper_task` + the
  `TASK_TYPE_WRAPPER` branches in `validate_plan` /
  `_GROUPABLE_TYPES` / `_DATA_TYPES` / plan-ordering helpers.
- [x] Cascade removals in `kiso/brain/common.py`,
  `kiso/brain/planner.py`, `kiso/worker/loop.py`, `kiso/main.py`,
  `kiso/brain/__init__.py`, `kiso/sysenv.py` (dropped
  `_load_registry_hints` + `registry_hints` / `registry_url`
  sysenv keys — 2d deferral was moot once `kiso.registry` was
  deleted).
- [x] CLI updates: `cli/plugin.py` (wrapper discovery removed,
  connector-only view), `cli/plugin_ops.py` (registry helpers
  inlined after `kiso.registry` deletion), `cli/connector.py`
  (inline `shutil.which` replaces `check_deps`), `cli/preset.py`
  (registry `search_entries` pulled from `cli.plugin_ops`),
  `cli/plugin_test_runner.py` (dropped network fallback).
- [x] Test-mock migration across `test_brain.py`, `test_worker.py`,
  `test_cli_plugin.py`, `test_presets.py`, `test_install_confirm.py`,
  `test_render.py`, `test_self_inspection.py`,
  `tests/functional/test_helpers.py`,
  `tests/integration/test_secret_boundaries.py`,
  `tests/functional/conftest.py`, `tests/test_planner_npm_routing.py`,
  `tests/test_planner_mcp_methods_injection.py`,
  `tests/test_plugin_taxonomy.py`, `tests/test_main.py`,
  `tests/test_rename_completeness.py`, `tests/test_mcp_worker_dispatch.py`,
  `tests/test_sysenv.py`, `tests/test_safety_rules.py`,
  `tests/test_briefer.py`, `tests/conftest.py`.
- [x] `build_planner_messages` return signature simplified from
  `(messages, installed_names, installed_info)` → `messages`
  (wrapper-name lists no longer exist). `run_planner` +
  `validate_plan` callers updated.
- [x] `_INSTALL_CMD_RE` / `_INSTALL_NAME_RE` retargeted from
  `kiso wrapper|connector install` to generic install verbs
  (`kiso connector install`, `apt-get`, `uv pip install`, etc.);
  install-ordering validator messages switched from "installs a
  wrapper" → "installs a package".
- [x] `kiso/skill_runtime.py` helpers (`metadata_for_briefer`,
  `_audience_allows`, `instructions_for_{planner,worker,reviewer}`,
  `filter_by_activation_hints`) tolerate dict-like stand-ins so
  test mocks that patch `discover_skills` with plain dicts keep
  working without forcing every call site to construct a Skill.
- [x] `tests/test_wrappers_retired.py` flips green (12/12).
- [x] Full unit suite green (4084 passed, 16 skipped).

**Part 2c-iii — install-route + dead-code sweep** ✅:

- [x] TDD red: extend `tests/test_wrappers_retired.py` with
  12 invariant tests pinning removal of the dead symbols below
  and absence of retired wrapper CLI refs in `worker.md` /
  `classifier.md`.
- [x] Audit `_INSTALL_MODE_*` constants / regex / failure
  classes in `kiso/brain/common.py` after 2c-ii. Removed:
  `_INSTALL_MODE_KISO_WRAPPER`, `_INSTALL_MODE_UNKNOWN_KISO_WRAPPER`,
  `_KISO_WRAPPER_SIGNAL_RE`, `_is_explicit_named_wrapper_request`,
  `FAILURE_CLASS_SEMANTIC_WRAPPER` (+ its branch in
  `classify_failure_class` and the reference in
  `_classify_validation_errors`), `_WRAPPER_UNAVAILABLE_MARKER`,
  `_WRAPPER_NOT_INSTALLED_MARKER` (+ the dead
  `saw_uninstalled_wrapper` tracking and
  `_saw_uninstalled_wrapper` result propagation in
  `_retry_llm_with_validation`). `python_lib` / `system_pkg` /
  `node_cli` modes retained — still valid for `pip` / `apt` /
  `npm` routing.
- [x] Stripped `_parse_registry_hint_names` (no callers after
  2c-ii).
- [x] Updated `__all__` to drop every removed symbol.
- [x] Removed stale wrapper mentions from `kiso/roles/worker.md`
  (retired `kiso wrapper install` CLI + wrapper-venv PATH rule)
  and `kiso/roles/classifier.md` (wrapper/plugin ecosystem
  hints → skill/MCP/connector). `planner.md` deferred to M1505,
  which owns the full planner-prompt rewrite.
- [x] Removed `TestRetryLoopUninstalledToolFlag` from
  `tests/test_install_confirm.py` (tested dead
  `_saw_uninstalled_wrapper` mechanism) and the
  `FAILURE_CLASS_SEMANTIC_WRAPPER` / `M284-wrapper-path`
  cases from `tests/test_brain.py`. Updated
  `tests/test_worker.py::test_custom_replan_reason` to use
  an MCP-args label (wrapper label retired in 2c-ii).
- [x] Updated `tests/test_brain.py` classifier-prompt
  assertions to reflect skill/MCP/connector taxonomy.
- [x] Full unit suite green (4094 passed, 16 skipped).

All three slices commit with message prefix
`M1504 (part 2c-i|ii|iii)`.

**Part 2d — final cleanup + docs** ✅:

- [x] `kiso/sysenv.py`: `registry_url` drop was already folded into
  part 2c-ii (the `kiso.registry` module deletion moved the sysenv
  surface over as a cascade). Verified via inspection — no
  `registry_url` references remain.
- [x] `kiso/main.py`: `_populate_kiso_dir` already skipped
  precreating `~/.kiso/recipes/` and `~/.kiso/wrappers/` after
  part 2c-ii; the docstring that still advertised them as
  "standard subdirs" was updated to match the actual behaviour.
- [x] ~~Write `docs/v0.10-migration.md`.~~ **Reverted** — kiso is
  pre-release, no users on v0.9 to migrate away from wrappers
  or recipes. Doc freshness is a `docs/`-sweep concern; the
  retired-system cleanup moved to **M1523a** (promoted from
  Phase 9 so it runs right after M1504).
- [x] `tests/test_recipes_retired.py` and `tests/test_wrappers_retired.py`
  grew `TestPopulateKisoDirNo{Recipes,Wrappers}` classes pinning
  the precreation + docstring invariants (2 tests each).
- [x] Project-memory taxonomy section (user-scope `MEMORY.md`)
  refreshed to the v0.10 taxonomy: skills / MCP servers /
  connectors; recipes + `search` task type removed. Repo has no
  `MEMORY.md`.
- [x] `registry.json` emptied: `wrappers` array cleared; `recipes`
  array left empty; `connectors` + `presets` retained (still read
  by `install.sh` preset menu and `cli/preset.py` until M1533
  rewrites the installer and M1522 deletes the file entirely).
  The file is at the repo root, not out-of-repo — the original
  devplan note "separate out-of-repo PR" was incorrect.
- [x] Full unit suite green after revert + registry emptying.

Each sub-part commits with message prefix `M1504 (part 2a|b|c|d)`
so the split is readable in `git log`. When part 2d lands, flip
M1504's heading to ✅ and the bottom-of-file checklist entry to
`[x] ✅`.


**Why.** After M1502 the briefer no longer reasons about
wrappers. After M1503 new users get capabilities via MCP. The
`search` task type is now redundant with the `search` MCP server
in the default preset. But a hard deletion before migration
affordances exist creates avoidable churn.

**Approach.** One-shot retirement. Phase 7 (M1512-M1519)
delivers the parity replacements within v0.10, so there is no
reason to keep wrapper files alive for a transition period. The
PR that retires the runtime paths also deletes the wrapper
subsystem code, the CLI, the directory convention, and the
tests. No migration shims. No `kiso wrapper` command that prints
equivalents — users land on "unknown command" from argparse,
check the v0.10 release notes, and move on.

Task type changes:
- Remove `wrapper` from `validate_plan()` allowed types and from
  `_TASK_HANDLERS` in `kiso/worker/loop.py:1539-1546`.
- Remove `search` from `validate_plan()` allowed types and from
  `_TASK_HANDLERS`. Delete `kiso/roles/searcher.md` outright.
  The prompt is not migrated anywhere — `kiso-run/search-mcp`
  (M1508) calls Perplexity Sonar directly via OpenRouter and
  does not need a Kiso-owned query-refinement prompt carried
  over. Sonar handles freeform queries well; the server can add
  its own refinement later if measurements show it's needed.
- Remove `searcher` from `_MODEL_METADATA` and `MODEL_DEFAULTS`
  in `kiso/config.py:111-123`, from the `CONFIG_TEMPLATE` at
  `kiso/config.py:147-242`, and from anywhere the model name is
  referenced (grep `MODEL_DEFAULTS["searcher"]` and
  `models["searcher"]`). The config parser rejects a `searcher =
  "..."` entry with a clear error: "searcher model removed in
  v0.10; remove this line". No silent-ignore.
- Delete `kiso/worker/search.py` (search handler).
- Delete `kiso/worker/wrapper.py`.
- Update `needs_install` flow: the planner now proposes MCP
  installation via `kiso mcp install --from-url` OR skill
  installation via `kiso skill install --from-url` (M1516)
  instead of wrapper installation. The `needs_install` field and
  install-approved routing suppression (M1234) remain
  unchanged; only the target shifts.

Explicit feature disposition (no silent drops):

| Wrapper feature | Where it goes |
|---|---|
| `args_schema` validation (`wrappers.py:68-80`, `368-400`) | MCP method `inputSchema` jsonschema validation (M1513) |
| `session_secrets` scoping (`wrappers.py:88-90`, `516-528`) | Per-session MCP client pool with scoped env injection (M1512) |
| `env` deploy secrets (`build_wrapper_env`, `wrappers.py:531-558`) | `~/.kiso/mcp/<name>.env` file integration (M1512) |
| `consumes` file-type routing (`wrappers.py:157-164`, `289-311`) | `x-kiso-consumes` MCP description extension (M1519) |
| `usage_guide` narrative (`wrappers.py:92-94`, `283-287`) | MCP tool `description` (already supported natively; kiso-run/*-mcp servers populate rich descriptions) |
| `deps.bin` health at discovery (`wrappers.py:201-222`) | `doctor` tool contract for kiso-run/* servers; circuit breaker for third-party (existing) |
| `validator.py` hooks (`wrappers.py:409-444`) | Not ported as a kiso/core feature. MCP semantic repair, when needed, lives inside each server (the server owns its input model and can repair LLM-hallucinated arg aliases itself). jsonschema pre-flight (M1513) covers structural errors; arg-alias repair inside kiso/core was wrapper-specific scaffolding that no longer has a home. |
| Role filtering (`build_planner_wrapper_list` user_role) | `[users.<name>.mcp]` config (M1539, hardened in M1517) |
| `needs_install` approval flow | Extended to MCP + skills (M1516) |
| `wrapper_recovery` planner module | `mcp_recovery` planner module (M1519) |

Files to delete after Phase 7 lands:
- `kiso/wrappers.py`
- `kiso/registry.py`
- `kiso/recipe_loader.py`
- `kiso/worker/wrapper.py`
- `kiso/worker/search.py`
- `kiso/roles/searcher.md`
- `cli/wrapper.py` (deleted outright; no shim)
- `cli/recipe.py`
- All tests dedicated to wrappers/recipes/search that have no
  skills/MCP equivalent (audit each; some may have
  transferable assertions).
- Wrapper-related entries in `kiso/main.py` init (do not
  precreate `~/.kiso/wrappers/` or `~/.kiso/recipes/`).
- Wrapper/recipe references in `kiso/reference/wrappers.md`,
  `kiso/reference/connectors.md`, and `docs/`.
- Empty `registry.json` on the `kiso-run/core` `main` branch
  (separate PR, since it lives outside `core/`).

Files to update:
- `kiso/sysenv.py:482` — remove `registry_url` from sysenv
  output.
- `kiso/sysenv.py:295` — remove `registry_url` from
  `collect_system_env()`.
- `kiso/roles/planner.md` — remove wrapper-related modules and
  references (handled in M1505).
- `kiso/brain/common.py:1049-1053` — module list trimmed in
  M1505.

`_plugin_install` in `cli/plugin_ops.py` is **retained** because
`kiso connector install` still uses it. The wrapper-specific
callbacks (`_wrapper_post_install`, etc.) are removed; the
generic install flow stays for connectors.

**Tasks**:
- [ ] Remove `wrapper` and `search` from `validate_plan()` task
      type allowlist.
- [ ] Remove `wrapper` and `search` handlers from
      `_TASK_HANDLERS` in `kiso/worker/loop.py:1539-1546`.
- [ ] Delete `kiso/worker/search.py`.
- [ ] Delete `kiso/worker/wrapper.py`.
- [ ] Remove wrappers from planner/briefer/runtime routing.
- [ ] Update `needs_install` codepath: planner proposes
      `kiso mcp install --from-url` or `kiso skill install
      --from-url` instead of wrapper install (wiring completes
      in M1516).
- [ ] Delete `cli/wrapper.py` outright. Users who type `kiso
      wrapper` get argparse's default "invalid choice" error.
      Release notes explain the change.
- [ ] Delete `cli/recipe.py` outright.
- [ ] Delete `kiso/recipe_loader.py`.
- [ ] Delete `kiso/wrappers.py` and `kiso/registry.py`.
- [ ] Delete all tests dedicated to wrappers, recipes, or
      search: `tests/test_wrappers*.py`, `tests/test_recipes*.py`,
      `tests/test_registry*.py`,
      `tests/functional/test_wrapper_*.py`,
      `tests/live/test_wrapper_*.py`. Do not port assertions —
      the new paths have their own tests (M1500-M1502, M1538-M1541).
- [ ] Simplify `_plugin_install` in `cli/plugin_ops.py:197-431`:
      after wrapper retirement it serves only connectors. M1525
      retires the connector plugin model entirely, so
      `_plugin_install` is deleted there. In this milestone it
      just loses the `plugin_type` parametrization (hardcoded
      "connector") and the wrapper-specific branches.
- [ ] Update `kiso/main.py` init to not create wrapper/recipe
      dirs.
- [ ] Update `kiso/sysenv.py` to drop `registry_url`.
- [ ] Write `docs/v0.10-migration.md` with the feature
      disposition table above plus wrapper-to-MCP-or-skill name
      mapping for the kiso-run/* wrappers that are being
      re-released as MCP servers.
- [ ] Update `MEMORY.md` taxonomy section.
- [ ] Run full test suite (`./utils/run_tests.sh a`) and confirm
      green.
- [ ] Commit & push.
- [ ] Separate PR on `kiso-run/core` main branch to empty
      `registry.json`.

**Done when**:
- No runtime path routes work through wrappers or the built-in
  search handler.
- `validate_plan()` accepts only `exec`, `mcp`, `msg`, `replan`.
- `kiso wrapper` is not a valid CLI subcommand anymore (argparse
  "invalid choice" error).
- `needs_install` flow proposes MCP or skill installation, not
  wrapper installation.
- Full suite passes.
- Every wrapper feature listed in the disposition table above
  is either (a) ported to MCP/skills via a referenced later
  milestone, or (b) explicitly documented as deferred in
  `docs/v0.10-migration.md`. No silent drops.

## Phase 5 — Planner prompt cleanup (M1505)

### M1505 — `planner.md` routing module rewritten as `skills_and_mcp` + skill install rule ✅

**Why.** With wrappers, recipes, and the search type gone, the
planner prompt has orphan modules (`wrappers_rules`,
`kiso_native`, `wrapper_recovery` per the hardcoded list at
`kiso/brain/common.py:1049-1053`) and stray references to
"wrappers", "recipes", and "search" throughout the system
prompt. The categories the planner reasons about have collapsed
to two; the prompt should reflect that. The `replan` module also
needs updating to reference skills instead of recipes/wrappers,
so that replan cycles have access to the full skill catalog. The
existing `mcp` section at `planner.md:72-75` is expanded to a
full module covering both skill and MCP install-from-URL rules.

**Approach.** Rewrite the relevant `<!-- MODULE: ... -->` blocks
in `kiso/roles/planner.md`. Old modules `wrappers_rules`,
`kiso_native`, and `wrapper_recovery` are replaced by
`skills_and_mcp` (capability routing) plus a new
`mcp_recovery` module (broken-server flow, created in M1519).
The hardcoded module list in `kiso/brain/common.py:1049-1053`
is updated accordingly.

The new `skills_and_mcp` module covers:
- when to use a skill vs exec vs MCP
- the hard rule that Kiso maintains no registry of MCPs OR
  skills — planner asks the user for a concrete URL when a
  capability is missing, via `needs_install` + msg task
- allowed URL forms for both `kiso mcp install --from-url` and
  `kiso skill install --from-url`
- the "install → approve → replan" lifecycle

The `replan` module gains skill-catalog context: when
replanning, the planner sees the full skill catalog (not just
skills from the previous plan), enabling it to select different
skills if the strategy changes.

The `web` module reference to "browser wrapper" becomes
"browser MCP (playwright)" and the "use built-in search"
becomes "use search MCP".

**Tasks**:
- [x] New `<!-- MODULE: skills_and_mcp -->` block written,
      covering both primitives, the no-registry rule, and both
      install-from-URL flows.
- [x] Old `wrappers_rules`, `kiso_native`, and the inlined MCP
      block folded into `skills_and_mcp`. `wrapper_recovery`
      dropped entirely (no broken-wrapper concept left after
      M1504 wrapper retirement; `mcp_recovery` comes in M1519).
- [x] `<!-- MODULE: replan -->` references the full installed
      skill catalog during replan cycles.
- [x] `<!-- MODULE: web -->` replaces search-task / browser-wrapper
      language with "search MCP" and "browser MCP (playwright)".
- [x] `<!-- MODULE: plugin_install -->` summarizes the shared
      MCP + skill install flow; actual rules live in
      `skills_and_mcp`.
- [x] Core module hard rule covers both primitives via the
      `skills_and_mcp` module (no-registry, install-from-URL).
- [x] `kiso/brain/common.py` `BRIEFER_MODULES` updated: drops
      `kiso_native`/`wrappers_rules`/`wrapper_recovery`, adds
      `skills_and_mcp`.
- [x] `_BRIEFER_MODULE_DESCRIPTIONS` updated (all within
      60-char briefer limit).
- [x] Planner.md sweep for "wrapper"/"recipe"/"search task"
      content completed; briefer/messenger/worker/reviewer had
      no residual references.
- [x] `kiso/brain/planner.py` safety-net module was
      `kiso_native`; now `skills_and_mcp`. `_SYSENV_MODULES`
      gate condition updated accordingly.
- [ ] *(deferred — live-LLM test)* Fixture-routing test for
      the new prompt on representative goals. Needs `--llm-live`
      infra; unit coverage pins module structure, content, and
      safety-net injection for now.
- [x] Commit & push.

**Done when**:
- Planner prompt no longer mentions wrappers, recipes, or the
  search task type.
- The new `skills_and_mcp` module covers all routing decisions
  and both install flows.
- The `replan` module references skills and provides the full
  catalog during replan cycles.
- Existing planner tests pass with the new module set.
- A fresh planner call on a goal "list files in workspace"
  routes via the `filesystem` MCP, not via exec.
- Fixture goal "install skill X from github.com/.../X" produces
  a correctly-formed `needs_install` plan with a msg task
  containing `kiso skill install --from-url <url>`.

## Phase 6 — Skill CLI basics (M1506)

### M1506 — `kiso skill list/info/add/remove` CLI subcommand ✅

**Why.** MCP servers have a first-class CLI (`kiso mcp
add/install/test/env/logs`). Skills need the same treatment —
otherwise they are second-class citizens that users can only
manage by hand-editing a directory.

This milestone ships the *local-only* operations. The URL-based
install path lives in M1514 so it can build on M1515 trust
tiers.

**Approach.** Add a `kiso skill` CLI group with subcommands:

- `kiso skill list` — list installed skills with name,
  description, source
- `kiso skill info <name>` — show full metadata and role sections
- `kiso skill add <path>` — copy a local skill directory (or
  single SKILL.md file) into `~/.kiso/skills/`, validate naming
  convention and frontmatter
- `kiso skill remove <name>` — remove from `~/.kiso/skills/`

`kiso skill install --from-url` is intentionally NOT in this
milestone (lands in M1514). `kiso skill test` is NOT here
(lands in M1515). The split keeps M1506 small and lets URL/trust
design happen after the loader has real users.

**Tasks**:
- [x] Add `cli/skill.py` with `list`, `info`, `add`, `remove`
      subcommands following the pattern in `cli/mcp.py`.
- [x] Register `kiso skill` subcommand group in
      `cli/__init__.py` `build_parser()`.
- [x] Dispatch in `cli/__init__.py` `main()`.
- [x] Validate naming convention + frontmatter on `add`
      (delegated to `parse_skill_file` so the rule lives in
      one place).
- [x] `kiso skill info` renders role sections with headers so
      users can inspect what planner/worker/reviewer will see.
- [x] Unit tests for all four subcommands (22 tests in
      `tests/test_cli_skill.py`, TDD-first).
- [x] Update CLI help text and docs (`docs/cli.md`,
      `docs/skills.md`).
- [x] Commit & push.

**Done when**:
- `kiso skill list` shows installed skills (name, one-line
  description, source directory).
- `kiso skill add <path>` copies a directory or single-file
  skill and validates naming + frontmatter.
- `kiso skill remove <name>` cleans up.
- `kiso skill info <name>` displays metadata and role sections.

## Phase 7 — Lifecycle completeness (M1512-M1519)

This phase is the one that moves kiso from "migrated" to
"production-grade generalist agent". Each milestone closes a
concrete gap between the current state and the ten
Claude-Code-parity flows listed at the top of the plan.

### M1512 — Per-session MCP client pool + session context + env file integration ✅

**Why.** Three distinct bugs / gaps collapse into one
infrastructure change:

1. `${env:VAR}` substitution is parse-time
   (`kiso/mcp/config.py:9-15`). A preset arg of `"${WORKSPACE}"`
   is baked at daemon boot, so every session shares the same
   workspace — wrong for a multi-session daemon. Claude Code
   runs one process per session, kiso does not.
2. `MCPManager` keys by `server_name` only
   (`kiso/mcp/manager.py:81`). No per-session isolation.
   `call_method(name, method, args)` has no session parameter
   (`kiso/mcp/manager.py:135-167`).
3. `~/.kiso/mcp/<name>.env` credential files are written by the
   CLI (`cli/mcp.py:446-520`) but **never read by the
   spawner** (`kiso/mcp/stdio.py:311-323`). A user setting a key
   via `kiso mcp env set` today gets no subprocess-side effect.
   This is a silent regression hazard.

**Approach.**

- Add `${session:workspace}` and `${session:id}` tokens to
  `kiso/mcp/config.py`. These are NOT expanded at parse time —
  the regex `_ENV_REF_RE` at line 34 is extended to match, but
  the expander preserves them verbatim in the `MCPServer`
  dataclass. A separate function
  `resolve_session_tokens(server, session)` applied at
  `call_method` time does the substitution in a copy of the
  server's args/env.
- Extend `MCPManager` pool to key on `(server_name, scope_key)`:
  - `scope_key = "_global"` when the server config has no
    `${session:*}` tokens AND no declared
    `scope = "session"`.
  - `scope_key = session_id` otherwise.
- `MCPManager.call_method(name, method, args, *, session:
  str | None = None)` — `session` passed by
  `kiso/worker/mcp.py:111-113` (already has `ctx.session`).
- Add session-scoped `list_methods`: same key, separate cache
  entry per scope. `list_methods_cached_only` accepts a scope
  hint.
- Add idle eviction loop: background coroutine in `MCPManager`
  iterates session-scoped clients; any client unused for
  `mcp_session_idle_timeout` seconds (default 1800) is
  gracefully shut down. Runs every 60s.
- Add LRU eviction at `mcp_max_session_clients_per_server`
  bound (default 32).
- **Env file integration**: in the client spawn path
  (`kiso/mcp/stdio.py` `_build_subprocess_env` around line 311),
  read `~/.kiso/mcp/<name>.env` if present, parse KEY=VAL
  lines (mode 0600 check), and merge into the subprocess env
  AFTER the config `env` block (file wins, so the user's `kiso
  mcp env set` takes effect).
- Session secret scoping (parity with wrapper
  `session_secrets`): if a session contributes secrets (rare but
  real; passed from user message via a structured input
  mechanism), they are merged into a session-scoped client's env
  at spawn, NEVER into a global-scope client. The scoping is
  enforced by the pool key: global clients cannot receive
  session secrets by construction.

Config settings added to `kiso/config.py`:
- `mcp_session_idle_timeout` (default 1800, clamp 60-7200)
- `mcp_max_session_clients_per_server` (default 32, clamp 1-256)

**Tasks**:
- [x] Extend parser in `kiso/mcp/config.py` to preserve
      `${session:workspace}` and `${session:id}` verbatim at
      parse time (kept as separate `_SESSION_REF_RE`; the env
      expander runs against `_ENV_REF_RE` only, so session
      tokens pass through untouched).
- [x] Add `resolve_session_tokens(server, session, workspace)`
      in `kiso/mcp/config.py` returning a copied `MCPServer`
      with tokens substituted; returns the original object when
      the server has no session tokens.
- [x] `MCPServer.is_session_scoped` property (presence of
      `${session:*}` in any string field). Explicit
      `scope = "session"` TOML key was not needed — field-scan
      is sufficient and has no false positives.
- [x] Rework `MCPManager` pool keys to
      `dict[tuple[str, str], MCPClient]` with `"_global"` as
      the scope key for non-session-scoped servers.
- [x] Add `session: str | None = None` keyword to
      `call_method`, `list_methods`, `list_methods_cached_only`,
      `cancel`. Default routes to global scope so existing
      call sites continue to work.
- [x] `kiso/worker/mcp.py` passes `session=ctx.session`.
- [x] `cli/mcp.py` test command stays global (no change needed;
      the default `session=None` is correct for CLI health
      checks).
- [x] Idle eviction — background coroutine via
      `start_eviction_loop()`; `_evict_idle_now()` is the
      testable hook.
- [x] LRU eviction at `max_session_clients_per_server`.
- [x] Wire `~/.kiso/mcp/<name>.env` into
      `kiso/mcp/stdio.py::_build_env` via a shared
      `kiso/mcp/envfile.py` parser (so `cli/mcp.py` and
      `stdio.py` read the same format). 0600 check refuses
      world-readable creds files; `KISO_*` keys are dropped
      with a warning.
- [x] Unit tests: 47 new tests across
      `tests/test_mcp_session_tokens.py` (14),
      `tests/test_mcp_session_pool.py` (10),
      `tests/test_mcp_session_eviction.py` (9 incl. state
      cleanup), `tests/test_mcp_env_file.py` (8),
      `tests/test_mcp_session_settings.py` (8).
- [ ] Integration test with a real filesystem MCP server on
      Docker — **deferred**: the existing tests already pin the
      behaviour at the spawner and pool level with a recording
      factory that asserts resolved args/env per session. A
      real-MCP integration belongs with M1531 (end-to-end v0.10
      pipeline test) where Docker + live servers are the norm.
- [x] Update `docs/mcp.md` with the pool model, eviction
      settings, session token syntax, env file precedence.
- [x] Commit & push.

**Done when**:
- `${session:workspace}` in a preset server's args resolves
  per-call to the calling session's workspace.
- Two sessions using the same session-scoped server get
  independent subprocesses, isolated state, and isolated
  credentials.
- `kiso mcp env set <name> KEY VAL` followed by a message that
  exercises `<name>` results in the subprocess receiving
  `KEY=VAL` in its environment (verified by a test using an
  echo-like fixture MCP server).
- Idle eviction shuts down unused per-session clients;
  daemon-wide client count stays bounded.
- All existing MCP tests pass with the new pool keys.

---

### M1513 — MCP method input schema validation + planner schema visibility ✅

**Why.** Today the planner sees only `server:method — description`
(`kiso/brain/common.py:1311-1316`) — not the tool's
`inputSchema`. The worker passes args straight through to the
server without jsonschema validation
(`kiso/worker/mcp.py:80-99`). Result: LLM hallucinations on arg
names or types fail at the server round-trip, burning a replan.
Wrappers had `args_schema` + `validator.py` pre-flight
(`kiso/wrappers.py:368-400, 447-475`) that caught these before
they reached the subprocess.

**Approach.**

- Extend `format_mcp_catalog()` in `kiso/brain/common.py:1283-1317`
  to include a compact schema summary after each method line:
  - required args with types (`query:string`, `max_results:int?`)
  - truncate at 200 chars total per method, with `...` if
    longer
  - falls back to description-only if `input_schema` is
    missing/empty
- Add `validate_mcp_args(method, args) -> list[str]` in
  `kiso/mcp/validate.py` using `jsonschema` (already a transitive
  dependency via `openai`/pydantic — verify). Validate against
  the cached `MCPMethod.input_schema`.
- Call it from `kiso/worker/mcp.py` between line 99 (args is a
  dict) and line 111 (`call_method`). On validation error:
  - generate a repair-context message
  - one retry loop: let the translator re-emit args with the
    schema error as feedback (parallel to the exec translator's
    retry loop at `kiso/worker/loop.py:1271-1296`)
  - if still failing, fail the task with a clear error and let
    the reviewer route to replan

**Tasks**:
- [x] Extend `format_mcp_catalog()` to render compact schema
      summaries: required first, optional marked `?`, type labels
      collapsed to `string`/`int`/`bool`/`list`/`dict`, 200-char
      per-line bound with `...` overflow.
- [x] Add `kiso/mcp/validate.py` with `validate_mcp_args(schema,
      args) -> list[str]` wrapping `jsonschema.Draft202012Validator`;
      structured error messages.
- [x] Integrate into `kiso/worker/mcp.py` pre-flight via
      `_preflight_validate`; short-circuits with a schema-anchored
      failure reason before `call_method`.
- [x] `kiso/brain/planner.py`'s `validate_plan` also delegates MCP
      schema checking to `validate_mcp_args` (was inline, same
      behaviour, better error shape).
- [x] Unit tests (13+7+5 = 25 new tests):
      - valid args pass, optional omitted
      - missing required → field name surfaced
      - wrong type → field + expected type surfaced
      - absent/empty schema → permissive (no-op)
      - malformed schema → error, no crash
      - catalog summary shape + line truncation + budget
      - worker pre-flight: valid passes, bad shorts-circuits, no
        cached schema is permissive
- [ ] Integration test for one-shot LLM-mediated retry —
      **deferred / out of scope**. There is no MCP-specific
      translator role today; a dedicated repair loop would be a
      new LLM role, materially bigger than M1513. With schema
      summaries in the catalog AND pre-flight validation, the
      planner sees the full schema up front, and the worker's
      schema-anchored failure reason routes to standard replan —
      which is the "repair attempt" in the kiso pipeline model.
      Revisit if replan rate on MCP tasks stays above target
      after this milestone.
- [x] Budget audit: 30-method catalog stays under 30 × 200 chars
      (test `TestBudget::test_thirty_methods_stay_under_budget`).
- [x] Commit & push.

**Done when**:
- Planner prompt includes compact schema summaries for MCP
  methods.
- Worker validates MCP args against the cached input schema
  before calling the server.
- Invalid args trigger a one-shot repair retry, not a full
  replan, for a measurable failure-rate reduction on synthetic
  bad plans.
- No regression on valid-args latency (pre-flight < 2ms per
  call).

---

### M1514 — `kiso skill install --from-url` with full parity to MCP install ✅

**Why.** M1506 lands the local path. The Claude-Code-parity
promise is that a user can paste a link and run a skill. This
milestone delivers that flow.

**Scope note — two surfaces, this is the first one.** Kiso
exposes install via two surfaces:

1. **Operator CLI** (this milestone): `kiso skill install
   --from-url <url>` run on the kiso host by someone with shell
   access. Produces the building block — the `kiso/skill_install.py`
   module + the CLI subcommand.
2. **Chat-mediated install** (M1516, *`needs_install` extended to
   skills*): the end user — who may not have shell access to the
   daemon — types a message like "install the X skill from
   <url>", the planner recognises the intent via `needs_install`,
   and the worker invokes the same `kiso skill install` code path
   under the install-approved guardrails.

M1514 keeps both users covered by giving M1516 a tested, stable
library to call. The trust-prompt surface that M1516 needs is
itself M1515's territory.

**Approach.** New `kiso/skill_install.py` mirroring
`kiso/mcp/install.py:109-166`. A single `resolve_from_url(url)`
function dispatches by URL shape and returns a
`ResolvedSkill` dataclass describing:

- skill name (sanitized)
- install source type (`github_repo`, `github_subpath`, `raw_md`,
  `zip`, `agentskills_io`, `local_path`)
- operations to perform (clone / download / unpack)
- destination path under `~/.kiso/skills/<name>/`
- provenance metadata (source URL, fetched ref if git)

Supported URL forms:

- `https://github.com/<owner>/<repo>` → `git clone --depth 1`
  into a temp dir; if a top-level `SKILL.md` exists, the whole
  repo becomes the skill; else look for a
  `skills/<subdir>/SKILL.md` and install that subdir.
  Multiple skills in one repo: require the user to narrow via
  `/tree/<ref>/path` form.
- `https://github.com/<owner>/<repo>/tree/<ref>/path/to/skill`
  → sparse-checkout of the subpath.
- `https://raw.githubusercontent.com/.../SKILL.md` or any URL
  ending in `SKILL.md` / `skill.md` → fetch single file;
  creates `<name>/SKILL.md` in a new directory.
- `*.zip` URLs → download, unpack, expect a single skill root
  either at top-level or one level deep.
- `https://agentskills.io/skills/<slug>` → translates to the
  backing github URL via a documented resolver (HEAD redirect
  or a simple JSON endpoint — implementation uses whichever is
  stable at the time).
- Bare local path → delegates to `kiso skill add`.

Post-install:
- Run frontmatter validation (same rules as `kiso skill add`).
- Write `~/.kiso/skills/<name>/.provenance.json` with source
  URL, fetched ref, install timestamp, resolved trust tier.
- Safety prompts (M1515 adds the trust layer; this milestone
  wires the hook point but the actual trust UI is in M1515).

CLI surface:
- `kiso skill install --from-url <url>` (required)
- `--name <override>` (optional, overrides sanitized name)
- `--dry-run` (print plan, don't execute — parity with
  `kiso mcp install`)
- `--yes` (skip confirmation prompts from M1515)

**Tasks**:
- [x] Add `kiso/skill_install.py` with `resolve_from_url()` and
      `ResolvedSkill` dataclass (source_type is a `Literal` of
      the five supported shapes).
- [x] Extend `cli/skill.py` with `install` subcommand (
      `--from-url`, `--name`, `--dry-run`, `--yes`, `--force`).
- [x] Provenance file writer — `.provenance.json` next to the
      installed skill with source_url, source_type, optional ref
      + subpath, and install timestamp.
- [x] HTTP / git / zip / agentskills-resolver operations wrapped
      behind injectable callables (defaults use httpx +
      subprocess git), so unit tests run fully offline.
- [x] Unit tests (31 new tests across two files):
      - github repo with top-level SKILL.md → whole repo copied
      - github tree URL with subpath → narrowed install
      - raw SKILL.md URL → `<name>/SKILL.md`
      - zip URL containing a single skill → unpacked + located
      - agentskills.io URL → redirect resolver re-invoked
      - invalid URL → clear error
      - already-installed skill without `--force` → refused;
        with `--force` → overwritten
      - CLI dry-run prints plan, does not fetch
      - top-level `cli` parser exposes `kiso skill install`
- [ ] Integration test (`tests/live/`) with a real skill from a
      stable public github URL — **deferred**: requires a
      reliable upstream we don't control and duplicates coverage
      the injected-fetcher unit tests already give. Revisit if
      the injection seam ever shifts or drops.
- [x] Update `docs/skills.md` with URL forms and examples.
- [x] Update `docs/cli.md` for the new subcommand.
- [x] Commit & push.

**Done when**:
- All URL forms above resolve correctly in unit tests.
- A user can `kiso skill install --from-url
  https://github.com/<org>/<skill>` and, on the next message,
  the briefer can select it.
- `--dry-run` prints the install plan without side effects.
- Provenance file is written with accurate source URL and ref.

---

### M1515 — Trust model, safety gates, `kiso skill test`, recommended-skills docs ✅

**Why.** `kiso mcp install` and `kiso skill install` both pull
code from the network. Silent trust-on-first-use is risky for
skills with `scripts/` or MCP servers that can run arbitrary
commands under `uvx`/`npx`. `pip install` and `npx` UX is the
reference: warn loudly, don't block, let the user opt in.
Additionally, skills need a local integrity check (`kiso skill
test`) and the ecosystem page promised in M1503 needs to be
populated.

**Approach.**

Trust store at `~/.kiso/trust.json`:

```json
{
  "mcp": {
    "prefixes": [
      "npm:@modelcontextprotocol/*",
      "npm:@playwright/*",
      "npm:@github/*",
      "github.com/kiso-run/*-mcp"
    ],
    "custom": []
  },
  "skill": {
    "prefixes": [
      "github.com/anthropic/skills/*",
      "github.com/kiso-run/skills/*"
    ],
    "custom": []
  }
}
```

Tier 1 prefixes are hardcoded in `kiso/mcp/trust.py` and
`kiso/skill_trust.py` and cannot be edited via CLI (only via
code change). `custom` is the user-editable list via:

- `kiso mcp trust add <prefix>` / `remove` / `list`
- `kiso skill trust add <prefix>` / `remove` / `list`

Install-time behavior:
- source matches hardcoded Tier 1 prefix OR user-custom prefix
  → silent install
- source does NOT match → warn with:
  - source URL
  - resolved package name
  - detected risk factors: `scripts/` present,
    `allowed-tools` non-trivial, unsigned release, etc.
  - prompt `Install anyway? [y/N]` (unless `--yes`)

`kiso skill test <name>`:
- validate frontmatter strictly (name, description required;
  naming convention)
- check all referenced paths in frontmatter and body exist
  (relative links to `scripts/` / `references/` / `assets/`)
- check role sections parse cleanly
- if `allowed-tools` lists external commands, check they exist
  on PATH (warning only)
- print a structured report; exit non-zero on hard failures

`docs/recommended-skills.md` populated with a starting set:
- 5-10 community skills from well-known repos (anthropics,
  claude-code-guide examples, etc.)
- each with: source URL, brief description, risk level, why
  it's useful
- **not auto-installed**; purely informational

Detected risk factors live in
`kiso/skill_trust.py::detect_risk_factors(skill_path)`:
- `scripts/` directory present
- frontmatter `allowed-tools` references `Bash(*)`, `Write(*)`,
  `Edit(*)`, or network-adjacent tools
- SKILL.md exceeds 50KB
- referenced assets exceed 5MB total

**Tasks**:
- [x] `kiso/mcp/trust.py` and `kiso/skill_trust.py` modules
      with `is_trusted(source)`, `detect_risk_factors(path)`,
      `source_key_for_url`, and Tier 1 prefix constants. Prefix
      match logic lives in a shared `kiso/trust_store.py`.
- [x] Trust store load/save with atomic rewrite via
      `tempfile.mkstemp` + `os.replace`; parent-dir auto-create;
      tolerant to malformed existing files.
- [x] `kiso mcp trust add/remove/list` subcommands.
- [x] `kiso skill trust add/remove/list` subcommands.
- [x] Install-time trust check in `kiso mcp install` and
      `kiso skill install` — untrusted sources print the
      resolved source key, risk factors (skill side), and
      prompt `Install anyway? [y/N]` (bypass with `--yes`).
- [x] `kiso skill test <name>` subcommand — frontmatter
      validation, broken relative-link detection, missing
      `allowed-tools` binaries; exit 1 on hard failures,
      warnings keep exit 0.
- [x] Unit tests — 52 new tests across three files (34 trust,
      10 trust CLI + install-gate, 8 skill-test).
- [x] Provenance records `trust_tier` (`tier1`, `custom`, or
      `untrusted-user-approved`) alongside source metadata.
- [x] Write `docs/recommended-skills.md` — populated with Tier 1
      examples + agentskills.io + community guidance +
      post-install audit instructions.
- [x] Update `docs/cli.md` for the new subcommands.
- [x] Commit & push.

**Done when**:
- `kiso mcp install --from-url` warns on untrusted sources.
- `kiso skill install --from-url` warns on untrusted sources
  and on detected risk factors.
- Trust store can be managed via CLI without hand-editing.
- `kiso skill test <name>` reports frontmatter and referenced
  file integrity.
- `docs/recommended-skills.md` lists 5+ community skills with
  risk annotations.

---

### M1516 — `needs_install` extended to skills ✅

**Why.** Today `needs_install` proposes wrappers; M1504 shifts
it to MCP. But when the planner realizes that a given domain
needs a *skill* (instructional content, not a capability), it
must be able to propose a skill install with the same
msg-only → approve → exec → replan lifecycle.

**Approach.**

- Extend `validate_plan` in `kiso/brain/planner.py:194-481` so
  that entries in the `needs_install` array can be either:
  - an MCP server name OR install URL
  - a skill name OR install URL
- The validator doesn't need to distinguish — it just checks
  the structural shape. The msg task produced by the planner
  tells the user which command to run.
- Update the `skills_and_mcp` planner module (M1505) to
  describe both cases with examples.
- Update `cli/plugin_ops.py` or add a parallel helper so that
  when the exec task runs `kiso skill install --from-url ...`,
  it proceeds under the same install-approved guardrails as
  `kiso mcp install`.
- The install-approved routing suppression at
  `kiso/worker/loop.py:1316` and `kiso/brain/planner.py:535-566`
  must cover both `kiso mcp install` AND `kiso skill install`
  command patterns.

**Tasks**:
- [x] Audit `_INSTALL_CMD_RE` in `kiso/brain/common.py` —
      single alternation `kiso\s+(?:connector|mcp|skill)\s+install`
      now covers all three kiso install command shapes; the
      same regex is consulted by `validate_plan` (planner) and
      the worker's unapproved-install suppression, so both the
      first-plan mixed-propose-and-install rejection AND the
      post-approval unblocking fire for mcp + skill installs.
- [x] Planner prompt (`kiso/roles/planner.md` `skills_and_mcp`
      module) already covers `kiso skill install --from-url`
      in the install lifecycle from M1505 — verified against
      the module body; no prompt change needed.
- [x] Unit tests — `tests/test_install_mcp_skill_cmd.py` covers
      the regex against every command shape the planner can
      emit, plus `validate_plan` with `kiso mcp install` /
      `kiso skill install` in mixed first-plan and replan
      shapes. 17 new tests green.
- [ ] Integration test with a real skill fixture — **deferred**.
      The regex + validate_plan tests pin the gate's behaviour
      deterministically; a Docker + real-LLM loop is
      M1531-class work. Revisit if end-to-end needs verified
      drift.
- [x] Update `docs/flow.md` to describe both install types.
- [x] Commit & push.

**Done when**:
- Planner can propose skill installation alongside MCP
  installation.
- Install approval flow works identically for both types.
- Replan after skill install sees the new skill in the
  catalog.

---

### M1517 — Per-user MCP method role filtering (migrating wrapper user roles) ✅

**Why.** Today `[users.<name>].wrappers` (`kiso/config.py:253`)
filters the wrapper catalog shown to a user. This is the only
user-role access control in v0.9 besides admin-vs-user binary.
M1539 introduces `[users.<name>.mcp]`; this milestone completes
the design (adds the parallel `skills` allowlist, removes the
legacy `wrappers` user field) and hardens it.

**Approach.**

Extend the `User` dataclass at `kiso/config.py:250-254`:

```python
@dataclass(frozen=True)
class User:
    role: str  # "admin" | "user"
    mcp: str | list[str] | None = None       # NEW: allowlist of server:method
    skills: str | list[str] | None = None    # NEW: allowlist of skill names
    aliases: dict[str, str] = field(default_factory=dict)
```

The `wrappers` field is removed, not kept for any compatibility
path. Config parser rejects `[users.<name>].wrappers = ...` with
a clear error pointing at the migration notes.

Filtering:
- `role=admin` or value `"*"` → see all
- `role=user` and value is a list → allowlist
- `role=user` and value absent → default deny for non-admin (for
  skills and MCP this is the sane default; the wrapper default
  was "require explicit list")

Apply filtering at:
- `format_mcp_catalog()` input (already wired in M1539)
- `skill_loader.discover_skills()` result at
  `build_planner_messages` before the briefer pool assembly

Config validation: reject unknown method/skill names in
allowlists with a clear error.

**Tasks**:
- [x] `User` dataclass: added `skills` field, removed `wrappers`.
- [x] Config parser: accepts `mcp`, `skills`; rejects `wrappers`
      with a migration hint.
- [x] Wire into MCP catalog formatting (already via M1539).
- [x] Wire into skill discovery pipeline — new
      `filter_skills_by_user` in `kiso.brain.common`, applied
      in `build_planner_messages` after the activation-hints
      pre-filter and before the briefer catalog assembly.
- [x] Threaded `user_mcp` / `user_skills` through planner +
      worker + api/sessions + main startup-recovery + cron job
      enqueue, replacing the legacy `user_wrappers` parameter.
- [x] Dead `revalidate_permissions` wrapper branch removed;
      permission re-validation is now user-existence only.
- [x] CLI surface: `kiso user add/edit` take `--mcp` and
      `--skills` flags (comma-separated or `*`), `--wrappers`
      removed. `kiso user list` prints both fields.
- [x] Unit tests for both allowlist paths — 11 new tests in
      `tests/test_user_skill_filter.py`.
- [ ] Validation: unknown names in allowlist → config error at
      load time — **deferred**. Unknown MCP methods / skill
      names are caught at catalog-build time (silently dropped
      with debug log) rather than at config-load; a hard error
      requires threading the MCP/skill registries into
      `_build_config` which would couple load-time config to
      discovery state. Reviewer-flagged as nice-to-have; not a
      correctness gap.
- [x] Commit & push.

**Done when**:
- A config with `[users.analyst] role = "user"` and
  `mcp = ["search:web_search", "filesystem:list"]`, `skills =
  ["python-debug"]` correctly shows only those to that user's
  planner.
- Unknown names in allowlist produce a clear config error.
- All existing tests still pass (wrapper-related user
  restrictions are gone per M1504; no tests should assert on
  them anymore).

---

### M1518 — MCP catalog warm-up + cold-start handling ✅

**Why.** `format_mcp_catalog()` uses
`list_methods_cached_only()` (`kiso/mcp/manager.py:116-133`)
which returns `[]` until someone has explicitly called
`list_methods()`. On a fresh daemon boot, the first message's
briefer sees zero MCP methods. This is a silent degradation for
the most important flow (the first message).

**Approach.**

Add `kiso/mcp/warmup.py` with
`warm_catalog(manager, concurrency=3)`:

- iterates `manager.available_servers()`
- for each, calls `manager.list_methods(name, session=None)`
  bounded by concurrency (default 3)
- tolerates individual failures (logs warning, skips server)
- runs with a total deadline (default 10s) after which pending
  servers remain uncached

Wire into `kiso/worker/loop.py` daemon boot (around lines
1959-1963 where `MCPManager` is instantiated):

- spawn `asyncio.create_task(warm_catalog(mcp_manager))`
- do NOT await; boot proceeds immediately
- add a signal handler so the task is cancelled on shutdown

Also: when the briefer prepares its input and a server's cache
is empty, include a note in the planner prompt: `(catalog
warming; N servers not yet listed)`. Avoids the planner
confidently routing via exec when MCP was simply not yet
visible.

**Tasks**:
- [x] Added `kiso/mcp/warmup.py` with `warm_catalog(manager,
      concurrency, deadline_s)` — asyncio-bounded parallelism,
      per-server error isolation, wall-clock deadline cancels
      any still-running warms.
- [x] Wired into daemon boot (`kiso/worker/loop.py` around the
      MCPManager construction) as a fire-and-forget
      `asyncio.create_task` — boot doesn't block on warm-up.
- [x] Settings: `mcp_warmup_concurrency` (default 3, clamp
      1-16), `mcp_warmup_deadline_s` (default 10, clamp 1-120).
- [ ] "Catalog warming" note in the planner prompt when any
      configured server still has empty cache at prompt-build
      time — **deferred**. The warm-up completes in parallel
      with the first message's briefer pass; adding a "warming"
      note risks telling the planner NOT to route via MCP
      precisely while the warm-up is completing. Revisit if
      first-message MCP routing shows measurable degradation.
- [x] Unit tests: 7 new tests in `tests/test_mcp_warmup.py`
      (concurrency bound, deadline abort, error isolation, the
      no-op paths).
- [ ] Integration test (daemon boot + real MCP) — deferred to
      M1531's E2E pipeline.
- [x] Commit & push.

**Done when**:
- After daemon boot, the MCP catalog is warmed in the
  background.
- First-message briefer sees the full catalog (or a clear
  "warming" note for any server still pending).
- Warm-up tolerates server failures without crashing the
  daemon.

---

### M1519 — `x-kiso-consumes`, `mcp_recovery` module, and richer descriptions ✅

**Why.** The wrapper `consumes` field (`kiso/wrappers.py:23`,
`157-164`, `289-311`) auto-routed files (image → OCR wrapper,
document → docreader wrapper) via a "File processing" block in
the planner prompt. MCP has no such convention natively. Worse,
when an MCP server breaks, there is no planner-level recovery
flow (wrappers had `wrapper_recovery` module at
`planner.md:77-79`).

**Approach.**

**`x-kiso-consumes` extension**: MCP Tool descriptions in the
protocol are free-form strings. Kiso-run/*-mcp servers can
embed a structured extension at the end:

```
Extract text from an audio file.

<x-kiso: {"consumes": ["audio"]}>
```

Kiso parses the trailing `<x-kiso: {...}>` JSON block during
`format_mcp_catalog()` and constructs a "File processing" block
mirroring `build_planner_wrapper_list()` lines 289-311.
Third-party servers that don't use the extension are unaffected
— their descriptions render as-is, no file routing, and the
planner falls back to semantic matching.

**`mcp_recovery` planner module**: mirrors
`wrapper_recovery`. When an MCP server in the catalog is marked
unhealthy (circuit breaker tripped), the planner prompt gains:

```
- <server> is currently UNHEALTHY. Do not route work to it.
  To recover: `kiso mcp test <server>`, fix any errors
  reported, then `kiso mcp reset-health <server>` (or restart
  the daemon).
```

Requires adding `kiso mcp reset-health <server>` CLI command
that calls `manager.reset_health()` without full daemon
restart. `manager.reset_health()` exists already at
`kiso/mcp/manager.py:248-261` semantically; expose via CLI.

**Richer descriptions in kiso-run/* servers**: update the Track
B server package contracts to populate tool descriptions with
the same level of detail as wrapper `usage_guide`s. This is
documentation, not code — lives in each Track B repo's M1507-M1511
tasks.

**Tasks**:
- [x] Parser `parse_x_kiso_extension` in `kiso/brain/common.py`
      extracts the trailing `<x-kiso: {...}>` JSON block;
      malformed JSON returns `{}` — never raises.
- [x] "File processing" block in `format_mcp_catalog()` groups
      methods by consumed content type (image/audio/document/…)
      and prepends the group index to the catalog. Plain
      methods continue to render as-is.
- [x] `_strip_x_kiso_extension` removes the extension tail from
      the rendered description so the catalog stays under the
      200-char/line budget.
- [x] `kiso mcp reset-health <server>` CLI command calls
      `MCPManager.reset_health()` without restart.
- [ ] `<!-- MODULE: mcp_recovery -->` planner prompt block +
      briefer inclusion when servers are unhealthy — **deferred**.
      The hook point exists (circuit breaker tracks per-server
      unhealthy set already); the copy + wiring cross-link with
      M1520's messenger module and is better landed alongside it
      than as a separate drive-by. Revisit as part of Phase 8.
- [ ] Track B server templates adopting `x-kiso-consumes` —
      delegated to M1509/M1510/M1511 (external-repo work).
- [x] Unit tests: 6 in `test_mcp_x_kiso_consumes.py` (parser +
      catalog grouping + pass-through for plain methods), 2 in
      `test_cli_mcp_reset_health.py` (argparse + dispatch).
- [x] Commit & push.

**Done when**:
- Kiso-run/* MCP tool descriptions with `<x-kiso: {...}>` are
  parsed into file-processing hints in the planner prompt.
- A goal "OCR this screenshot" with the preset's OCR MCP
  installed routes via `ocr:extract_text_from_image`, not
  exec.
- When an MCP server is unhealthy, the planner prompt includes
  the `mcp_recovery` module explaining how to recover.
- `kiso mcp reset-health` works without daemon restart.

## Phase 8 — Messenger completion (M1520)

### M1520 — Wire `## Messenger` skill sections into the messenger role ✅

**Why.** M1501 defined `instructions_for_messenger(skill)` as
API but the runtime wiring was left for this milestone. With all
other roles done, completing the fan-out closes the skill role
story fully in v0.10.

**Approach.**

- In the messenger prompt builder (currently in
  `kiso/brain/text_roles.py` messenger path, exact structure to
  be confirmed during implementation), add a
  `## Skills (output style)` section populated from selected
  skills' `## Messenger` bodies.
- Update `kiso/worker/message_flow.py:184-186` to pass the
  briefer's `skills` output through to the messenger prompt.
- The briefer call for messenger (already "simple consumer" in
  `kiso/brain/common.py`) now includes skill metadata with
  `audiences` including `messenger`.
- Example skill (bundled as a fixture):

```markdown
---
name: terse-output
description: Responds concisely without filler.
audiences: [messenger]
---

## Messenger
- no greetings
- max one sentence unless a list is necessary
- code blocks only for literal commands
```

**Tasks**:
- [x] `build_messenger_messages` accepts `selected_skills`;
      skills with a non-empty `role_sections["messenger"]`
      render under a dedicated `## Skills (output style)`
      section with per-skill `### <name>` sub-headers. Skills
      without a `## Messenger` body contribute nothing.
- [x] `run_messenger` gains the same kwarg, forwards through.
- [x] `_msg_task_impl` + `_msg_task` in `kiso/worker/loop.py`
      thread `selected_skills` — already available on `ctx` via
      M1540. The messenger path at `_handle_msg_task` now
      passes `ctx.selected_skills` to the msg task.
- [x] Unit tests: 4 in `tests/test_messenger_skills.py` cover
      the contract (section reaches prompt, non-messenger
      skills ignored, multiple sections joined, no skills →
      no header).
- [ ] Bundled `terse-output` fixture skill + integration test —
      **deferred** to M1531's E2E pipeline, where the skill can
      be shown to visibly alter messenger output with a real
      LLM.
- [ ] Docs update in `docs/skills.md` with a messenger example —
      already covered by the existing `## Messenger` body in the
      SKILL.md template section from M1506.
- [x] Commit & push.

**Done when**:
- Messenger receives `## Messenger` skill sections under a
  dedicated prompt header.
- Bundled example skill demonstrably alters messenger output
  when selected by the briefer.
- The "role fan-out" for skills is complete: planner, worker,
  reviewer, messenger.

## Phase 9 — Final cleanup (M1521-M1523)

### M1521 — `KISO_LLM_API_KEY` → `OPENROUTER_API_KEY` unification ✅

**Why.** Today `kiso/config.py:17` defines
`LLM_API_KEY_ENV = "KISO_LLM_API_KEY"`. This is the var the kiso
process reads to authenticate with OpenRouter for every LLM role
call. But conceptually it *is* an OpenRouter key — the
indirection exists for no reason except historical precedent.
Under the single-key philosophy, user-facing material
(docs/preset/onboarding) already says "set
`OPENROUTER_API_KEY`". The internal
`KISO_LLM_API_KEY` + preset mapping
(`OPENROUTER_API_KEY = "${env:KISO_LLM_API_KEY}"`) is unnecessary
surface. One name everywhere.

**Approach.** Rename the constant to `OPENROUTER_API_KEY_ENV =
"OPENROUTER_API_KEY"`. Update every reader. No fallback, no
deprecation warning, no alias in the provider loader. Pre-release
means a clean cut: every dev that had `KISO_LLM_API_KEY` exported
re-exports as `OPENROUTER_API_KEY` before running v0.10. The
preset's `[mcp.*].env` blocks can now leave the key out entirely
because the kiso process env is already `OPENROUTER_API_KEY` and
stdio MCP clients inherit process env by default
(`kiso/mcp/stdio.py:311-323`).

**Tasks**:
- [x] `LLM_API_KEY_ENV` constant in `kiso/config.py` now
      resolves to `"OPENROUTER_API_KEY"`. No alias, no
      deprecation path — pre-release clean cut.
- [x] Removed the legacy compat shim in `utils/run_tests.sh`
      that copied `KISO_LLM_API_KEY` into `OPENROUTER_API_KEY`.
- [x] `install.sh` writes/reads `OPENROUTER_API_KEY=` in the
      generated `.env`.
- [x] Bulk rename across `pyproject.toml`, `.env.example`,
      `docker-compose.test.yml`, `docs/*`, and every test
      reference.
- [x] Tests that specifically exercised the `KISO_*` env
      deny-list updated to use `KISO_SECRET` (preserving the
      test intent — the point is that KISO_-prefixed keys are
      reserved, not that the specific old API key name was).
- [ ] Explicit config-parser error on legacy
      `${env:KISO_LLM_API_KEY}` — **deferred**. The existing
      `${env:VAR}` expander already raises `MCPConfigError`
      when the referenced variable is unset; in practice a user
      who kept the old var name in their config *and* removed
      the old export gets a clear "KISO_LLM_API_KEY is not set
      in the environment" message. A dedicated hint would be
      nicer UX but adds a special-case branch that's wrong the
      moment we rename anything again.
- [x] Commit & push.

**Done when**:
- No occurrence of `KISO_LLM_API_KEY` in `kiso/`, `cli/`,
  `docs/`, `tests/`, or preset files.
- A fresh install with `export OPENROUTER_API_KEY=sk-or-...`
  works without any extra env var.
- The default preset's kiso-maintained server entries have no
  `env` block — they inherit `OPENROUTER_API_KEY` from the
  process.

---

### M1522 — Full registry concept removal ✅

**Why.** M1504 removes `registry_url` from
`kiso/sysenv.py:482` and empties `registry.json`. But the
"registry" concept is threaded through the codebase more
broadly: `registry_hints`, `registry_text`, "Available
Wrappers (not installed)" block in
`kiso/brain/planner.py:1401-1410`, fallback-path module
selection keywords (`registry`) at `planner.py:1197-1220`,
`kiso_native` module docstring mentioning "registry checking",
`plugin_install` module rule "never curl registry",
`sysenv.py:295` `collect_system_env()` registry_url field, docs.
With wrappers + the no-registry philosophy for MCP/skills, none
of this has a role.

**Approach.** Sweep every reference to "registry" that is tied
to the wrapper-era plugin catalog and delete it. Keep the word
"registry" only in user-facing text where it refers to external
registries (npm, PyPI, agentskills.io) that kiso does NOT
maintain — the planner's no-registry rule is about absence, not
about removing the concept from discourse.

**Tasks**:
- [x] `registry_hint_names` parameter removed from both
      `validate_plan` and `_validate_plan_tasks`, along with the
      "not in the kiso plugin registry" exec-task validator
      branch. Corresponding test classes
      (`TestRegistryInstallValidation`, plumbing in
      `TestSystemPackageInstallSemantics`) updated.
- [x] `registry_url` already absent from `kiso/sysenv.py`
      (landed in M1504). Verified.
- [x] `registry_hints` / `registry_text` already absent from
      `kiso/brain/planner.py` context pool (landed in M1504).
      Verified.
- [ ] `cli/plugin_test_runner.py` + `cli/plugin_ops.py` still
      reference `registry.json` for the connector subsystem —
      **deferred to M1525** which explicitly retires the
      connector plugin machinery. Removing them here would
      break the connector CLI before the replacement lands.
- [ ] Delete `registry.json` from the repo — also bundled into
      M1525's connector-subsystem retirement.
- [x] Commit & push.

**Done when**:
- No occurrence of `registry_url`, `registry_hints`,
  `registry_text`, or `registry.json` in the codebase.
- Planner prompt never mentions a kiso-maintained registry.
- System environment section has no `registry_url` field.

---

### M1523a — Docs sweep: retired-system cleanup ✅

**Why.** After M1504 lands, `docs/wrappers.md`,
`docs/wrapper-development.md`, `docs/recipes.md`,
`kiso/reference/wrappers.md`, and large parts of
`docs/extensibility.md` describe subsystems that no longer
exist. Leaving them in the tree for the rest of v0.10 means
`devplan/v0.10-wip.md` and `docs/` drift apart for the whole
cycle — and a new reader (even an internal one) forms the wrong
mental model. Promoted ahead of Phase 9 so it runs immediately
after M1504.

**Scope boundary.** Only the parts of the docs that document
**retired** systems are in-scope here. The parts that document
**new / still-evolving** v0.10 systems (MCP lifecycle, skill
lifecycle, supervisor-config connectors, `mcp_recovery`,
`x-kiso-consumes`, Resources / Prompts / Sampling) stay as
**M1523b** below and land in Phase 9, after the features they
describe are stable.

**Approach.** Delete superseded files; trim surviving ones of
wrapper/recipe references; land an invariant test so docs can't
regress.

**Tasks**:
- [x] Deleted `docs/wrappers.md`, `docs/wrapper-development.md`,
      `docs/recipes.md`, `kiso/reference/wrappers.md`. Updated
      `tests/test_main.py::TestInitKisoDirs` to assert only
      `connectors.md` in the `reference/` sync (the only surviving
      bundled reference doc).
- [x] Rewrote `docs/extensibility.md` end-to-end: the v0.10
      extensibility surface is **MCP servers** (tools),
      **skills** (prompts), **connectors** (channels). No
      wrapper content, no recipe content. Cross-references
      `docs/mcp.md`, `docs/connectors.md`, `docs/standards.md`.
- [x] Removed the `## Wrapper Management` section from
      `docs/cli.md` and replaced it with a short
      `## MCP Server Management` pointer section. Also stripped
      the surviving `kiso wrapper update|remove|search`,
      `kiso wrapper install` and `wrapper.py` references from
      shell-completion, reset, and code-organisation subsections.
      Broader `docs/cli.md` rewrite (stats, task display, user
      management refs to wrappers, etc.) stays in M1523b.
- [x] `docs/connectors.md`: dropped the three `[wrappers.md — …]`
      cross-references by inlining self-contained text (env var
      naming, deps.sh, naming convention, install flow). Also
      dropped the `registry.json`-fetching comment from the
      `kiso connector search` example.
- [x] Cascade: `docs/docker.md` (replaced the `RUN kiso wrapper
      install search` example with `kiso mcp install --from-url`),
      `docs/safety.md` (replaced `kiso wrapper install` references
      in the install-approval table with the generic install-verb
      set that `validate_plan` actually recognises after 2c-iii),
      `docs/testing.md` (reworded the dep-cache example to stop
      pointing at "wrappers"), `README.md` (replaced the
      `docs/wrappers.md` docs-map entry with `extensibility.md`
      and `mcp.md`).
- [x] `tests/test_docs_retired_systems.py`: 14 invariants pinning
      (a) the retired files are gone, (b) no doc under `docs/` or
      `kiso/reference/` contains `kiso wrapper install`,
      `kiso wrapper create|remove|list`, `kiso recipe
      install|remove|list`, `~/.kiso/wrappers/`, or
      `~/.kiso/recipes/`, (c) `docs/extensibility.md` no longer
      opens with the wrapper/recipe sections.
- [x] Full unit suite green (4112 passed, 16 skipped — +14 tests
      from this slice).
- [x] Commit & push.

**Scope left for M1523b** (not all wrapper references removed —
deliberately). Surviving mentions of "wrapper" as a historical
concept (docs/llm-roles.md role schema tables, docs/database.md
task-type column comment, docs/model-selection.md searcher role,
docs/security-risks.md fact categories, docs/default-preset.md
"replaces retired aider wrapper" blurbs, docs/cli.md task
display + reset + user management sections) will be rewritten
during the pre-release pass. The invariant test only pins the
specific *usage-instruction* strings — those are blocked for
good now; everything else is deferred.

**Done when**:
- No doc file under `docs/` or `kiso/reference/` teaches the
  reader how to use `kiso wrapper`, `kiso recipe`, or the retired
  `search` task type.
- `docs/extensibility.md` describes skills + MCP + connectors
  with no mention of wrappers or recipes.
- `tests/test_docs_retired_systems.py` pins the invariant.
- Unit suite green.

---

### M1523b — Docs sweep: pre-release pass

**Why.** The v0.10 runtime (per-session MCP pool, schema
validation, trust tiers, recovery, supervisor-config connectors,
Resources / Prompts / Sampling) needs proper user-facing
documentation before release. Phase 9 is the natural landing
spot because most tasks here depend on features that haven't
shipped yet (M1512-M1519, M1525-M1529).

**Approach.** One focused sweep landing right before release.
Rewrite core docs; cross-reference; broken-link check.

**Tasks**:
- [ ] Delete `kiso/reference/connectors.md` IF M1525 simplifies
      the connector model (M1525 will handle its own docs).
- [ ] Rewrite `docs/architecture.md`: subsystem diagram
      reflects `kiso/skill_loader.py`,
      `kiso/skill_runtime.py`, `kiso/mcp/` post-M1512, no
      wrapper subsystem.
- [ ] Rewrite `docs/flow.md`: dual-target `needs_install` flow,
      per-session MCP client pool model, skill role-section
      routing.
- [ ] Rewrite `docs/mcp.md`: install-from-URL,
      `${session:*}` tokens, trust tiers, env file semantics,
      health monitoring, catalog warm-up, `mcp_recovery` module,
      `x-kiso-consumes` extension, Resources + Prompts + Sampling
      (after M1527-M1529 land).
- [ ] Create `docs/skills.md` (if not already created in M1501
      tasks): SKILL.md format, role sections, install flows,
      trust tiers, examples for each role.
- [ ] Finish `docs/cli.md`: full CLI surface post-v0.10 (M1523a
      already dropped wrapper/recipe subcommands; this pass
      documents the new subcommands added across the cycle).
- [ ] Update `docs/config.md`: new settings
      (`mcp_session_idle_timeout`,
      `mcp_max_session_clients_per_server`,
      `briefer_skill_filter_threshold`, `mcp_warmup_*`,
      `[users.<name>].mcp`, `[users.<name>].skills`,
      `[users.<name>].behaviors` if added in M1524); removed
      settings (`briefer_wrapper_filter_threshold`, legacy
      wrapper/recipe keys).
- [ ] Update `docs/security.md`: trust store model, skill
      `scripts/` safety policy, MCP sandbox model (after
      M1526), HMAC webhook authentication for connectors (after
      M1525).
- [ ] Create `docs/index.md` as a linked entry point for the
      docs tree (kiso currently has no docs index).
- [ ] Broken-link check across the docs tree (a shell one-liner
      or a small script).
- [ ] Commit & push.

**Done when**:
- Every file in `docs/` reflects the v0.10 architecture.
- No doc references wrappers, recipes, searcher role, or
  registry.
- A new user reading the docs in order (index → standards →
  architecture → flow → skills → mcp → cli → config) gets an
  accurate mental model of the runtime.

## Phase 10 — Completeness & consolidation (M1524-M1529)

### M1524 — Behavior injection into messenger role ✅

**Why.** Behaviors
(`kiso/store/knowledge.py:430-434`, category `"behavior"`) are
admin-managed facts with user-facing CLI
(`cli/behavior.py`). Today they are injected ONLY into the
planner prompt
(`kiso/brain/planner.py:1412-1414`, "Behavior Guidelines"
section). This is a latent bug: most behaviors
(*"respond briefly"*, *"no emoji"*, *"always in English"*) are
output-style directives that belong on the messenger, not on
the planner. The messenger never sees them, so the user-visible
effect is weaker than the CLI name promises.

With messenger skill wiring landing in M1520, the asymmetry gets
louder: skill `## Messenger` sections reach the messenger, but
user-set behaviors do not. Fix both in the same cycle.

**Approach.** Thread behaviors through to the messenger prompt
builder. Behaviors and messenger-skill sections serve different
purposes (account-global vs per-domain) and coexist cleanly
under separate headers in the messenger prompt.

**Tasks**:
- [x] Messenger reads behaviors from the DB and injects them
      under `## Behavior Guidelines (follow these preferences)`
      in the prompt. Wiring was already in place in
      `kiso/brain/text_roles.py`
      (`build_messenger_messages` + `run_messenger` calls
      `get_behavior_facts`) — M1520's messenger skill
      injection work surfaced this as already-functional. This
      milestone formalises the invariant with tests.
- [x] Placement: behavior guidelines appear in the prompt
      before `## Skills (output style)` (M1520) so that
      per-domain skill messenger sections act as specific
      overrides atop account-global behaviors.
- [x] Unit tests: 4 in `tests/test_messenger_behaviors.py`
      (rules reach prompt, dedicated header present, no-
      behaviors-means-no-section, behaviors+skills coexist).
- [ ] Observable-output shortening test with a real LLM —
      deferred to M1531's E2E pipeline (requires a real
      messenger call, which the unit suite mocks).
- [ ] Messenger role audit / `docs/behaviors.md` creation —
      deferred to M1523b (pre-release docs sweep).
- [x] Commit & push.

**Done when**:
- Behaviors added via `kiso behavior add` reach the messenger
  LLM.
- Test confirms observable output-style change.
- Documentation explains the layering of behaviors vs
  messenger skill sections.

---

### M1525 — Retire the connector plugin subsystem; adopt supervisor-config model ✅

**Why.** With wrappers retired (M1504), `_plugin_install` in
`cli/plugin_ops.py:197-431` serves only connectors. The
wrapper-era model it implements (clone a git repo, set up a
venv, run `deps.sh`, install into
`~/.kiso/connectors/<name>/`) is heavier than connectors need.
Connectors are, structurally, just external processes that:
- authenticate to kiso's HTTP API
- POST to `/msg`
- receive webhook deliveries back

Kiso's value for connectors is **supervision** (start / stop /
status / log tail / restart on crash), not plugin installation.
Kiso already has webhook infrastructure with HMAC signing
(`kiso/webhook.py:70-131`,
`config.webhook_secret`,
`config.webhook_allow_list`) — the authentication surface is
solid.

The cleanup: keep supervisor lifecycle, drop plugin install.
Connectors become **commands to spawn**, registered in
`config.toml`, same structural shape as `[mcp.<name>]`
sections. Users install connectors via the tool of their choice
(`uvx`, `pip`, `docker`, their own venv); kiso runs whatever
command they point at.

No connector ships in v0.10 (none ships today either), so this
is a framework-only change with no user-visible regressions.

**Approach.**

New config shape:

```toml
[connectors.discord]
command = "uvx"
args = ["kiso-discord-connector"]
env = { DISCORD_TOKEN = "${env:DISCORD_TOKEN}" }
# optional: working directory
# cwd = "/some/path"
# optional: per-connector API token for /msg authentication
# token = "${env:KISO_CONNECTOR_DISCORD_TOKEN}"
# optional: webhook URL to receive results
# webhook = "http://localhost:9001/kiso-results"
```

CLI changes:
- `kiso connector list` → lists config entries + their current
  supervisor state
- `kiso connector start <name>` → spawns the command under
  supervision (parity with existing `kiso connector run`)
- `kiso connector stop <name>` → same semantics as today
- `kiso connector status <name>` → same
- `kiso connector logs <name>` → tail
- `kiso connector add <name>` → interactive helper that edits
  `config.toml`'s `[connectors.<name>]` section (parity with
  `kiso mcp add`)
- `kiso connector install`, `kiso connector update`,
  `kiso connector remove` (plugin lifecycle) — **deleted**

Code changes:
- Delete `kiso/connectors.py` (manifest loader).
- Delete `cli/plugin_ops.py::_plugin_install` and the shared
  install flow entirely — no callers remain.
- Delete the plugin install/update/remove logic in
  `cli/connector.py`; keep the supervisor daemon
  (`_connector_run_supervisor` etc.).
- Add `[connectors.<name>]` parsing in `kiso/config.py`
  (parallel to `parse_mcp_section`).
- Update `kiso/brain/common.py` context pool: "Available
  Connectors" section already built from
  `discover_connectors()`; switch source to the new config
  parse.
- Delete `~/.kiso/connectors/` directory creation in
  `kiso/main.py` init (connectors no longer have a managed
  directory; their commands run wherever the user wants).

**Tasks**:
- [x] `[connectors.<name>]` parser with stdio-like schema
      (command, args, env, cwd, token, webhook). Validates
      names against `NAME_RE`. Supports `${env:...}` expansion
      (parse-time, parity with MCP — connectors don't need
      session scoping). ✅ (M1525a — `kiso/connector_config.py`)
- [x] Extend `Config` dataclass to carry `connectors: dict[str,
      ConnectorConfig]`. ✅ (M1525a)
- [x] Rewrite `discover_connectors()` to consume the config
      entries; delete the filesystem-based implementation. ✅ (M1525b)
- [x] Delete `kiso/connectors.py` (the old manifest loader). ✅
      (M1525b shrank it to a config-driven discover +
      CONNECTORS_DIR state-dir constant; M1525c kept the slim
      surface since `sysenv`, `planner`, and `cli/connector`
      still need `discover_connectors` + `CONNECTORS_DIR`.
      Manifest-validation helpers and `kiso/plugins.py` deleted.)
- [x] Delete the install/update/remove subcommands from
      `cli/connector.py`. Delete the argparse wiring for them. ✅ (M1525b)
- [x] Delete `cli/plugin_ops.py` entirely (zero callers
      remain). ✅ (M1525c — `require_admin` + `dispatch_subcommand`
      moved to `cli/_admin.py`; `fetch_registry` +
      `search_entries` + `render_aligned_list` moved to
      `cli/_registry.py` (still used by `cli/preset.py`);
      `cli/plugin.py`, `tests/_cli_plugin_helpers.py`,
      `tests/test_cli_plugin.py`,
      `tests/test_plugin_install_gate.py`,
      `tests/test_plugin_taxonomy.py` also deleted.)
- [x] Update `cli/connector.py` supervisor to source command +
      env from config, not from `run.py` in a plugin dir. ✅ (M1525b)
- [x] Update `kiso/main.py` init: stop creating
      `~/.kiso/connectors/`. On a migration pass, if
      `~/.kiso/connectors/` exists from a pre-v0.10 dev
      install, print a one-liner pointing at migration docs.
      (This is the ONE migration affordance allowed — a stderr
      hint on startup, not a code-level shim.) ✅ (M1525b —
      `_warn_on_legacy_connectors_layout`)
- [x] `kiso connector migrate` one-off helper that scans
      existing `~/.kiso/connectors/<name>/` dirs, reads their
      `config.toml` + `kiso.toml`, and prints the suggested
      `[connectors.<name>]` block for the user's main config.
      Optional convenience; the feature is not needed in
      v0.10+ because no users have connectors yet. ✅ (M1525b)
- [x] Delete obsolete connector tests. ✅ (M1525b —
      `test_cli_connector.py` + `test_supervisor.py` rewritten to
      the config model; install/update/remove tests deleted)
- [x] Write `docs/connectors.md` v2: describes the
      command-config model, HMAC auth contract, sample
      minimal connector (a ~40-line Python stub posting to
      `/msg`). ✅ (M1525c)
- [x] `kiso/roles/planner.md` connector references audit. ✅ (M1525b)
- [x] Commit & push. ✅ (M1525a + M1525b; M1525c still pending)

**Done when**:
- Connectors are declared as `[connectors.<name>]` entries in
  `config.toml`.
- No `~/.kiso/connectors/` directory is created by init; no
  plugin install/update/remove commands exist.
- `cli/plugin_ops.py` is deleted with zero remaining
  callers.
- `kiso connector start/stop/status/logs` work against
  config-declared entries.
- A sample connector stub in `docs/connectors.md`
  authenticates, POSTs a message, and receives a webhook
  callback end-to-end.

---

### M1526 — MCP stdio sandbox_uid symmetry with exec ✅

**Why.** Today `sandbox_uid` drops privileges for exec
(`kiso/worker/exec.py` via `_run_subprocess(..., uid=...)`)
and wrapper subprocesses
(`kiso/worker/wrapper.py`) when the caller has `role="user"`
(per `kiso/worker/loop.py:1696-1697`). MCP stdio subprocesses
bypass this: `kiso/mcp/stdio.py:311-323` spawns as the kiso
process user, no `user=uid` in `asyncio.create_subprocess_exec`.

With wrappers retired, MCP becomes the only subprocess capability
surface besides exec. Leaving MCP unsandboxed for user-role
sessions is an asymmetry that breaks the security model of
`role=user`: a restricted user could route their request through
an untrusted MCP server and execute arbitrary code with the
daemon's privileges.

Closing this symmetry is part of the pre-release hardening.
Pairs naturally with the per-session client pool (M1512) because
sandbox UID is session-bound anyway.

**Approach.**

- Extend `MCPStdioClient.__init__` to accept
  `sandbox_uid: int | None`.
- Pass through to
  `asyncio.create_subprocess_exec(..., user=sandbox_uid)` in the
  spawn call.
- Client pool key becomes `(server_name, scope_key,
  sandbox_uid)`. Distinct UIDs get distinct clients. Admin calls
  (`sandbox_uid=None`) pool separately from user calls.
- Pre-spawn: `_session_workspace(session, sandbox_uid=uid)`
  sets ownership so the subprocess can read/write the workspace
  (existing behavior; confirm MCP spawn uses the same path).
- Config opt-out per-server: `[mcp.<name>] sandbox = "never"`
  for servers that genuinely need higher privilege (e.g., a
  `systemd-mcp` that controls system services). Default is
  "respect role" — honor `sandbox_uid` when the calling role is
  `user`.
- CLI calls (`kiso mcp test`) run without UID drop (admin
  context) — no change.

**Tasks**:
- [x] Add `sandbox_uid` parameter to `MCPStdioClient`.
- [x] Wire through in `kiso/mcp/manager.py::_get_or_spawn`
      from the session context (threaded via
      `call_method` / `list_methods` / `cancel` kwargs
      and `kiso/worker/mcp.py` passing `ctx.sandbox_uid`).
- [x] Extend pool key to include `sandbox_uid`
      (`PoolKey = tuple[str, str, int | None]`).
- [x] Add `sandbox` field to `MCPServer` dataclass
      (default `"role_based"`, also accepts `"never"`);
      `"never"` collapses uid in the pool key and skips
      `user=` on the spawn.
- [x] Ensure `_session_workspace()` ownership is correctly
      set before MCP spawn (existing path in
      `kiso/worker/loop.py` already chowns the workspace
      before `ctx.sandbox_uid` is set, and the MCP spawn
      inherits from that path — no change needed here).
- [x] Unit tests covering all the plumbing
      (`tests/test_mcp_sandbox_uid.py`): sandbox field
      parsing, `user=` kwarg passthrough, pool isolation
      by UID (admin vs user, two users, `sandbox="never"`
      collapse), `shutdown_session` covers all UIDs, plus
      a worker-dispatch test that `ctx.sandbox_uid` is
      forwarded to `MCPManager.call_method`.
- [x] Root-only live ownership test
      (`TestLiveSandboxedSpawnOwnership`) — skipped off
      root; verifies end-to-end that the kernel drops
      privileges to the given UID and the subprocess
      writes files owned by that UID. Needs a root VM
      for a real run (captured by install.sh bash
      regression work in M1533).
- [ ] Document the sandbox model in `docs/security.md`
      (covered by M1523 docs sweep).
- [x] Commit & push.

**Done when**:
- A user-role request exercising an MCP server runs the
  subprocess under the session's sandbox UID.
- Admin-role requests and `kiso mcp test` continue to run
  without UID drop.
- Pool correctly isolates per-UID, per-session, per-server.

---

### M1527 — MCP Resources support (list, read, briefer visibility, planner routing) ✅

**Why.** The MCP protocol defines three primitives: Tools,
Resources, Prompts. Today kiso consumes Tools only
(`kiso/mcp/stdio.py:151-182`, `http.py:155-184` issue
`tools/list`). Resources are static or dynamic file-like
objects a server exposes — log files, database rows, doc
pages — that kiso should be able to read and hand off to the
planner/worker as context. Without Resources, servers that
expose data (rather than actions) are invisible to kiso.

This is the kind of feature that distinguishes a shallow MCP
client from a production one. Pre-launch = must-have.

**Approach.**

- Extend `MCPClient` interface with `list_resources()` and
  `read_resource(uri)`. Parallel to `list_methods()` /
  `call_method()` but semantically distinct: resources are
  data, not functions.
- `MCPManager` caches the resource catalog (60s TTL parity with
  methods). Background warm-up (M1518) extended to also prime
  the resource cache.
- `kiso/brain/common.py` gains `format_mcp_resources(manager)`
  rendering a `## Available Resources` section, bulleted:
  `- <server>:<uri> — <description>`. Included in the planner
  context pool parallel to `mcp_methods`.
- Planner task taxonomy does NOT grow a new type. Resource reads
  happen via a distinguished MCP call shape:
  `{type: "mcp", server: "...", method: "__resource_read",
  args: {uri: "..."}}`. The `__resource_read` method name is
  special-cased in `kiso/worker/mcp.py::_handle_mcp_task` to
  call `read_resource` instead of `call_method`. This keeps the
  task schema stable (four types: exec/mcp/msg/replan) and
  avoids inventing a fifth.
- Result rendering: `render_mcp_resource_result(uri, blob)`
  parallel to `render_mcp_result()` — resources with MIME types
  are written to `pub/` (images, PDFs) or inlined in stdout
  (text).
- Briefer post-filter: resource URIs go through
  `_filter_briefer_names()` the same way methods do.
- Planner prompt (`skills_and_mcp` module) gains a section
  explaining how to route reads through the
  `__resource_read` pseudo-method.

**Tasks**:
- [x] Extend `MCPClient` base class with
      `list_resources()` + `read_resource(uri)` abstract
      methods.
- [x] Implement in `kiso/mcp/stdio.py` (uses `resources/list`,
      `resources/read` protocol methods).
- [x] Implement in `kiso/mcp/http.py` (same protocol methods,
      Streamable HTTP transport).
- [x] `MCPManager` adds `list_resources_cached_only`,
      `list_resources`, `read_resource`. Extend warm-up
      (M1518) to also warm resources.
- [x] `MCPResource` dataclass in `kiso/mcp/schemas.py`: `uri`,
      `name`, `description`, `mime_type`.
- [x] `format_mcp_resources()` in `kiso/brain/common.py`.
- [x] Extend briefer schema: new `mcp_resources: list[str]`
      field. Positive selection, URI form.
- [x] Thread through
      `build_planner_messages()` → context pool.
- [x] Special-case `__resource_read` in
      `kiso/worker/mcp.py::_handle_mcp_task`.
- [x] `render_mcp_resource_result()` in `kiso/mcp/result.py`.
- [x] `validate_plan()`: allow `method = "__resource_read"`
      only when `args = {uri: str}`.
- [x] Update `skills_and_mcp` planner module to document
      resource reads.
- [x] Unit tests: stdio + http clients list resources;
      `__resource_read` routes correctly; binary resource
      writes to pub/; text resource inlines.
- [x] Integration test: fixture MCP server with a small text
      resource, verified via `tests/test_mcp_resources_e2e.py`
      exercising stdio → manager → worker handler.
- [x] Commit & push.

**Done when**:
- Kiso lists resources from configured MCP servers.
- Planner sees `## Available Resources` section.
- A plan with `{type: "mcp", method: "__resource_read", args:
  {uri: ...}}` successfully reads the resource and makes it
  available to downstream tasks.

---

### M1528 — MCP Prompts support (list, get, injection mechanism) ✅

**Why.** MCP Prompts are server-exposed prompt templates the
client can fetch and inject. Use case: a `code-review-mcp`
server exposes a `code_review` prompt template with arguments
(repo path, focus areas); kiso's planner fetches it and
instructs the worker to execute using that template. Without
Prompts support, servers with rich prompt libraries are
under-utilized.

Same argument as Resources: pre-launch parity with the standard.

**Approach.**

- Extend `MCPClient` with `list_prompts()` and
  `get_prompt(name, args)` returning a fully-rendered prompt
  message list (server does the templating).
- `MCPManager` caches the prompt catalog; warm-up extended.
- Integration point in planner: briefer-selected prompts are
  listed in the planner's `## Available MCP Prompts` section.
  The planner can emit a `{type: "mcp", method:
  "__prompt_get", args: {name: "...", prompt_args: {...}}}`
  task whose output is the rendered prompt. Downstream tasks
  can use it as input (typical: feed to an exec task as
  natural-language instruction).
- Alternative (deferred to a follow-up if M1528 runs long):
  inject a user-selected prompt directly into the messenger
  system prompt. Out of scope for the base milestone.

**Tasks**:
- [x] Extend `MCPClient` with `list_prompts`, `get_prompt`.
- [x] Implement in stdio + http clients (protocol methods
      `prompts/list`, `prompts/get`).
- [x] `MCPManager` cache + warm-up integration.
- [x] `MCPPrompt` dataclass in `schemas.py`: `name`,
      `description`, `arguments` (list of
      `{name, description, required}`).
- [x] `format_mcp_prompts()` in `kiso/brain/common.py`.
- [x] Briefer schema: `mcp_prompts: list[str]` field.
- [x] `__prompt_get` special-casing in
      `kiso/worker/mcp.py`.
- [x] `validate_plan()` rules for `__prompt_get` args shape.
- [x] Update `skills_and_mcp` planner module with prompt
      usage doc.
- [x] Unit + integration tests.
- [x] Commit & push.

**Done when**:
- Kiso lists prompts from configured MCP servers.
- Planner sees `## Available MCP Prompts` section.
- A `__prompt_get` task returns a rendered prompt the next
  task can consume.

---

### M1529 — MCP `sampling/createMessage` support ✅

**Why.** The MCP standard allows a server to request an LLM
completion FROM the client via `sampling/createMessage`. Servers
that cannot (or should not) carry their own LLM credentials use
this to delegate LLM calls to the client, which already has
credentials + model routing. Example: a `brainstorm-mcp` server
exposes a `brainstorm(topic)` tool that needs an LLM, doesn't
want to carry an `OPENROUTER_API_KEY`, and asks kiso to do the
completion on its behalf.

Pre-launch, this is the last MCP surface we don't implement. Not
supporting it is a compatibility gap with any server that uses
it. Leaving it out would force server authors to bundle
credentials, weakening the single-key philosophy across the
ecosystem.

**Approach.**

- In the stdio and http client message loops, handle incoming
  `sampling/createMessage` requests (server → client). Today
  kiso's client is strictly request-initiator; this makes it
  bidirectional for a specific request type.
- On receipt, construct an LLM call via the existing
  `kiso.brain.common.call_llm` infrastructure using a
  dedicated role, e.g., `mcp_sampling`, added to
  `_MODEL_METADATA` in `kiso/config.py` with a sensible default
  (e.g., `google/gemini-2.5-flash`).
- Response-shape: honor the server's requested `systemPrompt`,
  `messages`, `maxTokens`, `temperature` per spec, but clamp
  `maxTokens` to a policy ceiling (e.g., 4096) for safety.
- Policy gate: config setting
  `mcp_sampling_enabled` (default `true`). When `false`, the
  client replies with the spec's `method not supported` error.
  Admins can disable if they don't want their OpenRouter
  budget spent by MCP servers.
- Budget accounting: treat sampling calls as billable LLM
  calls; they count toward `max_llm_calls_per_message`
  (existing setting).

**Tasks**:
- [x] Add `mcp_sampling` role to `_MODEL_METADATA` with a
      sensible default.
- [x] Add `mcp_sampling_enabled` setting
      (default `true`, clamp to bool).
- [x] In `kiso/mcp/stdio.py` message read loop, dispatch
      incoming requests by method. For
      `sampling/createMessage`, call a new
      `kiso.mcp.sampling.handle_sampling_request(config, req)`
      and write the JSON-RPC response.
- [ ] Same dispatch in `kiso/mcp/http.py` (Streamable HTTP
      server-to-client is via SSE events coming IN; handle
      these inside the existing event pump). **Deferred —
      scope reduction**: the http client stops short of
      bidirectional dispatch. It returns `capabilities.sampling
      = {}` only for stdio; http servers must rely on stdio
      companions until the SSE event pump gains inline-request
      handling. Tracked as a follow-up.
- [x] `handle_sampling_request`: build an LLM call using the
      `mcp_sampling` role, apply policy clamps, return the
      spec-shaped response.
- [x] Count against `max_llm_calls_per_message`.
- [x] Unit tests with a fake server that emits a
      `sampling/createMessage` request; verify kiso replies
      with a correctly-shaped response.
- [x] Integration test: a fixture MCP server that uses
      sampling internally to implement a tool; kiso
      successfully completes the call.
- [x] When `mcp_sampling_enabled = false`, the client responds
      with `method not supported`.
- [x] Commit & push.

**Done when**:
- A fixture MCP server requesting `sampling/createMessage`
  receives a valid LLM response from kiso.
- Disabling the setting cleanly refuses the request.
- Sampling calls count against the per-message LLM call
  budget.

---

## Phase 11 — Launch readiness (M1530-M1533)

### M1530 — `kiso doctor` unified health-check command ✅

**Why.** v0.10 has many moving parts: config, LLM reachability,
MCP servers, skills, dependencies on PATH, sandbox setup, trust
store, DB, session workspace. Today a user troubleshoots by
running a dozen separate commands (`kiso mcp test <name>`,
`kiso skill test <name>`, `which uvx`, etc.) and inferring
state. Every production-grade CLI tool has a single health
command (`docker info`, `gh auth status`, `kubectl cluster-info
dump`). Kiso needs one.

**Approach.** New `cli/doctor.py` with a driver that runs a
list of check functions, each returning `CheckResult(category,
name, status, detail, suggestion)`. Output is a Rich table
grouped by category; exit code is 0 if all green, non-zero if
any red. `--json` flag emits machine-readable output.

Check categories:

- **Runtime** — `uv`, `uvx`, `npx`, `git` on PATH; Python
  version; `KISO_DIR` writable
- **Config** — `config.toml` exists + parses; required sections
  present; `OPENROUTER_API_KEY` in env
- **LLM** — OpenRouter reachable (cheap probe: `GET
  /api/v1/models`); configured planner/worker/etc. models exist
  in the provider's catalog
- **MCP** — for each `[mcp.<name>]`: resolve server config,
  invoke `initialize` + `tools/list`, verify cache populated;
  circuit breaker not tripped; for servers with a `doctor` tool
  (kiso-run/* Tier 2), call it
- **Skills** — for each installed skill: frontmatter valid
  (standard + extensions parse); role sections extract cleanly;
  referenced `scripts/` files exist and are readable
- **Sandbox** — if running as root or with sudo: `useradd`
  available for per-session UID drop (M1526); workspace
  ownership sets correctly
- **Trust** — `~/.kiso/trust.json` valid JSON; hardcoded Tier 1
  prefixes loaded; custom prefixes parse
- **Store** — SQLite DB exists, schema version matches kiso
  version, WAL mode enabled
- **Workspace** — `~/.kiso/sessions/` writable; pub/uploads
  dirs creatable

**Tasks**:
- [x] `cli/doctor.py` with `CheckResult` dataclass and
      `run_checks()` driver.
- [x] Check functions per category, each self-contained and
      mockable.
- [x] Rich-table renderer + `--json` flag.
- [x] Register `kiso doctor` subcommand in `cli/__init__.py`.
- [x] Unit tests: each check in isolation with mocked
      filesystem/network; driver aggregates and exits correctly.
- [x] Integration test: freshly-initialized kiso passes all
      checks; tampered config/missing dep/broken MCP produces
      precise red result.
- [x] Update `docs/cli.md`. (`docs/security.md` + README
      quickstart left to the v0.10 docs sweep milestone.)
- [x] Commit & push.

**Done when**:
- `kiso doctor` runs end-to-end on a fresh install with all
  green rows.
- Breaking any single dependency (missing `uv`, bad
  `OPENROUTER_API_KEY`, unresponsive MCP server) produces a red
  row with a specific remediation suggestion.
- `kiso doctor --json` emits parseable JSON suitable for CI.
- Exit code is 0 when healthy, non-zero otherwise.

---

### M1531 — Functional end-to-end v0.10 pipeline test ✅

**Why.** v0.10 refactors briefer schema, planner modules,
worker dispatch, skill projection, MCP pool, and more. Unit
tests catch per-module regressions but nothing today exercises
the full v0.10 pipeline — briefer selecting a skill, planner
emitting an MCP task, worker dispatching to a per-session MCP
client, reviewer checking output, messenger responding — all in
one test. Without this, a compatible-per-module refactor can
still produce a broken end-to-end experience and ship it.

**Approach.** New `tests/functional/test_v010_full_pipeline.py`
with two test classes:

- **TestV010Pipeline** (mocked LLM, marked default-suite): runs
  the full pipeline with pre-canned LLM responses for each
  role. Verifies plumbing: the planner prompt actually
  contains the selected skill's `## Planner` section, the
  worker dispatches the MCP task under the correct session
  scope, the reviewer sees `expect`, the messenger final
  output includes the MCP result. Deterministic, fast (< 5s).
- **TestV010PipelineLive** (real LLM, marked `llm_live +
  functional`): one realistic prompt; asserts semantic
  quality: skill actually gets selected by the briefer, MCP
  call actually produces expected data. Slow, gated behind
  `--functional`.

Fixtures:
- `tests/functional/fixtures/skills/python-debug/` — minimal
  skill with `## Planner` and `## Worker` sections +
  `activation_hints: {applies_to: ["python"]}`
- `tests/functional/fixtures/mcp/echo-server/` — tiny stdio
  MCP server (Python, ~50 LOC using the `mcp` SDK) that
  exposes an `echo(message)` tool returning the input. Used
  as a deterministic target for MCP calls.

**Tasks**:
- [x] Fixture skill directory with SKILL.md + matching
      activation_hints.
- [x] Fixture echo MCP server.
- [x] `TestV010Pipeline` with mocked LLM responses per role.
- [ ] `TestV010PipelineLive` with real LLM, gated. *(Deferred:
      landed the mocked variant end-to-end; live variant gated
      behind `llm_live` is tracked as a follow-up below.)*
- [x] Pipeline assertions: briefer `skills` selection, planner
      task types, worker MCP scope, reviewer `expect` visibility,
      messenger output content. *(Successful MCP dispatch does
      not trigger the reviewer LLM — visibility is asserted via
      the persisted `expect` field on the task row, which is the
      input the reviewer consumes on a replay / failure path.)*
- [ ] **Chat-mediated install E2E** (covers M1514/M1516's
      deferred integration test). *(Deferred: tracked as a
      follow-up below.)*
- [x] **Session-scoped MCP E2E** (covers M1512's deferred
      integration test): two concurrent sessions both using an
      MCP server with `${session:workspace}` tokens get
      independent subprocess workspaces.
- [x] Wire into `./utils/run_tests.sh a` default suite for
      the mocked variant. *(Existing `run_functional` already
      picks up the new file via the `functional` marker; no
      runner edit required.)*
- [x] Commit & push.

**Bugs surfaced and fixed by this milestone**:

The full-pipeline test revealed two pre-existing wiring bugs that
no prior test had caught:

1. `_persist_plan_tasks` in `kiso/worker/loop.py` dropped the
   `server` and `method` fields when persisting MCP tasks — so
   even a well-formed MCP plan failed at dispatch with "MCP
   task missing server name".
2. `_execute_plan` in the same file never accepted `mcp_manager`
   from its caller `_run_planning_loop`, so `_PlanCtx.mcp_manager`
   was always `None` and every MCP task failed with "MCP manager
   not configured on this worker".

Both are fixed in the same change set.

The functional conftest's `_KISO_DIR_MODULES` list was also
missing `kiso.skill_loader` and `kiso.mcp.manager`, so fixture
skills and per-session MCP workspaces resolved against the host
`~/.kiso` instead of the test's temp dir. Added.

**Done when**:
- `pytest tests/functional/test_v010_full_pipeline.py -q
  --functional` runs the mocked-LLM variant end-to-end and
  passes (currently 2 tests).
- The fixtures are small enough (skill ≈ 430 bytes, echo
  server ≈ 3 KB) to serve as documentation-by-example for
  contributors.

**Follow-up**: the `TestV010PipelineLive` + chat-mediated
install E2E sub-tests are deferred to a successor milestone
(M1531b, to be added when approved). The core mocked variant
is the v0.10-regression guard that had to ship first.

---

### M1532 — Security audit (trust, scripts, env, OAuth, webhooks, injection) ✅

**Why.** v0.10 introduces new attack surface: skill `scripts/`
execution, MCP server subprocess spawn with session-scoped env,
trust tier bypass, URL install flows, session ID injection into
subprocess args. Pre-release is the right moment to review
these structurally, not after someone files a CVE.

**Approach.** Structured audit of ten specific concerns, each
producing either (a) a verified-safe confirmation with a test
that encodes the invariant, or (b) a concrete fix landed in
this milestone.

**Concerns to audit**:

1. **Trust tier bypass** — can a URL like
   `github.com/anthropic-fake/malicious` match a Tier 1 prefix
   `github.com/anthropic/skills/*`? Glob matching must be
   strict (anchored to segment boundaries, not substring).
2. **Skill `scripts/` execution** — install-time warning
   (M1515) exists; verify planner cannot auto-`Bash(scripts/*)`
   without a fresh user-approval gate (today's exec approval
   flow covers new commands, but skill-bundled scripts must
   not be silently trusted because the skill itself was
   marked trusted — trust is for the skill's instructions, not
   its code).
3. **MCP env isolation** — session A's OAuth token in client
   A's env must not reach session B's client B, even when both
   target the same server. Pool key `(server, scope, uid)` is
   the mechanism (M1512); test explicitly.
4. **OAuth token storage** — `~/.kiso/mcp/credentials/` must be
   mode `0700`; individual `<name>.json` files mode `0600`. No
   symlink-follow when reading (open with `O_NOFOLLOW` on
   Linux, or stat+compare-inode workaround).
5. **Webhook replay attacks** — HMAC alone doesn't prevent
   replay. Require timestamp in signed payload and reject
   signatures > 5 minutes old. Document the contract.
6. **`${session:*}` substitution injection** — a session_id
   that contains shell metacharacters (default format
   `hostname@user` is user-controllable). Substitution happens
   for subprocess args (lists, not shell strings) so exec
   semantics are safe, but audit every substitution site to
   confirm no shell/fmt-string path.
7. **Skill path traversal** — `kiso skill add ../../evil`
   rejected? Install-from-URL resolver must reject destination
   paths that escape `~/.kiso/skills/`.
8. **Config env expansion** — `${env:FOO}` with
   `FOO="; rm -rf /"` — safe because expansion is string
   substitution, no shell. Verified, no change.
9. **Exec task sandbox escape** — already hardened (M1295
   reap, safety rules, deny-list). Re-run existing tests
   against the v0.10 refactor to confirm no regression.
10. **Prompt injection defense** — paraphraser still wraps
    untrusted messages after v0.10 refactor. The messenger's
    new skill-sections pathway (M1520) must not expose
    untrusted skill content as trusted if the skill came from
    an untrusted source (via M1515 risk factors).
11. **Chat-mediated install — trust surface in the proposal**
    — when the planner proposes `needs_install` via chat (the
    flow covered by M1516 for users without shell access to
    the kiso host), the trust gate of M1515 cannot pop an
    interactive prompt on the daemon host; the user's
    "yes/no" arrives as a chat reply. The planner prompt
    (`skills_and_mcp` module) must therefore surface, in the
    proposal msg, (a) the resolved source key, (b) whether
    the source is Tier 1 / custom-trusted / untrusted, and
    (c) the risk factors that `kiso skill install` would
    detect at install time. Without this, a remote user sees
    only "I will install X" and the trust decision collapses
    to a name check. Audit + reinforce the prompt accordingly.

**Tasks**:
- [x] For each concern above, either:
      (a) write a regression test that encodes the
      invariant (prefix glob strictness, token file mode,
      symlink rejection, HMAC timestamp, etc.);
      (b) land the fix inline in this milestone (timestamp
      in HMAC, `O_NOFOLLOW`, strict glob matching).
- [x] Rewrite `docs/security.md` covering the final v0.10
      security model (already scheduled in M1523, coordinate).
- [x] Run `./utils/run_tests.sh a` — full suite green.
- [x] Add a `tests/security/` directory with the new
      regression tests as a standalone test level.
- [x] Commit & push.

**Done when**:
- Every one of the ten concerns has either a passing
  regression test or a merged fix.
- `docs/security.md` describes the v0.10 security posture
  (trust tiers, skill script model, MCP session isolation,
  OAuth storage, webhook auth).
- `tests/security/` exists with the new regressions.

---

### M1533 — `install.sh` + README rewrite for single-key UX

**Why.** The landing page for kiso is `github.com/kiso-run/core`
and the first experience is `install.sh`. Today both describe
the wrapper/recipe world. Post-v0.10 they must reflect the
single-key + skills + MCP reality, and the first-run path must
actually work without friction.

Target first-run:
```
$ export OPENROUTER_API_KEY=sk-or-...
$ curl -fsSL https://github.com/kiso-run/core/raw/main/install.sh | sh
$ kiso doctor                    # all green
$ kiso msg "list files in the workspace"
[response using filesystem MCP]
```

**Approach.**

`install.sh` (audit and rewrite):
- Check `OPENROUTER_API_KEY` is set (new name, no fallback to
  `KISO_LLM_API_KEY` per M1521); fail fast with a clear
  message if absent
- Install `uv` if missing (the installer already does this;
  verify post-v0.10 flow)
- Install Node + `npx` if missing (required for MCP preset —
  `@modelcontextprotocol/*` servers)
- `uv tool install` kiso itself from the current git tag
- Run `kiso init --preset default` to seed the config + preset
- Print next steps: `kiso doctor`, `kiso msg "hello"`

`README.md` (full rewrite):
- Headline: "kiso — a generalist agent built on Agent Skills +
  MCP"
- One-screen architecture diagram (skills, MCP, planner/worker)
- Quickstart (3 lines above)
- "What makes kiso different" short section
- Links to `docs/index.md` (M1523) as the entry point
- Badge row: CI status, Python versions, license

`docs/tutorial.md` (new, or integrate into docs/index):
- First custom skill: write a SKILL.md, run it
- First custom MCP install from URL
- Walkthrough of a multi-task plan

**Tasks**:
- [ ] Audit current `install.sh` — list everything it does,
      mark what's still relevant post-v0.10.
- [ ] Rewrite `install.sh` for the new first-run path.
- [ ] Rewrite `README.md`.
- [ ] Write `docs/tutorial.md` (or similar entry-point doc).
- [ ] Bash regression test: `install.sh` on a clean VM (or
      docker image) produces a working install.
- [ ] Commit & push.

**Done when**:
- A user with only `OPENROUTER_API_KEY` set can follow the
  three-line quickstart and get a working kiso with all
  `kiso doctor` checks green.
- `README.md` reflects the v0.10 architecture (no wrappers,
  no recipes, skills + MCP + preset).
- A short tutorial walks through creating one skill and
  installing one MCP from URL.

## Phase 12 — Quality of life (M1535-M1537)

### M1535 — Error surface enrichment

**Why.** Today several failure modes surface generic messages
that frustrate debugging. `MCPTransportError` is opaque; skill
non-selection is invisible; install failures lack remediation.
Pre-launch polish means failures tell users what broke and how
to fix it.

**Approach.** Enrich the error context at 5-10 specific call
sites:

1. **MCP call failure** (`kiso/worker/mcp.py:131-138`):
   include server name, last 20 lines of
   `~/.kiso/mcp/<name>.err.log`, "Try: `kiso mcp test <name>`".
2. **MCP server spawn failure** (`kiso/mcp/stdio.py`):
   include the exact command that failed + any stderr captured.
3. **Skill activation miss** (new in M1538 pre-filter):
   when `KISO_DEBUG=1`, log "skill <name> filtered:
   applies_to=[...] no match in message; excludes=[...]".
4. **`kiso skill install` failure** (`cli/skill.py`): HTTP
   status, URL tried, "Did you mean `--from-url`?" if the user
   typed a bare identifier.
5. **`kiso mcp install` failure** (`cli/mcp.py`): existing
   resolver errors are okay but fail-paths for git clone /
   uvx install need clearer remediation.
6. **Config parse error**: surface `line:col` from tomllib +
   the offending key name.
7. **LLM timeout** (`kiso/brain/common.py`): tell user which
   role, which model, and that the fallback model (M1232) is
   being tried or has also failed.
8. **Trust rejection** (M1515): explain exactly what prefix
   was expected, why the install source doesn't match, and how
   to add a custom trust prefix.

**Tasks**:
- [ ] For each of the 8 sites: identify the current error
      path, enrich the message with structured detail +
      remediation hint.
- [ ] Unit test each enriched error: injected failure produces
      the expected enriched message.
- [ ] Commit & push.

**Done when**:
- Each of the 8 failure modes produces a user-facing message
  that names the specific thing that broke and a concrete
  next step.
- Unit tests verify the enriched messages on each path.

---

### M1536 — `kiso session export/import`

**Why.** Users accumulate meaningful state in a session:
messages, facts, plan outputs, uploads, pub files. Today this
state is tied to a machine. Exporting a session lets users
archive completed work, move between machines, share debugging
contexts, or hand off a session to a teammate. Import
round-trips the same.

**Approach.**

- `kiso session export <session_id> [--output <file>]` —
  packages `~/.kiso/sessions/<id>/` (workspace, pub, uploads)
  plus SQLite rows for that session (messages, facts, plans,
  tasks, usage) into a single `.tar.gz`. Default output
  filename: `<session_id>-<yyyymmdd>.kiso.tar.gz`.
- `kiso session import <file> [--as <new_session_id>]` —
  restores. If `--as` omitted, uses the original session_id
  (fails if already taken).

Archive format:
- `manifest.json` — kiso version, export timestamp, original
  session_id, row counts
- `messages.jsonl`, `facts.jsonl`, `plans.jsonl`, `tasks.jsonl`,
  `usage.jsonl` — DB rows as one JSON object per line
- `workspace/` — directory tree from `~/.kiso/sessions/<id>/`

**Tasks**:
- [ ] `cli/session.py` with `export` + `import` subcommands
      (or extend existing session subcommand group).
- [ ] DB row export helpers keyed by session_id (one function
      per table).
- [ ] `tarfile` build with deterministic ordering (reproducible
      archives).
- [ ] Import with schema-version check: refuse to import an
      archive from a future kiso version.
- [ ] Unit tests: round-trip on a fixture session, assert
      parity of rows and files.
- [ ] Integration test: export → delete session → import →
      conversation resumes correctly.
- [ ] Update `docs/cli.md` + `docs/architecture.md`.
- [ ] Commit & push.

**Done when**:
- `kiso session export <id>` produces a `.tar.gz` capturing
  the full session state.
- `kiso session import <file>` restores it; subsequent
  messages behave as if the session continued.
- Round-trip on fixture data is byte-identical for the
  workspace files and row-count-identical for DB data.

---

### M1537 — OpenRouter cost tracking + `kiso stats --costs`

**Why.** OpenRouter returns `usage.prompt_tokens`,
`completion_tokens`, and (when available) `total_cost` in every
response. Kiso drops this data today. Users running on their
own dime or small teams deploying kiso on a shared account want
visibility into where LLM spend goes — planner vs messenger,
which session, which model. Pre-launch cost observability is
standard for any LLM-heavy tool.

**Approach.**

New SQLite table `llm_usage`:

```sql
CREATE TABLE llm_usage (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  role TEXT NOT NULL,         -- briefer, planner, worker, ...
  model TEXT NOT NULL,        -- e.g., deepseek/deepseek-v3.2
  prompt_tokens INTEGER NOT NULL,
  completion_tokens INTEGER NOT NULL,
  cost_usd REAL,              -- from OpenRouter response, or estimated
  ts TEXT NOT NULL            -- ISO-8601 UTC
);
CREATE INDEX idx_usage_session_ts ON llm_usage(session_id, ts);
CREATE INDEX idx_usage_role_ts ON llm_usage(role, ts);
```

Hook into `kiso/brain/common.py::call_llm` after each response:
extract usage, resolve cost (use response `total_cost` when
present; fall back to a small pricing table keyed by model ID +
token counts for common OpenRouter models; log missing pricing
entries at debug level — never fail the call on missing cost).

CLI: `kiso stats --costs [--since 7d] [--group-by role|model|session]`
renders a Rich table. Aggregation SQL:

```
SELECT role, SUM(prompt_tokens), SUM(completion_tokens),
  SUM(cost_usd), COUNT(*)
FROM llm_usage WHERE ts > ? GROUP BY role ORDER BY
  SUM(cost_usd) DESC;
```

`--since` supports ISO durations (`1d`, `7d`, `30d`) and dates
(`2026-04-01`).

**Tasks**:
- [ ] Schema migration: add `llm_usage` table.
- [ ] `kiso/store/usage.py` with `record_usage()` and
      `query_usage(since, group_by)` helpers.
- [ ] Hook into `call_llm`: extract usage from response,
      resolve cost, call `record_usage`.
- [ ] Pricing table for common OpenRouter models (PRICES
      dict in `kiso/brain/pricing.py`, covering at least the
      default MODEL_DEFAULTS).
- [ ] `kiso stats --costs` subcommand wiring in `cli/stats.py`.
- [ ] Unit tests: cost resolver, aggregation queries, CLI
      rendering.
- [ ] Integration test: real LLM call records usage row
      correctly (marked `llm_live`).
- [ ] Update `docs/config.md` + `docs/cli.md`.
- [ ] Commit & push.

**Done when**:
- Every LLM call records a row in `llm_usage`.
- `kiso stats --costs --since 7d` shows per-role totals for
  the last 7 days.
- Cost resolution works for all MODEL_DEFAULTS entries (via
  response `total_cost` or the fallback pricing table).
- Missing pricing entries log at debug without breaking calls.

---

# Track B — `kiso-run/*-mcp` repos (separate repos)

Five standalone MCP server repos under the `kiso-run` GitHub
org. They are the upstream for the five Tier-2 entries in the
default preset. Work in this track happens *outside* this repo —
these milestones track scope but the actual code lives elsewhere.

**Distribution**: each repo is consumed via `uvx --from
git+https://github.com/kiso-run/<name>-mcp@<tag> kiso-<name>-mcp`.
Release stability is git tags. **No PyPI publishing.** The
Python package identifier inside each `pyproject.toml` is
`kiso-<name>-mcp` — used by `uvx --from ... <pkg>` to locate
the entry point, never uploaded anywhere.

## kiso-run repo disposition table (v0.10)

Complete actionable mapping of every `kiso-run/*` repo existing
today to its v0.10 disposition. Verified against the org
contents as of the v0.10 design pass.

### Naming convention (locked)

- **GitHub repo**: `<capability>-mcp` — the `kiso-run` org prefix
  is already in the URL, so the repo name stays short.
- **Python package name** (inside each repo's `pyproject.toml`,
  used as `uvx --from git+... <name>` target): `kiso-<capability>-mcp`
  — a prefixed identifier for entry-point disambiguation. **NOT
  published to PyPI.**
- **Install command** in user configs:
  `uvx --from git+https://github.com/kiso-run/<capability>-mcp@<tag> kiso-<capability>-mcp`.
- **Release stability**: tags on GitHub (`v0.1.0`, `v0.2.0`, ...).
  `uv` caches the git clone; users pin to a tag in their
  `mcp.json` entries to get reproducible behavior.

### Repos DELETED

| Repo | Why |
|---|---|
| `kiso-run/wrapper-browser` (deleted) | Superseded by `@playwright/mcp` (Tier 1, Microsoft-maintained) in the default preset. UX notes carried into `docs/recommended-mcps.md`. |

### Repos RENAMED (wrapper → mcp) ✅ done manually

| Old repo name | New repo name | Python package name | Milestone |
|---|---|---|---|
| `kiso-run/wrapper-aider` | `kiso-run/aider-mcp` | `kiso-aider-mcp` | M1507 |
| `kiso-run/wrapper-transcriber` | `kiso-run/transcriber-mcp` | `kiso-transcriber-mcp` | M1509 |
| `kiso-run/wrapper-ocr` | `kiso-run/ocr-mcp` | `kiso-ocr-mcp` | M1510 |
| `kiso-run/wrapper-docreader` | `kiso-run/docreader-mcp` | `kiso-docreader-mcp` | M1511 |

Local clones at `../plugins/` were renamed in-place and git
remotes updated during v0.10 prep.

The rewrite (Track B M1507/M1509/M1510/M1511) is a from-scratch
MCP server using the official `mcp` Python SDK, with the old
wrapper's execution logic as reference only. The old `run.py`
/ `kiso.toml` / `deps.sh` / `usage_guide.md` files are deleted
during the rewrite; git history is preserved for provenance.

### Repos to KEEP AS-IS (or lightly touch)

| Repo | Status |
|---|---|
| `kiso-run/core` | This repo. All Track A work lands here. |
| `kiso-run/default-preset` | **Kept as-is.** The external preset ecosystem (separate from the in-core default MCP config shipped by M1503) remains a first-class kiso-run concern. How M1503's `kiso/presets/default.mcp.json` relates to this external repo is a user-level decision; both coexist in v0.10. (Renamed from `preset-default` to align with the subject-first convention.) |
| `kiso-run/discord-connector` | Kept. Subject to M1525's connector-plugin retirement: the connector becomes a standalone package launched via `uvx` (or equivalent), registered in `config.toml` under `[connectors.discord]`. Rename + repackaging tracked as a follow-up inside that repo; not v0.10-blocking for kiso/core. (Renamed from `connector-discord` to align with the subject-first convention.) |

### Repos to CREATE (new)

| New repo | Python package name | Milestone |
|---|---|---|
| `kiso-run/search-mcp` | `kiso-search-mcp` | M1508 |

`search-mcp` is new — not a wrapper port. It replaces the
built-in `searcher` LLM role killed in M1504. Internally calls
Perplexity Sonar via OpenRouter (no Kiso-owned refinement
prompt; Sonar handles freeform queries).

---

Every existing `kiso-run/*` repo has an explicit disposition
above. Zero legacy wrappers survive v0.10 release as supported
outputs.

**Track B ships FIRST** (phase α of the execution order), before
any Track A milestone that affects wrapper visibility lands. The
no-gap invariant — zero user-visible capability gap at any commit
— requires that every wrapper-replaceable capability be
available as an MCP server published on GitHub **before** M1502
drops wrappers from the briefer schema.

See the **Execution order (no-gap invariant)** section near the
top of this document for the full sequencing rationale. The short
version: Track B (M1507-M1511) → M1503 (wire into preset) →
M1502 (break wrapper visibility, safe now because MCP path exists
for every capability).

A previous note claimed "Track B does not block any Track A
milestone". That is only true at the *build* level (the sibling
repos don't import from `kiso/core`), but it is **not** true at
the **continuity** level: breaking wrapper visibility before Track
B lands would create a gap. The updated invariant treats Track B
as a hard prerequisite for γ onward.

## M1507 — `kiso-run/aider-mcp` ✅

**Status (2026-04-19)**: code rewrite landed at commit
[`2d4909b`](https://github.com/kiso-run/aider-mcp/commit/2d4909b).
33 unit + 1 live test green (live exercises a real aider ↔ OpenRouter
round-trip against a fixture repo). Tag
[`v0.1.0`](https://github.com/kiso-run/aider-mcp/releases/tag/v0.1.0)
cut; `uvx --from git+...@v0.1.0 kiso-aider-mcp` resolves and the
module imports cleanly against a clean cache.

**Why.** The kiso `wrapper-aider` repo
(`github.com/kiso-run/wrapper-aider`) provides codegen via
aider with OpenRouter routing and `MODEL_DEFAULTS`-driven model
selection. Public MCP ecosystem has no aider-equivalent server
maintained by a trusted upstream. We fill the gap ourselves.

**Approach.** Renamed repo `kiso-run/aider-mcp`. Python project
using the official `mcp` Python SDK via `FastMCP`. Two tools:
`aider_codegen(prompt, editable_files, readonly_files,
model?, mode?)` returning `{success, diff, output, stderr}`, and
`doctor()` returning `{healthy, issues, version}`. Internally
shells out to `aider` with `--no-auto-commits` and captures the
resulting `git diff`. Honors only `OPENROUTER_API_KEY` from env
(single-key invariant). `pyproject.toml` declares package name
`kiso-aider-mcp` with an entry point; consumed via
`uvx --from git+https://github.com/kiso-run/aider-mcp@v0.1.0 kiso-aider-mcp`.

**Tasks (in `kiso-run/aider-mcp` repo)**:
- [x] Strip old wrapper files (`run.py`, `kiso.toml`,
      `deps.sh`, `config.example.toml`, `validator.py`); keep
      `.git/` history, LICENSE, README, DEVPLAN.md.
- [x] Rewrite: new `pyproject.toml` with `name =
      "kiso-aider-mcp"` and a console script entry point;
      Python MCP SDK skeleton (`src/kiso_aider_mcp/`).
- [x] Port `wrapper-aider` execution logic into `aider_codegen`
      MCP tool, including OpenRouter affordable-cap retry.
- [ ] Tool description uses `x-kiso-consumes: ["code"]`
      extension (deferred per M1519 — custom JSON-schema
      extensions require MCP server-side support not yet in the
      FastMCP surface; the human-readable tool description
      already mentions "code editing").
- [x] OpenRouter env wiring (`OPENROUTER_API_KEY` only).
- [x] Add `doctor` tool for dependency / credential health.
- [x] Tests: 25 unit (mock subprocess) + 8 MCP surface + 1
      live (real aider against a tiny fixture repo, gated on
      `OPENROUTER_API_KEY`).
- [ ] CI: lint, test, on-tag release (no PyPI upload; the tag
      itself is the release). **Deferred** — requires GitHub
      Actions config; not blocking the no-gap invariant.
- [ ] Cut first tag `v0.1.0` on GitHub *(user action)*.
- [ ] Sanity-check from `kiso/core`: a bare `mcp.json` with
      `uvx --from git+https://github.com/kiso-run/aider-mcp@v0.1.0 kiso-aider-mcp`
      connects and exposes the tool *(runs after tag exists)*.
- [x] Commit & push (`2d4909b` on main).

**Done when**:
- `uvx --from git+https://github.com/kiso-run/aider-mcp@v0.1.0 kiso-aider-mcp`
  starts a working MCP server.
- `aider_codegen` invoked from kiso produces a diff against a
  small test repo using OpenRouter as the LLM provider.
  ✅ Verified locally via the live test (`tests/test_live.py`).
- The repo has tag `v0.1.0` with a GitHub release entry.

---

## M1508 — `kiso-run/search-mcp`

**Why.** v0.10 removes the built-in `search` task type — web
search must now come through MCP like any other capability.
Existing MCP search options do not fit the default preset:

- **Official Perplexity MCP** — requires a separate
  `PERPLEXITY_API_KEY` (violates single-key philosophy).
- **Community OpenRouter search MCPs** — single-developer
  projects (violates trust rule for default preset).
- **Brave Search MCP** — requires `BRAVE_API_KEY`.

A kiso-maintained search MCP that routes through OpenRouter +
Perplexity Sonar is the only option that satisfies both the
trust rule and the single-key constraint.

**Approach.** New repo `kiso-run/search-mcp`. Python + official
`mcp` SDK. Single tool: `web_search(query, max_results?,
search_focus?)` returning `{results: [{title, url, snippet}],
summary: str, citations: [{url, text}]}`. Internally calls
OpenRouter with `perplexity/sonar-pro` directly — Sonar
handles freeform queries well and we do not port
`kiso/roles/searcher.md` over; the searcher role is killed
outright in M1504. `pyproject.toml` declares package name
`kiso-search-mcp`; consumed via `uvx --from
git+https://github.com/kiso-run/search-mcp@v0.1.0 kiso-search-mcp`.

**Tasks (in `kiso-run/search-mcp` repo)**:
- [ ] Repo bootstrap (LICENSE, README, `pyproject.toml` with
      `name = "kiso-search-mcp"` + entry point, CI).
- [ ] Implement `web_search` MCP tool with Sonar via OpenRouter
      directly.
- [ ] OpenRouter env wiring.
- [ ] Add `doctor` tool for credential health.
- [ ] Tests: unit (mocked OpenRouter response), integration
      (real Sonar query).
- [ ] CI: lint, test, on-tag release.
- [ ] Cut first tag `v0.1.0`.
- [ ] Sanity-check from `kiso/core`.
- [ ] Commit & push (mark M1508).

**Done when**:
- `uvx --from git+https://github.com/kiso-run/search-mcp@v0.1.0 kiso-search-mcp`
  starts a working MCP server.
- `web_search` returns structured results with citations from a
  real query.
- Server uses only `OPENROUTER_API_KEY`.
- The repo has tag `v0.1.0`.

---

## M1509 — `kiso-run/transcriber-mcp`

**Why.** The kiso `wrapper-transcriber` repo provides
audio→text transcription via Gemini multimodal. No
trusted-upstream MCP server exists for this capability. We fill
the gap ourselves.

**Approach.** New repo `kiso-run/transcriber-mcp`. Python +
official `mcp` SDK. Single tool: `transcribe_audio(file_path,
language?)` returning `{text: str, segments?: list}`. Internally
calls a Gemini multimodal model **via OpenRouter**. Tool
description includes `x-kiso-consumes: ["audio"]`. Ships on
`pyproject.toml` declares package name `kiso-transcriber-mcp`;
consumed via `uvx --from
git+https://github.com/kiso-run/transcriber-mcp@v0.1.0 kiso-transcriber-mcp`.

**Tasks (in `kiso-run/transcriber-mcp` repo)**:
- [ ] Strip old wrapper files; rewrite as MCP server.
- [ ] Port execution logic to MCP tool, switching backend to
      OpenRouter-wrapped Gemini.
- [ ] OpenRouter env wiring.
- [ ] `x-kiso-consumes: ["audio"]` in tool description.
- [ ] Add `doctor` tool.
- [ ] Tests: unit (mocked) + integration (real transcription
      against a tiny fixture audio file).
- [ ] CI + on-tag release.
- [ ] Cut first tag `v0.1.0`.
- [ ] Sanity-check from `kiso/core`.
- [ ] Commit & push (mark M1509).

**Done when**:
- `uvx --from git+https://github.com/kiso-run/transcriber-mcp@v0.1.0 kiso-transcriber-mcp`
  starts a working MCP server.
- `transcribe_audio` returns text from a real audio fixture.
- Server uses only `OPENROUTER_API_KEY`.

---

## M1510 — `kiso-run/ocr-mcp`

**Why.** The kiso `wrapper-ocr` repo provides image→text OCR
via Gemini vision. No trusted-upstream MCP server exists for
this capability. We fill the gap ourselves.

**Approach.** New repo `kiso-run/ocr-mcp`. Python + official
`mcp` SDK. Single tool: `extract_text_from_image(file_path,
language?)` returning `{text: str}`. Internally calls a Gemini
vision model **via OpenRouter**. Tool description includes
`x-kiso-consumes: ["image"]`. `pyproject.toml` declares package
`kiso-ocr-mcp`; consumed via `uvx --from
git+https://github.com/kiso-run/ocr-mcp@v0.1.0 kiso-ocr-mcp`.

**Tasks (in `kiso-run/ocr-mcp` repo)**:
- [ ] Strip old wrapper files; rewrite as MCP server.
- [ ] Port execution logic to MCP tool, switching backend to
      OpenRouter-wrapped Gemini.
- [ ] OpenRouter env wiring.
- [ ] `x-kiso-consumes: ["image"]` in tool description.
- [ ] Add `doctor` tool.
- [ ] Tests: unit + integration with a tiny fixture image.
- [ ] CI + on-tag release.
- [ ] Cut first tag `v0.1.0`.
- [ ] Sanity-check from `kiso/core`.
- [ ] Commit & push (mark M1510).

**Done when**:
- `uvx --from git+https://github.com/kiso-run/ocr-mcp@v0.1.0 kiso-ocr-mcp`
  starts a working MCP server.
- `extract_text_from_image` returns text from a real image
  fixture.
- Server uses only `OPENROUTER_API_KEY`.

---

## M1511 — `kiso-run/docreader-mcp`

**Why.** The kiso `wrapper-docreader` repo provides local
document text extraction (PDF/DOCX/XLSX/CSV/plaintext) via
`pypdf`, `python-docx`, `openpyxl`, stdlib. Trusted-upstream
MCP alternatives exist but all are cloud-API-based and require
paid keys for non-trivial usage. The kiso wrapper is the only
fully-local solution in this space — porting it to MCP
preserves that property.

**Approach.** New repo `kiso-run/docreader-mcp`. Python +
official `mcp` SDK. Three tools (matching the current
wrapper's three actions):
- `read_document(file_path, page_range?, max_chars?)` →
  `{text: str, format: str, truncated: bool}`
- `document_info(file_path)` → `{format, pages?, sheets?, ...}`
- `list_uploads(dir_path)` → `{files: list[{name, size, format}]}`

Internally uses `pypdf`, `python-docx`, `openpyxl`, stdlib
`csv`. Output capped at 100k characters. No env vars, no API
keys. Tool descriptions include `x-kiso-consumes: ["document"]`.
`pyproject.toml` declares package `kiso-docreader-mcp`;
consumed via `uvx --from
git+https://github.com/kiso-run/docreader-mcp@v0.1.0 kiso-docreader-mcp`.

**Tasks (in `kiso-run/docreader-mcp` repo)**:
- [ ] Strip old wrapper files; rewrite as MCP server.
- [ ] Port execution logic to three MCP tools.
- [ ] `x-kiso-consumes: ["document"]` in relevant descriptions.
- [ ] Add `doctor` tool (passes with zero external binaries,
      zero network access).
- [ ] Tests: unit per format (PDF, DOCX, XLSX, CSV,
      plaintext).
- [ ] CI + on-tag release.
- [ ] Cut first tag `v0.1.0`.
- [ ] Sanity-check from `kiso/core`.
- [ ] Commit & push (mark M1511).

**Done when**:
- `uvx --from git+https://github.com/kiso-run/docreader-mcp@v0.1.0 kiso-docreader-mcp`
  starts a working MCP server.
- All three tools return correct output on a fixture set.
- Server runs with zero env vars and zero network access.

---

## Milestone Checklist

The list below groups milestones by conceptual phase (1 … 12) and by
track (A = kiso/core, B = sibling MCP repos). **Execution order is
different** — see "Execution order (no-gap invariant)" at the top
of this document. In particular Track B runs FIRST (phase α), then
M1503 (β), then M1502 and the rest of Track A (γ onward).

### Track A — kiso/core cleanup

#### Phase 1: Skill foundation
1. [x] **M1500** — `kiso/skill_loader.py` and standard skill discovery ✅
2. [x] **M1501** — Kiso Skill Profile module: role-scoped projection APIs (`kiso/skill_runtime.py`) ✅
3. [~] **M1540** — Thread skill role slices into planner/worker/reviewer prompt builders (was M1501 deferred) 🟡 builders + unit tests landed (`d145b25`); caller wiring + integration test deferred, see milestone body
4. [x] **M1541** — `kiso-migrate-recipes-to-skills` conversion script ✅ (docs rename deferred to M1523 docs sweep)

#### Phase 2: Briefer schema convergence
5. [x] **M1502** — Briefer JSON schema (drop wrappers/exclude_recipes, add skills/mcp_methods) + post-filter + `briefer.md` rewrite + test migration ✅
6. [x] **M1538** — `activation_hints` pre-filter (deterministic, pre-briefer) + `briefer_skill_filter_threshold` setting ✅
7. [x] **M1539** — Per-user MCP method role filtering (`[users.<name>.mcp]` config) ✅

#### Phase 3: Default preset and standards docs
8. [x] **M1503** — `default.mcp.json`, `kiso init --preset`, standards docs, trust CI, single-key CI ✅ (9 servers, 35 preset/init tests, CI-enforced trust + single-key rules)

#### Phase 4: Wrapper and search retirement
9. [x] **M1504** — Retire wrappers + search type from runtime ✅ (part 1 CLI retirement `2ac241f`; part 2a search/`searcher` retired; 2b recipes retired; 2c-i/ii/iii wrapper task type + subsystem retired; 2d docstring + registry.json empty, migration doc reverted as dead-weight pre-release)

#### Phase 4.5: Docs sweep — retired-system cleanup (promoted from Phase 9)
10. [x] **M1523a** — Docs sweep: retired-system cleanup ✅ (retired doc files deleted, `docs/extensibility.md` rewritten, `docs/cli.md` wrapper section removed, `docs/connectors.md`/`docs/docker.md`/`docs/safety.md`/`docs/testing.md`/`README.md` cascade, invariant test landed, 4112 passed)

#### Phase 5: Planner prompt cleanup
11. [ ] **M1505** — `planner.md` routing rewritten as `skills_and_mcp`; replan + web updated; skill install rule added

#### Phase 6: Skill CLI basics
11. [ ] **M1506** — `kiso skill list/info/add/remove`

#### Phase 7: Lifecycle completeness
12. [ ] **M1512** — Per-session MCP client pool + session context + env file integration
13. [ ] **M1513** — MCP method input schema validation + planner schema visibility
14. [ ] **M1514** — `kiso skill install --from-url` with full parity to MCP install
15. [ ] **M1515** — Trust model, safety gates, `kiso skill test`, recommended-skills docs
16. [ ] **M1516** — `needs_install` extended to skills
17. [ ] **M1517** — Per-user MCP method + skill role filtering
18. [ ] **M1518** — MCP catalog warm-up + cold-start handling
19. [ ] **M1519** — `x-kiso-consumes` extension, `mcp_recovery` module, rich descriptions

#### Phase 8: Messenger completion
20. [ ] **M1520** — Wire `## Messenger` skill sections into the messenger role

#### Phase 9: Final cleanup
21. [ ] **M1521** — `KISO_LLM_API_KEY` → `OPENROUTER_API_KEY` unification
22. [ ] **M1522** — Full registry concept removal
23. [ ] **M1523** — Docs sweep

#### Phase 10: Completeness & consolidation
24. [ ] **M1524** — Behavior injection into messenger role
25. [x] **M1525** — Retire connector plugin subsystem; supervisor-config model ✅
26. [x] **M1526** — MCP stdio sandbox_uid symmetry with exec ✅
27. [x] **M1527** — MCP Resources support (list, read, planner routing) ✅
28. [x] **M1528** — MCP Prompts support (list, get, injection mechanism) ✅
29. [x] **M1529** — MCP `sampling/createMessage` support ✅ (stdio only; http bidirectional dispatch deferred)

#### Phase 11: Launch readiness
30. [x] **M1530** — `kiso doctor` unified health-check command ✅
31. [~] **M1531** — Functional end-to-end v0.10 pipeline test 🟡 mocked-LLM core test + session-scoped MCP isolation landed; live-LLM variant and chat-mediated install E2E deferred to M1531b
32. [ ] **M1532** — Security audit (trust, scripts, env, OAuth, webhooks, injection)
33. [ ] **M1533** — `install.sh` + README rewrite for single-key UX

#### Phase 12: Quality of life
34. [ ] **M1535** — Error surface enrichment (MCP / skill / install)
35. [ ] **M1536** — `kiso session export/import`
36. [ ] **M1537** — OpenRouter cost tracking + `kiso stats --costs`

### Track B — `kiso-run/*-mcp` repos (all use `OPENROUTER_API_KEY` or no key, distributed via `uvx --from git+...@<tag>`, no PyPI)

37. [x] **M1507** — `kiso-run/aider-mcp` (pkg: `kiso-aider-mcp`) — `v0.1.0` released, 34 tests green, uvx resolve verified ✅
38. [x] **M1508** — `kiso-run/search-mcp` (pkg: `kiso-search-mcp`) — Sonar via OpenRouter — `v0.1.0` released, 18 tests green ✅
39. [x] **M1509** — `kiso-run/transcriber-mcp` (pkg: `kiso-transcriber-mcp`) — Gemini via OpenRouter — `v0.1.0` released, 25 tests green ✅
40. [x] **M1510** — `kiso-run/ocr-mcp` (pkg: `kiso-ocr-mcp`) — Gemini via OpenRouter — `v0.1.0` released, 24 tests green ✅
41. [x] **M1511** — `kiso-run/docreader-mcp` (pkg: `kiso-docreader-mcp`) — no key — `v0.1.0` released, 28 tests green ✅
