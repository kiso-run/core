# Development Plan — v0.2

In-progress milestones for kiso v0.2. Continues from v0.1 (all M1–M43 complete).

**Completed**: M44–M75, M84–M99, M100, M101, M102–M121

**Completed**: M122–M135 (post-mortem from guidance.studio session) ✅

**Pending**: M136–M146 (bugs from guidance.studio real-world sessions)

## Principles

- **Agile**: smallest testable increment first, then layer on
- **No dead code**: every line written is immediately reachable and testable
- **Fail loud**: missing config, broken provider, invalid input → clear error, never silent fallback
- **Tested**: every milestone adds tests. `uv run pytest` must pass before moving on.

---

## Milestone 44: Deferred items + polish

Collect all items explicitly deferred during v0.1 development, plus small UX fixes observed in production.

### 44a. worker — extract `search.py` from `loop.py`

**Problem**: M36 planned `kiso/worker/search.py` as a dedicated module for the search handler (analogous to `exec.py` and `skill.py`). The logic instead stayed in `loop.py`, breaking the package's symmetry. `__init__.py` exports nothing search-related.

**Fix**: extract the search handler from `loop.py` into `kiso/worker/search.py`, analogously to `exec.py` and `skill.py`. Update `__init__.py` and imports.

- [x] `kiso/worker/search.py`: new module with `_parse_search_args()` and `_search_task()` extracted from `loop.py`
- [x] `kiso/worker/loop.py`: removed inline args parsing and direct `run_searcher` call; uses `_search_task()`
- [x] `kiso/worker/__init__.py`: exports `_parse_search_args` and `_search_task`
- [x] `tests/test_worker.py`: updated patch targets from `kiso.worker.loop.run_searcher` to `kiso.worker.search.run_searcher` (17 occurrences)
- [x] `tests/test_search.py`: 33 isolated unit tests for `_parse_search_args` and `_search_task` (boundary max_results, type errors, malformed JSON, warning log, parameter propagation)

---

### 44b. CLI — loader on new line

**Problem**: the "thinking..." spinner appears inline with the last content line instead of on its own line. Visually confusing when the previous output ends without a trailing newline.

**Fix**: ensure the loader/spinner always starts on a fresh line.

- [x] `cli/__init__.py`: `_poll_status` gains `_at_col0: bool = True` parameter; emits `\n` before first frame when `_at_col0=False`
- [x] tests: 2 new cases verify `\n\n\r` (extra newline) vs `\n\r` (no extra newline)

---

### 44c. Fact poisoning — `save_learning` content filter

**Context**: during M21 the curator prompt was deemed sufficient to guard against manipulative learnings. Monitor in production whether this holds or whether pre-storage filtering is needed.

- [x] Added `_SENSITIVE_PATTERN` regex + filter in `save_learning()` (keywords: password/passwd/token, hex ≥32 chars); returns 0 + logs warning
- [x] 7 unit tests: keyword rejection (case-insensitive), hex threshold, benign acceptance, sentinel preservation

---

### 44d. Verbose mode — incremental LLM rendering

**Context**: `append_task_llm_call()` was added in M31 but the worker still batches all LLM call data at task end. Documented as known limitation in `docs/cli.md`.

**Fix**: call `append_task_llm_call()` after each individual LLM call (searcher, exec translator, reviewer, messenger) so verbose panels appear incrementally during task execution.

- [x] `kiso/worker/loop.py`: `_append_calls()` helper added; called after each LLM op (translator, reviewer, messenger, searcher) in all task branches and fast-path
- [x] `llm_calls=` removed from all `update_task_usage` call sites; `_KEEP_LLM_CALLS` sentinel preserves incrementally-appended column
- [x] `docs/cli.md`: removed "Known limitations — batch rendering" note
- [x] 5 tests: assert `_append_calls` call count per task type; verify `update_task_usage` called without explicit `llm_calls`

---

### 44e. Post-review hardening — M44b/M44c/M44d follow-up

High and medium severity issues identified during review of M44b+M44d.

#### High severity

- [x] `cli/__init__.py`: full docstring for `_poll_status()` explaining the `_at_col0` parameter
- [x] `kiso/store.py`: `save_learning()` — TypeError on `content=None`; return 0 on empty/whitespace string; updated docstring
- [x] `kiso/worker/loop.py`: `_append_calls()` — `try/except Exception` with log.warning; no worker crash on usage tracking errors
- [x] `kiso/worker/loop.py`: `_fast_path_chat()` — `_append_calls()` also called in the failure path (messenger exception)

#### Medium severity

- [x] `tests/test_store.py`: guard None (TypeError), empty string, whitespace, boundary hex 31 chars (accepted) vs 32 chars (rejected)
- [x] `tests/test_worker.py`: `_append_calls` handles exception, fast_path failure appends, success vs failure same call_count; retry scenario verifies 5 `_append_calls` (2 attempts × 2 + 1 msg)
- [x] `tests/test_cli.py`: `\n\n\r` appears exactly once — extra newline only on first frame, not subsequent frames

---

### 44f. Admin context — session labels on facts

**Context**: M43 implemented strict session scoping for regular users (Option B). Admin users already bypass the filter and receive all facts, but with no indication of which session each fact originated from, making cross-session facts indistinguishable in the planner context.

**Fix**: when building planner context for an admin caller, split facts into two priority tiers:
- **Primary** (`## Known Facts`): facts from the current session + global facts (session IS NULL). Shown without session label — these are the most relevant.
- **Background** (`## Context from Other Sessions`): facts from other sessions, annotated with `[session:<name>]`. Available as broader memory if needed.

Non-admin path is unchanged (single `## Known Facts` block, no labels).

- [x] `kiso/brain.py`: `build_planner_messages()` — admin path splits `facts` into `primary` and `other`; `_group_by_category()` helper renders both blocks; non-admin uses `primary=facts, other=[]`
- [x] `tests/test_brain.py`: `test_admin_facts_hierarchy` — current-session + global facts in `## Known Facts` (no label); other-session fact in `## Context from Other Sessions` with `[session:name]`; current-session fact never carries a label
- [x] `tests/test_brain.py`: `test_non_admin_facts_no_session_labels` — non-admin context has no `[session:` and no `## Context from Other Sessions`

---

## Milestone 45: Planner — plugin discovery via registry (not web search)

**Problem observed in production**: the user asks "I want to install the discord connector" and the planner emits `[search] kiso discord connector` (web search) instead of `[exec] curl <registry_url>`. The reviewer correctly rejects the result ("sa-mp discord connector", not kiso's), but the cycle is wasted.

**Root causes**:
1. The rule "Prefer search over exec curl/wget for web lookups" in the `search` task type had no explicit exception for the kiso registry.
2. The MANDATORY rules on registry-based discovery were buried at the end and didn't explicitly link the exception to the search rule.
3. `kiso` was not in `PROBE_BINARIES` → didn't appear in the "available binaries" list in System Environment.

**Fix**:

- [x] `kiso/roles/planner.md`: added explicit exception in the `search` task description: "NEVER use search to discover kiso plugins — use exec curl on the registry URL instead (see Plugin installation rule below)"
- [x] `kiso/roles/planner.md`: MANDATORY rules unified and rewritten as "Plugin installation (MANDATORY)": single block covering discovery (exec curl registry, never web search) + install + env requirements
- [x] `kiso/sysenv.py`: added `"kiso"` at the head of `PROBE_BINARIES` — the binary appears in "available binaries" if installed, making its availability explicit to the planner
- [x] `tests/test_brain.py`: `test_m45_plugin_install_uses_registry_not_search` — verifies the prompt contains "NEVER" + "registry" + "web search"
- [x] `tests/test_brain.py`: `test_m45_plugin_install_rule_is_mandatory` — verifies "Plugin installation (MANDATORY)" is in the prompt
- [x] `tests/test_sysenv.py`: `test_kiso_in_probe_list` — verifies `"kiso"` is in `PROBE_BINARIES`
- [x] `docs/flow.md`: updated sections "Facts" (was "global", now "session-scoped"), "Planner Context" (added admin example with `## Context from Other Sessions`)
- [x] `docs/security-risks.md`: updated two stale sections ("Facts are global" → M43 scoping + residual risk for global categories)
- [x] `docs/llm-roles.md`: updated context table ("Facts (global)" → "Facts (session-scoped; admin sees all)")

---

## Milestone 47: Planner/Reviewer — plan quality and evaluation correctness

Problems observed in production during the Discord installation. Three distinct bugs that chained together causing a needless replan loop until timeout.

---

### 47a. Planner — self-awareness and intent disambiguation

**Problem**: the planner doesn't know it's the brain of a specific system (Kiso). It behaves as a generic OS-level task planner. This causes two cascading bugs: (1) it doesn't distinguish between requests targeting the OS layer and those targeting the Kiso layer; (2) it has a bias toward execution even when intent is ambiguous.

**Root cause**: the prompt starts with "You are a task planner." — no identity, no Kiso context, no awareness of the two available layers.

**Agreed solution — two prompt interventions**:

**(1) Self-awareness** — add at the top of the prompt:
- Identity: the planner is Kiso's brain, not a generic planner
- Two-layer awareness: it has both OS commands and native Kiso primitives (skill, connector, env, memory) at its disposal; before planning an OS-level solution, check if a Kiso-native solution already exists. Scoped to solution choice, not as an absolute global preference.

**(2) Disambiguation bias** — invert the default on clarification:
- Current (permissive): "if unclear, ask" — the planner must justify asking
- New (restrictive): proceed only if both intent AND target are unambiguous; otherwise ask
- Single line, no enumerated cases; two-layer awareness provides the context that makes the rule applicable

- [x] `kiso/roles/planner.md`: added Kiso identity + two-layer awareness at the top of the prompt
- [x] `kiso/roles/planner.md`: clarification rule inverted — "proceed only if unambiguous, else ask"
- [x] `kiso/roles/planner.md`: guidance on task-scoped `expect`
- [x] `docs/llm-roles.md`: "Identity and Environment Awareness" section added to Planner
- [x] `docs/llm-roles.md`: `expect` field updated with note on task scope
- [x] `tests/test_brain.py`: `TestM47PlannerIdentityAndTwoLayer` — 5 tests on identity, two-layer, Kiso-native preference, clarification bias, expect scoping

---

### 47b. Reviewer — evaluate task `expect`, not plan goal

**Problem**: the reviewer uses the Plan Goal as success criterion instead of the individual task's `expect`. Concrete case: `apt-get install -f` with output "0 upgraded, 0 installed" (= no broken dependencies = success) was marked `replan` because "Discord is not yet fully functional" — which is the plan goal, not the task criterion.

**Root cause**: the reviewer prompt receives the `Plan Goal` as context but doesn't explicitly distinguish between "context" and "success criterion". The model tends to merge them.

**Agreed solution — two interventions**:

1. **Rename** the `Plan Goal` field → `Plan Context` in the message built by `build_reviewer_messages` in `brain.py`. The name changes the frame at a structural level: the model sees data consistent with the rule rather than data that contradicts it.

2. **Add a rule** to the reviewer prompt: "Plan Context is background only. The sole success criterion is the task's `expect`." Output "nothing to do" / "0 changes" for maintenance commands is valid success if consistent with `expect`.

- [x] `kiso/brain.py`: `build_reviewer_messages` — renamed `## Plan Goal` → `## Plan Context`
- [x] `kiso/roles/reviewer.md`: added rule "Plan Goal is background context only" + rule on "0 changes"
- [x] `docs/llm-roles.md`: context table updated ("Plan context (goal) — as background"); `goal` output field updated
- [x] `tests/test_brain.py`: `TestM47ReviewerPlanContext` — 4 tests on label, background rule, sole criterion, zero-changes

---

### 47c. Planner — `expect` scoped to task, not plan

**Problem**: the planner writes `expect` fields describing the plan's global goal instead of the expected output of the individual task. Example: for `apt-get install -f`, the planner wrote `"All dependencies resolved, Discord should be fully functional"` — which is the plan goal. This causes problem 47b.

**Root cause**: no instructions in the planner prompt on how to write task-scoped `expect`. The planner propagates the plan goal into each task's `expect` instead of describing what that specific task must produce/show.

**Note**: 47c is independent of 47b and necessary even after the reviewer fix. A correctly calibrated reviewer that ignores Plan Context still evaluates `expect` literally — if `expect` says "Discord should be fully functional", the reviewer is forced to mark `apt-get install -f` as failed even if that's wrong. The problem is upstream: the planner wrote the wrong `expect`.

**Agreed solution**: add explicit guidance to the planner prompt on how to write `expect`:
- describes the direct output of this specific task, not the plan goal
- must be falsifiable from the task output alone
- for maintenance/cleanup commands (apt-get install -f, git clean, etc.), "0 changes" is valid success and should be stated explicitly in `expect`

- [x] implemented together with 47a (`kiso/roles/planner.md` + test `test_planner_prompt_expect_scoping`)

---

### 47d. Worker (exec translator) — retry hint ignored

**Problem**: on retry, the worker ignores the hint in the Retry Context and re-translates the original task `detail` literally, producing the same failed command. In the Discord case, the hint said "use `apt install discord`" but the worker reproduced `apt-get install -f` because the task `detail` specified it.

**Root cause**: the worker prompt doesn't mention the Retry Context. The worker doesn't know that in case of retry, the hint should guide the alternative translation.

**Agreed solution**: add to the worker prompt that when a Retry Context with a hint is present, the hint takes priority over literal translation of `detail`. The `detail` remains context for understanding the task, but the hint guides the command to produce.

- [x] `kiso/roles/worker.md`: added rule on hint priority
- [x] `docs/llm-roles.md`: rule added in "Rules in the Default Prompt" for the exec translator
- [x] `tests/test_brain.py`: `TestM47WorkerHintPriority` — test on presence of rule in prompt

---

## Milestone 48: Prompt hygiene — cross-cutting optimizations to role prompts

Optimizations identified from systematic review of all role prompts. No new behavior: only coherence, redundancy reduction, and filling instruction gaps that the model currently resolves by inference (with variable results).

---

### 48a. Reviewer — "You receive" inconsistent with actual message

**Problem**: the prompt describes "You receive: The plan goal" but since M47 the message arrives labeled `## Plan Context`. The model sees a label different from what the prompt describes — structural inconsistency.

**Fix**: align the prompt description to `Plan Context`.

- [x] `kiso/roles/reviewer.md`: "You receive: The plan goal" → "The plan context"
- [x] `tests/test_brain.py`: `TestM48ReviewerPromptHygiene` — test_48a_you_receive_says_plan_context, test_48a_no_plan_goal_in_receive_block

---

### 48b. Reviewer — exit code not used in rules

**Problem**: the reviewer receives `## Command Status` (succeeded/FAILED exit code) but no rule says how to use it. The model interprets it autonomously. In ambiguous cases (output looks OK but exit code 1, or stderr with exit code 0) it can go the wrong way without explicit instructions.

**Fix**: add explicit rule: non-zero exit code is a strong signal of failure even if output looks partially OK; exit code 0 is not sufficient alone if output doesn't satisfy `expect`.

- [x] `kiso/roles/reviewer.md`: added rule "Exit code is a strong signal: non-zero = strong failure indicator; zero = necessary but not sufficient"
- [x] `tests/test_brain.py`: `TestM48ReviewerPromptHygiene` — test_48b_exit_code_rule_present, test_48b_nonzero_is_failure_signal, test_48b_zero_not_sufficient_alone

---

### 48c. Planner — `expect` and `detail` rules fragmented

**Problem**: two pairs of conceptually identical rules are split and separated in the prompt:
1. Line 21 (`expect non-null`) + line 28 (`expect task-scoped`) — same rule in two pieces
2. Line 26 (`detail self-contained`) + line 46 (`detail specific, include commands/paths`) — same instruction repeated; the second (more important) arrives last and gets less attention

**Fix**: merge each pair into a single rule, moved to a relevant position. Reduces total prompt text.

- [x] `kiso/roles/planner.md`: expect rule unified (non-null + task-scoped in one line); detail rule unified (self-contained + specific + commands/paths in one line); removed redundant line "exec task detail must be specific"
- [x] `tests/test_brain.py`: `TestM48PlannerMergedRules` — 6 tests on presence of unified rules and absence of duplicates

---

### 48d. Curator — no category in promote

**Problem**: the curator promotes facts as free text without indicating the category (project/user/tool/general). The information is lost and the consolidator must infer it. If the curator already distinguished the type of fact, subsequent categorization would be more reliable.

**Fix**: add `category` field to the curator's JSON output. `_apply_curator_result` uses the category to determine session scoping: only `"user"` is session-scoped, others are global. Bug fix: non-user facts must no longer be saved with `session=session`.

- [x] `kiso/roles/curator.md`: added category instruction for promote (project/user/tool/general)
- [x] `kiso/brain.py`: `CURATOR_SCHEMA` — added nullable `category` field with enum; added to `required`
- [x] `kiso/brain.py`: `validate_curator` — optional validation: if category != null, must be a valid value
- [x] `kiso/worker/loop.py`: `_apply_curator_result` — uses `ev.get("category") or "general"`; session=session only if category=="user", otherwise session=None
- [x] `tests/test_brain.py`: `TestM48CuratorCategoryField` — 11 tests on prompt, schema, validate_curator
- [x] `tests/test_worker.py`: `TestM48ApplyCuratorCategory` — 9 tests on session scoping per category

---

### 48e. Summarizer-facts — no tiebreaker for contradictions

**Problem**: the prompt says "remove contradictory items" but doesn't specify which to keep when two facts contradict each other. The model chooses arbitrarily.

**Fix**: add explicit resolution criterion: on contradiction, prefer the fact with higher confidence; if equal, prefer the more specific one.

- [x] `kiso/roles/summarizer-facts.md`: added tiebreaker rule (higher confidence wins; if equal, more specific wins)
- [x] `tests/test_brain.py`: `TestM48SummarizerFactsTiebreaker` — 3 tests on presence of tiebreaker rule

---

### 48f. Worker (exec translator) — no rule on sudo

**Problem**: no instructions on `sudo`. In non-root environments, the planner can write task details that imply elevated privileges, and the worker adds `sudo` autonomously or doesn't — non-deterministic behavior.

**Fix**: add rule: do not add `sudo` unless explicitly mentioned in the task detail or in the system environment.

- [x] `kiso/roles/worker.md`: added rule "Do NOT add sudo unless explicitly mentioned"
- [x] `tests/test_brain.py`: `TestM48WorkerNoSudo` — 3 tests on presence of no-sudo rule
- [x] `docs/llm-roles.md`: updated sections Reviewer (exit code rule), Exec Translator (no-sudo), Curator (schema with category, verdicts), Summarizer-facts (tiebreaker)

---

## Milestone 49: Versioning — version file + `kiso version` command

**Problem**: the project version was implicit (only in `pyproject.toml`). There was no canonical file to use as reference when writing the version in docs/devplan, nor a CLI command to print it.

**Fix**: single `kiso/_version.py` file as source of truth + `kiso version` command + `--version/-V` flag.

- [x] `kiso/_version.py`: `__version__ = "0.1.0"` — single source of truth for the version
- [x] `cli/__init__.py`: imports `__version__`; adds `-V/--version` flag (argparse built-in); adds `version` subcommand that prints `kiso {__version__}`; parser description includes the version
- [x] `tests/test_cli.py`: `TestVersionFile` (3 tests: file exists, semver format, cli re-export) and `TestVersionCommand` (5 tests: subcommand, print output, -V, --version, help includes version)

---

## Milestone 50: Multi-instance — named bots via Docker

**Context**: `kiso-host.sh` and `install.sh` already exist and work. The Docker-native architecture is the current deployment model (single instance, container `kiso`, hardcoded port 8333, data in `~/.kiso/`). M50 extends the wrapper to support multiple named instances on the same machine without touching the Python core.

**Principle**: each instance is a bot with a unique name. The core knows nothing about "instances" — it's a Docker image that sees only `/root/.kiso`. All management is in the host wrapper.

---

### Local layout

```
~/.kiso/
  instances.json                   # registry (see below)
  instances/
    jarvis/                        # mounted as /root/.kiso in kiso-jarvis
      config.toml
      .env
      kiso.db
      sessions/
      roles/
      connectors/
        discord/
          config.toml              # webhook_port = 9001 (port from jarvis's range)
    work/
      config.toml                  # separate config (different models, providers, users)
      connectors/
        discord/
          config.toml              # webhook_port = 9101 (port from work's range)
```

**`~/.kiso/instances.json`**:
```json
{
  "jarvis": { "server_port": 8333, "connector_port_base": 9000 },
  "work":   { "server_port": 8334, "connector_port_base": 9100 }
}
```

---

### Ports — strategy

**Server port**: auto-detect first free starting from 8333.

**Connector port base**: auto-detect first free multiple of 100 starting from 9000 (10-port range per instance: base+1...base+10). Docker exposes the entire range: `-p {base+1}-{base+10}:{base+1}-{base+10}`. Internal port = external port (no asymmetric NAT — the connector knows its external port without extra configuration).

When `kiso connector install NAME` is run via wrapper:
1. The wrapper runs the install inside the container (as now)
2. Assigns the next free port from the instance's range
3. Overwrites `webhook_port` in the just-installed connector's `config.toml`
4. Registers the port in `instances.json`: `connectors: { discord: 9001 }`

---

### Implicit instance resolution

Applied to all commands when `--instance` is not specified:
- 0 instances → error: `"No bot configured. Run: kiso instance create NAME"`
- 1 instance → used implicitly (zero overhead for single-bot users)
- 2+ instances → error: `"Multiple bots available. Specify: kiso --instance NAME"`

---

### Commands — new wrapper

**Instance management** (all in the bash wrapper, not the Python CLI):

```bash
kiso instance create NAME          # create dir, docker run, register in instances.json
kiso instance start [NAME]         # docker start kiso-{NAME}
kiso instance stop [NAME]          # docker stop kiso-{NAME}
kiso instance restart [NAME]       # docker restart kiso-{NAME}
kiso instance list                 # name / server port / connector base / status
kiso instance status [NAME]        # container state + health check
kiso instance logs [NAME] [-f]     # docker logs kiso-{NAME}
kiso instance shell [NAME]         # docker exec -it kiso-{NAME} bash
kiso instance explore [NAME] [session]   # shell in the session workspace
kiso instance remove [NAME] [--yes]      # docker rm + rm -rf instances/{NAME}/
```

**Existing commands** — unchanged, accept optional `--instance`:
```bash
kiso [--instance NAME]                   # REPL
kiso [--instance NAME] msg "..."
kiso [--instance NAME] env set KEY VAL
kiso [--instance NAME] sessions
kiso [--instance NAME] skill install X
kiso [--instance NAME] connector run discord
kiso [--instance NAME] reset session
```

**Commands removed** (absorbed into `kiso instance *`):
`kiso up`, `kiso down`, `kiso restart`, `kiso status`, `kiso health`, `kiso logs`, `kiso shell`, `kiso explore`

---

### Instance name validation

Applied wherever the user provides a name (install.sh + `kiso instance create`):

- Format: `^[a-z0-9][a-z0-9-]*$` (or `^[a-z0-9]$` for 1-char names), max 32 characters
- Rationale: compatible with DNS hostname syntax, Docker container names, directory paths
- Clear error if invalid: `"Instance name must be lowercase alphanumeric + hyphens, no leading/trailing hyphen"`

---

### `install.sh` — updated flow

1. Check prerequisites (docker, git) — unchanged
2. **Ask for bot name** (default: `kiso`) — validated with regex above
3. Ask username, bot_name, provider, API key, models — unchanged
4. Create `~/.kiso/instances/NAME/config.toml` and `.env` (instead of `~/.kiso/config.toml`)
5. Auto-detect server port and connector base
6. Build Docker image — unchanged
7. `docker run -d --name kiso-{NAME} -p {server}:8333 -p {cbase+1}-{cbase+10}:{cbase+1}-{cbase+10} -v ~/.kiso/instances/{NAME}:/root/.kiso kiso:latest`
8. Register in `~/.kiso/instances.json`
9. Install wrapper and completions — unchanged

Re-running (`./install.sh` on an existing installation) offers: install another instance or update an existing one.

---

### Dockerfile — CMD → direct uvicorn

```dockerfile
# Before (removed):
CMD ["uv", "run", "kiso", "serve"]

# After:
CMD ["uv", "run", "uvicorn", "kiso.server:app", "--host", "0.0.0.0", "--port", "8333"]
```

Eliminates the dependency on `kiso serve` at runtime. Allows removing `kiso serve` from the Python parser without breaking anything.

---

### Python core changes

- `cli/__init__.py`: removal of `kiso serve` from parser and `main()` (the server no longer starts via CLI)
- Nothing else changes in the core

---

### Files to update

| File | Type of change |
|---|---|
| `kiso-host.sh` | complete rewrite |
| `install.sh` | flow refactor (bot name + multi-instance) |
| `Dockerfile` | CMD → uvicorn |
| `docker-compose.yml` | removed or reduced to dev tool |
| `completions/kiso.bash` + `kiso.zsh` | added `instance *` |
| `cli/__init__.py` | removal of `kiso serve` |
| `docs/docker.md` | complete rewrite |
| `docs/cli.md` | command reference update |
| `docs/config.md` | path `~/.kiso/instances/{name}/` |
| `docs/connectors.md` | path + connector port strategy |
| `docs/logging.md` | updated paths |

---

### Subtasks

- [x] 50a. Python core: removal of `kiso serve` from parser + Dockerfile CMD → uvicorn
- [x] 50b. `kiso-host.sh`: full rewrite — multi-instance, port resolution, `kiso instance *`, implicit resolution, connector port assignment on install
- [x] 50c. `install.sh`: bot name + `~/.kiso/instances/{name}/` + `instances.json` + docker run with port range
- [x] 50d. Docs: `docker.md` (rewrite), `cli.md`, `config.md`, `connectors.md`, `logging.md` + `completions/`

---

## Milestone 46: Plugin install — ask env vars before installing, with how-to instructions

**Problem observed in production**: the planner installed the discord connector, then discovered missing env vars from the install output, and only at the end asked the user for the values. Double problem:
1. The user waits for the entire install process before being prompted.
2. The message to the user didn't explain how to obtain the values (where to find the bot token, what the kiso token is, etc.).

**Root causes**:
1. The planner prompt said "install, then check" — wrong order.
2. The discord connector's `kiso.toml` had no `description` on env var fields.
3. `cli/connector.py` printed only `warning: VAR not set`, without description.

**Fix**:

- [x] `plugins/connector-discord/kiso.toml`: added `description` field to each env var (`bot_token`, `kiso_token`, `webhook_secret`) with instructions on how to obtain them
- [x] `cli/connector.py`: `_connector_install()` — prints `(required|optional) — {description}` next to each missing env var warning; uses `decl.items()` instead of `for key in env_decl` to access descriptions
- [x] `kiso/roles/planner.md`: "Plugin installation (MANDATORY)" rule rewritten as ordered list of 6 steps: (1) curl registry, (2) curl kiso.toml from GitHub BEFORE installing, (3) msg user with descriptions if env vars missing, (4) kiso env set, (5) install, (6) run
- [x] `tests/test_cli_connector.py`: `test_connector_install_env_warning_includes_description` — verifies that install output contains description next to warning for required and optional
- [x] `tests/test_brain.py`: `test_m46_plugin_install_checks_kiso_toml_before_install` — verifies that raw.githubusercontent.com/kiso.toml appears before `kiso connector install` in the prompt
- [x] `tests/test_brain.py`: `test_m46_plugin_install_includes_env_description_in_msg` — verifies that the prompt instructs the planner to include descriptions in the user message
- [x] `kiso/roles/messenger.md`: added "verbatim" rule — setup instructions (commands, URLs, step-by-step) must be reported exactly, not paraphrased
- [x] `tests/test_brain.py`: `test_verbatim_instructions_rule` — verifies that "verbatim" is in the messenger prompt

---

## Milestone 51: Infrastructure — automated tests for kiso-host.sh and install.sh

**Problem**: `kiso-host.sh` and `install.sh` have zero automated coverage. The logic for name validation, implicit instance resolution, port detection, and `kiso instance *` commands is entirely manually tested.

**Framework**: [bats-core](https://github.com/bats-core/bats-core) — the de facto standard for bash testing. Installed as a dev dependency (`tests/bash/bats/` via git submodule or downloaded in CI). No runtime dependencies.

**Mock strategy**: `PATH` is prepended with a `tests/bash/mocks/` directory containing stubs for `docker`, `git`, `ss`, `python3` (instances.json), etc. Tests verify flags, arguments, and observable side-effects (exit code, output, written files).

### 51a. Tests for `kiso-host.sh`

- [x] `tests/bash/test_host_name_validation.bats`: `validate_name()` — valid (`kiso`, `my-bot`, `a`), invalid (`MyBot`, `-bot`, `bot_name`, 33-char string, empty string)
- [x] `tests/bash/test_host_resolve_instance.bats`: 0 instances → exit 1 + message; 1 instance → implicit; 2+ instances without `--instance` → exit 1; 2+ instances with `--instance NAME` → NAME used
- [x] `tests/bash/test_host_instance_commands.bats`: `instance list`, `instance start`, `instance stop`, `instance restart`, `instance logs`, `instance shell` — verifies the correct container is targeted and docker flags are correct
- [x] `tests/bash/test_host_instance_remove.bats`: without `--yes` → prompts for confirmation; with `--yes` → removes without prompt; 'n' → abort
- [x] `tests/bash/test_host_instance_explore.bats`: non-existent SESSION in directory → exit 1 with message; existing SESSION → `docker exec` with `-e SESSION=...`
- [x] `tests/bash/test_host_port_detection.bats`: `_next_free_server_port()` skips ports already in use (mock `ss`); `_next_free_connector_base()` skips bases already in `instances.json`

### 51b. Tests for `install.sh`

- [x] `tests/bash/test_install_name_validation.bats`: same rules as 51a — valid, invalid, duplicate already in `instances.json`
- [x] `tests/bash/test_install_port_allocation.bats`: `next_free_server_port()` and `next_free_connector_base()` with mock `instances.json` containing existing instances
- [x] `tests/bash/test_install_register.bats`: `register_instance()` creates `instances.json` if missing; adds entry without overwriting existing instances

---

## Milestone 52: Core — multi-instance bug fixes

Three targeted fixes to `kiso-host.sh` identified during post-M50 analysis.

### 52a. `kiso instance explore` — SESSION validation

**Problem**: `kiso instance explore SESSION` enters the container even if `SESSION` does not exist as a workspace directory, producing a shell in `/` with no useful context.

**Fix**: before `docker exec`, verify that `~/.kiso/instances/{name}/sessions/{session}/` exists on the host. If missing → exit 1 with message "session '{session}' not found in instance '{name}'". If `SESSION` is omitted → shell in `/root/.kiso/` (current behavior, useful for debug).

- [x] `kiso-host.sh`: check `[ -d "$KISO_DIR/instances/$INST/sessions/$SESSION" ]` before `docker exec`; SESSION=default (hostname@user) when omitted
- [x] `tests/bash/test_host_instance_explore.bats`: updated — mkdir session in test "with SESSION"; new test "SESSION not found → error"

### 52b. `kiso instance remove` — interactive confirmation when `--yes` absent

**Problem**: without `--yes`, the command exits silently (or performs the remove without a prompt — to be verified). No test covers this today.

**Fix**: if `--yes` is absent → interactive prompt "Remove instance '{name}' and all its data? [y/N]"; default N; only `y`/`Y` proceeds.

- [x] `kiso-host.sh`: interactive prompt in the `remove` branch when `--yes` is not present (already implemented in M50)
- [x] `tests/bash/test_host_instance_remove.bats`: covered in M51 (5 tests: --yes, -y, 'y' stdin, 'n' abort, keeps others)

### 52c. `kiso instance create` — port already in use at docker run time

**Problem**: `_next_free_server_port()` checks ports at detection time; but between detection and `docker run` another instance may have claimed the port. Currently silent: docker run fails with an unhelpful error.

**Fix**: after `docker run`, verify the container is in `running` state (retry 1s × 3). If it doesn't start → `docker logs kiso-{NAME} | tail -20` + message "container failed to start — see above" + cleanup entry from `instances.json`.

- [x] `kiso-host.sh`: health check with retry (3×) after docker run; skip instances.json write on failure; `docker rm -f` cleanup
- [x] `tests/bash/test_host_instance_commands.bats`: test `create` fails → instances.json does not contain the entry; `sleep` mock in helpers for instant tests

---

## Milestone 53: Versioning — line count in `kiso version` command

**Goal**: `kiso version --stats` shows the project's line count, broken down by area, computed in Python (no `wc -l` — for portability and control over what to count).

**What is counted**: non-empty, non-comment lines (lines of code, LOC) in `.py` files of the project. Three areas:

| Area | Path | Description |
|---|---|---|
| `core` | `kiso/` | Main framework |
| `cli` | `cli/` | Interactive CLI |
| `tests` | `tests/` | Test suite |

**Output**:
```
kiso 0.1.0

  core     3 241 loc   (kiso/)
  cli      1 088 loc   (cli/)
  tests    8 470 loc   (tests/)
  ─────────────────
  total   12 799 loc
```

**Implementation**:

- [x] `kiso/_version.py`: new function `count_loc(root: Path) -> dict` + helper `_loc_in_dir(directory)` — rglob *.py, counts non-empty and non-comment lines
- [x] `cli/__init__.py`: `version_p.add_argument("--stats")`; `_print_version_stats()` calls `count_loc`, formats output with thousands separator (space); without `--stats` → current behavior
- [x] `tests/test_version.py`: `TestLocCounter` — 5 tests passed

---

## Milestone 54: `kiso stats` — token usage per session and instance

**Goal**: `kiso stats` command that aggregates token consumption data (already present in the audit log) by model, session, or role. The wrapper adds per-instance differentiation with `--all`.

### Current state

The data collection infrastructure is already complete:
- `kiso/audit.py`: every LLM call generates a JSONL row `{type, session, role, model, provider, input_tokens, output_tokens, duration_ms, status, timestamp}` in `~/.kiso/instances/{name}/audit/YYYY-MM-DD.jsonl`
- `kiso/llm.py`: tokens tracked in-memory via contextvars and written to the audit log
- `kiso/store.py`: tokens accumulated per task (`tasks.input_tokens/output_tokens`) and per plan (`plans.total_input_tokens/output_tokens`)

Only the aggregation and presentation layer is missing.

### 54a. Aggregation engine (`kiso/stats.py`)

- [x] `kiso/stats.py`: `read_audit_entries`, `aggregate`, `estimate_cost`, `MODEL_PRICES`
- [x] `tests/test_stats.py`: 16 unit tests (6 read_audit_entries, 6 aggregate, 4 estimate_cost)

### 54b. API endpoint `GET /admin/stats`

- [x] `kiso/main.py`: `GET /admin/stats` — admin-only, params `since`, `session`, `by`
- [x] `tests/test_stats.py`: 4 endpoint tests (admin guard, empty rows, aggregate by model, session filter)

### 54c. CLI `kiso stats`

- [x] `cli/stats.py`: `run_stats_command`, `print_stats`, `_fmt_k`, `_fmt_cost`
- [x] `cli/__init__.py`: subparser `stats` + dispatch + `/stats` REPL slash command + `_slash_stats`
- [x] `tests/test_cli.py`: 3 tests (subcommand exists, flags registered, defaults correct)
- [x] `docs/cli.md`: "Token Usage Statistics" section with examples and formatted output

### 54d. Wrapper `kiso stats [--all]` in `kiso-host.sh`

- [x] `kiso-host.sh`: `stats)` branch — resolves instance, delegates via `docker exec`; `--all` loops all instances with header
- [x] `tests/bash/test_host_stats.bats`: 4 tests (docker exec called, --since passed, --all headers, instance not running)

---

## Milestone 55: Autocomplete — shell, REPL, wrapper

**Goal**: consistent TAB completion across all three levels — bash/zsh shell (commands and dynamic arguments), interactive REPL (slash commands), Python CLI (registered subparsers).

### 55a. CLI `kiso completion bash|zsh` + wrapper handler

- [x] `kiso/completions/kiso.bash` and `kiso/completions/kiso.zsh`: scripts bundled in the Python package (via importlib.resources)
- [x] `cli/__init__.py`: subparser `completion shell` + dispatch that reads from `kiso/completions/`
- [x] `kiso-host.sh completion`: delegates to `docker exec kiso-$INST uv run kiso completion {shell}`; fallback to system install
- [x] `tests/test_cli.py`: 4 tests (bash contains `_kiso`/`complete -F`; zsh contains `#compdef kiso`; stats in both)
- [x] `tests/bash/test_host_completion.bats`: 3 tests (bash output, zsh output, invalid shell → error)

### 55b. Updated completions — `stats` + multi-instance sessions + `explore`

- [x] `kiso/completions/kiso.bash`: added `stats` to top-level commands; `stats)` branch with `--since/--session/--by/--all`; `_kiso_sessions()` uses `COMP_WORDS` to detect `--instance/-i`; `explore)` in instance sub-commands calls `_kiso_sessions`
- [x] `kiso/completions/kiso.zsh`: same changes; `_kiso_sessions()` uses `$words` (zsh completion global); `explore)` + `stats)` + `--session` top-level completed
- [x] `completions/kiso.bash` and `kiso.zsh`: synchronized

### 55c. REPL `/stats` slash command

- [x] `cli/__init__.py`: `/stats` in `_SLASH_COMMANDS`; `_slash_stats()` calls `GET /admin/stats?since=7&session=...&by=model`; already implemented in M54

---

## Milestone 56: Audit docs and tests — M54-M55 edge cases

**Goal**: fill documentation and test gaps discovered after M54-M55.

### 56a. Documentation

- [x] `docs/api.md`: complete `GET /admin/stats` section (params, response JSON, 400/403)
- [x] `docs/audit.md`: "Querying the Audit Log" section with `kiso stats` examples and dimensions table
- [x] `docs/cli.md`: error handling table for `kiso stats`; "Shell Completion" section with `source <(...)` examples
- [x] `docs/testing.md`: updated counts (unit ~1670, bats 60)

### 56b. Tests

- [x] `tests/test_stats.py`: `TestPrintStats` (6 tests: empty rows→"(no data)", all-unknown→cost column omitted, known model→column shown, single row, session filter in header, mixed known/unknown); `TestFmtHelpers` (7 tests: `_fmt_k` 0/999/1000/1.234M; `_fmt_cost` None/zero/small/normal)
- [x] `tests/test_stats.py`: `test_unreadable_file_silently_skipped`, `test_missing_field_falls_back_to_unknown`
- [x] `tests/bash/test_host_stats.bats`: `--all` with 0 instances shows message and exits 0; explicit `--instance` uses the specified instance

---

## Milestone 57: install.sh — UX naming (bot name first, derive instance id)

**Problem**: the old flow asked for instance name and display name as consecutive questions — counter-intuitive order and unclear distinction for the user.

**Solution**: ask for the bot's display name first, automatically derive the identifier via slug, show it for confirmation/override.

- [x] `install.sh`: `_derive_instance_name()` — slugify: lowercase, spaces/underscores→hyphens, strip non-alnum, collapse multiple hyphens, truncate to 32, fallback "kiso"
- [x] `install.sh`: `ask_bot_and_instance_name()` — replaces `ask_instance_name()` + `ask_bot_name()`; globals `BOT_NAME` and `INST_NAME`; flow: ask bot name [Kiso], show derived identifier, ask for confirmation/override
- [x] `docs/docker.md`: "Instance names" section updated with note on automatic derivation flow
- [x] `tests/bash/test_install_derive_name.bats`: 10 tests — passthrough, uppercase→lowercase, spaces→hyphens, underscores→hyphens, special chars stripped, consecutive hyphens collapsed, leading/trailing hyphens stripped, truncate to 32, empty input→"kiso", digits preserved

---

## Milestone 58: Dead code removal — migration and legacy session scope

**Goal**: remove backward-compatibility code for users/DBs that never existed (software not yet distributed).

### 58a. install.sh — remove single-instance layout check

- [x] `install.sh`: removed `OLD_CONFIG` block (18 lines) that detected the old `~/.kiso/config.toml` structure and suggested manual migration to multi-instance

### 58b. store.py — remove runtime migration and legacy session scope

- [x] `kiso/store.py`: removed `_migrate()` (54 lines) + FTS backfill — all columns already present in base SCHEMA; no old DB exists
- [x] `kiso/store.py`: removed `OR session IS NULL` from `get_facts` and `search_facts` — pre-M43 leftover for facts without a session; never existed in production
- [x] `kiso/worker/loop.py`: made explicit `is_admin=True` in the two places that called `get_facts(db)` without arguments (bump fact usage)
- [x] `tests/test_store.py`: removed `test_get_facts_null_session_user_fact_is_global`
- [x] `docs/flow.md`: removed point "user facts with session IS NULL (legacy): treated as global"

---

## Milestone 59: Security — path traversal, pub token, install source injection

**Goal**: fix three vulnerabilities identified during code review.

### 59a. Path traversal in `GET /pub/{token}/{path}` (`main.py:241`)

**Problem**: the check uses `str(file_path).startswith(str(pub_dir.resolve()))`. If `pub_dir=/home/user/pub`, the path `/home/user/pub-evil/secret` passes the check because it is a valid string prefix.

**Fix**: `file_path.resolve().is_relative_to(pub_dir.resolve())` (available since Python 3.9).

- [x] `kiso/main.py`: replace the startswith check with `Path.is_relative_to()`
- [x] `tests/test_published.py`: added `test_path_traversal_sibling_dir_blocked` — directory with identical prefix but different name is rejected

### 59b. Insecure pub token fallback (`pub.py:15`)

**Problem**: `config.tokens.get("cli") or "kiso"` — if the CLI token is not configured, pub token signing uses the guessable string `"kiso"`.

**Fix**: raise `ValueError` instead of using the fallback; `_report_pub_files()` catches and logs, returning `[]`.

- [x] `kiso/pub.py`: removed fallback `or "kiso"`; raised `ValueError` with explicit message
- [x] `kiso/worker/utils.py`: `_report_pub_files()` catches `ValueError` from `pub_token`, logs warning, returns `[]`
- [x] `tests/test_published.py`: `test_fallback_key_when_no_cli_token` → `test_raises_when_no_cli_token`

### 59c. Code injection in `install.sh` via `source "$ENV_FILE"` (`install.sh:657`)

**Problem**: `set -a; source "$ENV_FILE"; set +a` — if `.env` contains shell commands (not just `KEY=VALUE` assignments), they are executed with the installer's privileges.

**Fix**: `grep -E '^KISO_LLM_API_KEY='` + `cut` to extract only the needed value, without executing the file.

- [x] `install.sh`: replaced `source "$ENV_FILE"` with safe read via grep/cut; temporary variable `_api_key_check` unset after the check
- [x] `tests/bash/test_install_env_safe_read.bats`: 5 tests — normal read, command injection blocked, subshell blocked, missing key, commented line ignored
- [x] `docs/security.md`: "Published File Security" section rewritten (was incorrect: said UUID4, it's HMAC-SHA256); added detail on `is_relative_to`, symlinks, missing token
- [x] `docs/api.md`: path traversal description updated with `is_relative_to` and covered vectors; note on missing CLI token
- [x] `tests/test_published.py`: added `test_path_traversal_symlink_blocked` and `test_path_traversal_absolute_filename_blocked`
- [x] `tests/test_worker.py`: added `test_empty_when_no_cli_token` for `_report_pub_files`

---

## Milestone 60: Correctness — store atomicity, transactions, zombie processes

**Goal**: fix race conditions, transactional inconsistency, and resource leaks identified during code review.

### 60a. `append_task_llm_call` not atomic (`store.py:340`)

**Problem**: reads JSON → deserializes → appends → rewrites. If two coroutines write to the same task row in parallel, one overwrites the other.

**Fix**: native atomic SQLite operation with `json_insert`:
```sql
UPDATE tasks SET llm_calls = json_insert(COALESCE(llm_calls, '[]'), '$[#]', json(?)) WHERE id = ?
```

- [x] `kiso/store.py`: rewrite `append_task_llm_call()` with native SQLite `json_insert` — uses `CASE WHEN json_valid(llm_calls) THEN llm_calls ELSE '[]' END` to handle NULL and corrupt JSON
- [x] `tests/test_store.py`: `test_append_task_llm_call_atomic_no_data_loss` — 10 concurrent appends via `asyncio.gather`, verifies all present without duplicates/losses

### 60b. `recover_stale_running` without transaction (`store.py:510`)

**Problem**: two separate UPDATEs (plans and tasks) without a transaction — if the process dies between the two, plans are reset but tasks remain `running`.

**Analysis**: the two UPDATEs are already in a single implicit aiosqlite transaction (a single `db.commit()` at the end of the function). No real bug.

- [x] `kiso/store.py`: verified — no changes needed, the two UPDATEs already share the same implicit transaction

### 60c. Zombie processes on `asyncio.TimeoutError` (`exec.py:35`, `skill.py:35`)

**Problem**: when `asyncio.wait_for` times out, the child process keeps running. No `proc.kill()` or `await proc.wait()` in the timeout path.

**Fix**:
```python
except asyncio.TimeoutError:
    proc.kill()
    await proc.wait()
    return "", "Timed out", False
```

- [x] `kiso/worker/exec.py`: added kill + wait in the `TimeoutError` handler
- [x] `kiso/worker/skill.py`: same fix
- [x] `tests/test_worker.py`: `test_timeout_kills_process` in `TestExecTask` and `TestSkillTask` — mock subprocess, verifies `kill` and `wait` called on timeout

### 60d. Non-atomic connector status write (`connector.py:120`)

**Problem**: `_write_status()` writes directly to the JSON file — if a reader accesses it mid-write, it gets truncated/corrupt JSON.

**Fix**: write to `.tmp` then `os.replace()` (atomic on POSIX).

- [x] `cli/connector.py`: rewrite `_write_status()` with write-tmp + replace pattern — uses `status_file.with_suffix(".tmp")` and `tmp_file.replace(status_file)`
- [x] `tests/test_cli_connector.py`: existing tests verify the behavior; atomic write does not alter the public interface

---

## Milestone 61: Performance — connection pooling, cache, transaction batching

**Goal**: eliminate redundant I/O and allocations identified during code review.

### 61a. HTTP connection pooling for LLM calls (`llm.py`)

**Problem**: new `httpx.AsyncClient` on every LLM call — each time a new TLS handshake. With a 10-task plan × 3 retries = 30+ TCP connections.

**Fix**: shared module-level client, initialized from `main.py` lifespan. Fallback to per-call client when `_http_client is None` (tests, standalone use).

- [x] `kiso/llm.py`: `_http_client: httpx.AsyncClient | None`, `init_http_client(timeout)`, `close_http_client()`. `call_llm` uses the shared client if available, otherwise `async with httpx.AsyncClient(...)`.
- [x] `kiso/main.py`: `init_http_client(timeout=exec_timeout)` at startup, `close_http_client()` at shutdown.
- [x] `tests/test_llm.py`: 3 new tests (`TestSharedHttpClient`) — shared client used, per-call fallback, init+close.

### 61b. Cache `discover_skills()` (`skills.py`) — REVERTED

**Note**: the cache was implemented and then removed (commit "M61b: remove discover_skills cache — scan on every call"). The scan is fast enough (parsing TOML of a few files) to not justify the added complexity. `discover_skills()` rescans on every call — no cache.

- [x] Implemented and then removed: no caching code in the current source
- [x] `tests/test_skills.py`: `TestDiscoverSkillsNoCache` — verifies that newly installed skills are immediately visible without invalidating anything

### 61c. Audit dir init only once (`audit.py`)

**Problem**: `mkdir(parents=True, exist_ok=True)` + `os.chmod(0o700)` executed on every write to the audit log.

**Fix**: `_audit_dir_ready: set[Path]` — the directory is initialized only on first access per process.

- [x] `kiso/audit.py`: `_ensure_audit_dir(path)` with guard `_audit_dir_ready`
- [x] `tests/test_audit.py`: `TestEnsureAuditDir` — 3 tests (creates dir + permissions, mkdir called only once, N `_write_entry` → chmod only once)

### 61d. `rglob` with `islice` for large workspaces (`sysenv.py`)

**Analysis**: `_collect_workspace_files()` already uses a loop with `break` at `_MAX_SCAN=1000` candidates — never materializes the entire tree. Refactoring with `islice` would be purely cosmetic on already correct and lazy logic.

- [x] `kiso/sysenv.py`: verified — no changes needed

### 61e. `httpx.AsyncClient` singleton in `webhook.py`

**Problem**: the client is created inside the retry loop — 3 attempts = 3 TCP connections.

**Fix**: `async with httpx.AsyncClient(...)` wraps the entire retry loop.

- [x] `kiso/webhook.py`: `async with httpx.AsyncClient(timeout=10.0, follow_redirects=False) as client:` moved outside the `for attempt, delay in ...`

### 61f. `rich.Console` singleton in `render.py`

**Analysis**: the `Console` in `render.py` is parameterized per-call with `file=buf` (StringIO/AsciiBuffer), `width=caps.width`, `force_terminal`, `no_color` — all dependent on the current `TermCaps`. Cannot be a singleton. No changes.

- [x] `cli/render.py`: verified — no changes needed

---

## Milestone 62: Worker refactor — dispatch, split `_process_message`, dedup task-step ✅

**Goal**: reduce cyclomatic complexity and duplication in the core of the worker.

### 62a. Dispatch pattern in `_execute_plan()` ✅

- [x] `kiso/worker/loop.py`: extracted `_handle_exec_task`, `_handle_msg_task`, `_handle_skill_task`, `_handle_search_task`, `_handle_replan_task` + `_PlanCtx` + `_TaskHandlerResult`
- [x] `kiso/worker/loop.py`: if/elif replaced with `_TASK_HANDLERS.get(task_type)`. `_execute_plan` reduced from ~533 to ~80 lines
- [x] `tests/test_worker.py`: `TestTaskHandlers` (9 tests per handler in isolation)

### 62b. Deduplicate the task-step pattern ✅

- [x] `kiso/worker/loop.py`: extracted `_store_step_usage()` and `_run_review_step()` — used by exec, skill, search handlers to eliminate duplication
- Note: general `_run_task_step` not extracted (review/usage pattern not uniform enough across task types); the two specific helpers cover ~80% of the effective duplication

### 62c. Split `_process_message()` ✅

- [x] `kiso/worker/loop.py`: extracted `_run_planning_loop()` — while-True replan loop (~280 lines). `_process_message` reduced from ~435 to ~105 lines
- [x] `kiso/worker/__init__.py`: all new names exported for testability
- [x] `tests/test_worker.py`: `TestRunPlanningLoop` (2 tests: success path, failure path)

---

## Milestone 63: Plugin system refactor — dedup install, validate, subprocess ✅

**Goal**: eliminate ~300+ lines of duplication between skill and connector.

### 63a. Unify `_skill_install` and `_connector_install` (`skill.py`, `connector.py`)

**Problem**: the two functions are ~150 lines of nearly identical code — clone → marker → validate → unofficial warning → `uv sync` → `deps.sh` → `check_deps` → `check_env_vars` → remove marker.

**Fix**: extract `_plugin_install(plugin_dir, plugin_type, validate_fn, check_deps_fn)` in `plugin_ops.py`.

- [x] `cli/plugin_ops.py`: add `_plugin_install()` parameterized (with `post_install` callback)
- [x] `cli/skill.py`: delegate to `_plugin_install`; `_skill_post_install()` for env vars, usage_guide, git exclude
- [x] `cli/connector.py`: delegate to `_plugin_install`; `_connector_post_install()` for env vars + config.example.toml
- [x] Tests: behavior verified (patch target `check_deps_fn` passed as parameter)

### 63b. Unify `_validate_*_manifest` (`skills.py`, `connectors.py`)

**Problem**: `_validate_connector_manifest()` and `_validate_skill_manifest()` duplicate the same logic (required fields, version format, env vars, commands). They differ only in field names.

**Fix**: `_validate_manifest(manifest, required_fields, manifest_type)` in a shared module.

- [x] Extracted in `kiso/plugins.py`: `_validate_plugin_manifest_base(manifest, plugin_dir, plugin_type)`
- [x] `kiso/skills.py`: `_validate_manifest` calls base + skill-specific checks
- [x] `kiso/connectors.py`: `_validate_connector_manifest` delegates directly to base
- [x] `tests/test_skills.py`: `TestValidatePluginManifestBase` (7 tests)

### 63c. Extract `_run_subprocess` from `exec.py` and `skill.py`

**Problem**: `run_exec()` and `run_skill()` have identical structure — command construction, `asyncio.create_subprocess_exec`, `wait_for`, `communicate`, output/error handling.

**Fix**: `async def _run_subprocess(cmd, env, timeout, cwd) -> SubprocessResult` in `worker/utils.py`.

- [x] `kiso/worker/utils.py`: add `_run_subprocess()` (shell/exec, stdin, uid, max_output_size)
- [x] `kiso/worker/exec.py`: delegate to `_run_subprocess`
- [x] `kiso/worker/skill.py`: delegate to `_run_subprocess`
- [x] `kiso/worker/__init__.py`: exported `_run_subprocess`
- [x] `tests/test_worker.py`: `TestRunSubprocess` (5 tests); patch targets updated to `kiso.worker.utils.asyncio.*`

---

## Milestone 64: CLI and brain refactor — split `_poll_status`, simplify `_retry_llm` ✅

**Goal**: reduce the longest functions in the CLI and brain.

### 64a. Split `_poll_status()` (`cli/__init__.py:200`)

**Problem**: ~380 lines mixing polling, rendering, spinner management, and stop conditions.

**Fix**: extract:
- `_render_plan_status(data, verbose) -> None` — rendering only
- `_should_stop_polling(data) -> bool` — stop conditions
- `_poll_status(session, ...)` — loop only, calls the others

- [x] `cli/__init__.py`: `_PollRenderState` (dataclass), `_should_stop_polling(plan, tasks, message_id, worker_running, seen, failed_stable_polls) -> tuple[bool, int]`, `_render_plan_status(data, message_id, quiet, verbose, caps, bot_name, state) -> list` — mutates state in-place
- [x] `tests/test_cli.py`: `TestShouldStopPolling` (9 tests), `TestRenderPlanStatus` (7 tests)

### 64b. Simplify `_retry_llm_with_validation()` (`brain.py:65`)

**Problem**: handles 3 distinct cases (planner, reviewer, curator) with all-optional parameters. High cyclomatic complexity.

**Fix**: separate into `_retry_with_json_schema()` and `_retry_with_key_check()`, or use an injectable `validator` callable.

- [x] `kiso/brain.py`: removed `validate_kwargs`; `validate_fn` always has signature `(result) -> list[str]`; callers use lambda where needed (`run_planner`, `run_curator`)

### 64c. Deduplicate setup in `_chat()` and `_msg_cmd()` (`cli/__init__.py:750`)

**Problem**: ~60 lines of identical setup (config loading, token, user/session, httpx client) in two functions.

**Fix**: extract `_setup_client_context(args) -> ClientContext` (namedtuple or dataclass).

- [x] `cli/__init__.py`: `_ClientContext(NamedTuple)` + `_setup_client_context(args)` — used by `_chat` and `_msg_cmd`

---

## Milestone 65: Tech debt — TypedDict, cache prompts, bash robustness, log rotation ✅

**Goal**: resolve low-priority tech debt accumulated during development.

### 65a. TypedDict for store entities (`store.py`)

**Problem**: `dict[str, Any]` used everywhere for session, message, task, fact — no semantic distinction, no type checker support.

**Fix**: define `TypedDict` for the 4 main entities: `Session`, `Message`, `Task`, `Fact`.

- [x] `kiso/store.py`: add `SessionDict`, `MessageDict`, `PlanDict`, `TaskDict`, `FactDict`; update signatures of main store functions

### 65b. Cache `_load_system_prompt()` (`brain.py:120`)

**Problem**: role files are re-read from disk on every LLM call. The `invalidate_cache()` cache already exists for `sysenv` — extend the same pattern.

- [x] `kiso/brain.py`: add `_prompt_cache: dict[str, str] = {}` and `invalidate_prompt_cache()`; autouse fixture in `test_brain.py` for isolation

### 65c. `MODEL_PRICES` — ordering enforcement (`stats.py:20`)

**Problem**: matching depends on key order in the dict — "more specific before less specific" is documented but not verified. Adding a key in the wrong position silently breaks cost estimates.

- [x] `tests/test_stats.py`: `TestFindPriceOrdering` — 8 tests on gemini-2.x vs gemini-flash, gpt-4o-mini vs gpt-4o, llama-3.3 vs llama-3, dict key order

### 65d. `build_secret_variants()` silent on exception (`security.py:120`)

**Problem**: if building variants for a secret fails (e.g. encoding error), the exception is silently swallowed.

- [x] `kiso/security.py`: added `import logging` + `log`; `logger.error(...)` in the except block

### 65e. Bash robustness: `set -euo pipefail` in `kiso-host.sh`, `base_url` unbound in `install.sh`

**Problem 1**: `kiso-host.sh` does not use `set -euo pipefail` — intermediate errors are ignored.
**Problem 2**: `install.sh:639` — `base_url="${base_url:-...}"` can be unbound with `set -u` if `NEED_CONFIG=false`.
**Problem 3**: `install.sh:300` — non-interactive detection uses `ARG_USER && ARG_API_KEY` instead of `[[ ! -t 0 ]]`.
**Problem 4**: `install.sh:450` — `while *--*` loop in `_derive_instance_name` is O(n²) on degenerate input. Fix: `raw=$(printf '%s' "$raw" | tr -s '-')`.

- [x] `kiso-host.sh`: already had `set -euo pipefail` ✓
- [x] `install.sh`: `base_url=""` initialized before the NEED_CONFIG block; `tr -s '-'` in `_derive_instance_name`; `[[ ! -t 0 ]]` added to non-interactive detection in `ask_models`
- [x] `tests/bash/test_install.bats`: 10 bats tests for `_derive_instance_name`

### 65f. Log rotation for session log (`log.py`)

**Problem**: session logs are never rotated.

- [x] `kiso/log.py`: `RotatingFileHandler` for session log (2 MB, 2 backups); test `test_session_log_uses_rotating_handler`

### 65g. `SessionLogger` — add `debug()` and `exception()` methods (`log.py:40`)

- [x] `kiso/log.py`: added `debug()` and `exception()` to `SessionLogger`; tests in `test_log.py`

### 65h. Fragile `client` fixture in `conftest.py` relative to `lifespan` (`conftest.py:143`)

**Problem**: `app.state.config` and `app.state.db` set directly in the fixture. If `lifespan()` initializes new fields, tests break silently.

**Fix**: extract `_init_app_state(app, config, db)` from `lifespan()`, used both in production and in tests.

- [x] `kiso/main.py`: extracted `_init_app_state()`
- [x] `tests/conftest.py`: updated `client` fixture to use it

---

## Milestone 66: Code review — performance, correctness, robustness

Improvements identified during a systematic code review of the codebase. No user-visible behavior changes.

---

### 66a. Performance — `_post_plan_knowledge` parallelism

**Problem**: Curator (LLM call) and Summarizer (LLM call) run serially even though they are completely independent. With both active, post-plan time includes two sequential LLM calls. Decay and Archive (SQL only) same issue.

**Fix**: three phases instead of five sequential:
1. `asyncio.gather(curator, summarizer)` — independent, no data dependency
2. Fact consolidation — after curator (sees newly promoted facts)
3. `asyncio.gather(decay, archive)` — SQL only, independent

- [x] `kiso/worker/loop.py`: `_post_plan_knowledge()` — phase 1: `asyncio.gather` on curator and summarizer; phase 3: `asyncio.gather` on decay and archive
- [x] `tests/test_worker.py`: `TestPostPlanKnowledgeParallel` — verifies curator and summarizer called with `gather`; consolidation launched after; decay and archive with `gather`

---

### 66b. Correctness — `update_task_review` does not update `updated_at`

**Problem**: `update_task` and `update_task_substatus` update `updated_at = CURRENT_TIMESTAMP`; `update_task_review` does not. A task receiving the reviewer's verdict does not update its timestamp — the `/status` polling may serve stale data.

**Fix**: add `updated_at = CURRENT_TIMESTAMP` to the UPDATE.

- [x] `kiso/store.py`: `update_task_review()` — added `updated_at = CURRENT_TIMESTAMP` to the UPDATE query
- [x] `tests/test_store.py`: `test_update_task_review_bumps_updated_at` — verifies `updated_at` changes after the call

---

### 66c. Robustness — missing `PRAGMA busy_timeout`

**Problem**: `init_db` sets WAL but not `busy_timeout`. If two coroutines commit close together (substatus update + task update in the same cycle), SQLite may return `SQLITE_BUSY` immediately instead of waiting briefly.

**Fix**: add `PRAGMA busy_timeout = 5000` (5 seconds) immediately after `PRAGMA journal_mode=WAL`.

- [x] `kiso/store.py`: `init_db()` — `await db.execute("PRAGMA busy_timeout = 5000")`
- [x] `tests/test_store.py`: `test_busy_timeout_is_set` — verifies the pragma is active on the test db

---

### 66d. API — unused `collect_deploy_secrets` parameter

**Problem**: `collect_deploy_secrets(config=None)` declares `config` but never reads it. Every caller invokes it without arguments. The parameter misleadingly suggests that `config` affects secret collection.

**Fix**: remove the parameter.

- [x] `kiso/security.py`: `collect_deploy_secrets()` — removed `config=None`
- [x] `kiso/worker/loop.py`: call sites unchanged (already called without argument)
- [x] `tests/test_security.py`: verify existing tests pass

---

### 66e. Maintainability — `build_*_messages` declared `async` without `await`

**Problem**: `build_reviewer_messages`, `build_paraphraser_messages`, `build_summarizer_messages`, `build_searcher_messages`, `build_exec_translator_messages`, `build_messenger_messages`, `build_classifier_messages` are `async def` but contain no `await`. Callers await a coroutine that does no I/O, with unnecessary scheduling overhead and a misleading API about the function's contract.

**Fix**: convert to regular `def`; update call sites that `await` them.

- [x] `kiso/brain.py`: 7 `build_*_messages` functions from `async def` to `def`; removed `await` at internal call sites in `brain.py`
- [x] `tests/test_brain.py`: verify existing tests pass (no functional changes)

---

### 66f. Robustness — silent FTS5 fallback

**Problem**: the `except Exception` block in `search_facts` that activates the fallback to `get_facts` does not log the exception. If FTS5 fails for an unexpected reason (corrupt table, bug), the degradation happens invisibly.

**Fix**: add `log.debug(...)` in the except block with the exception and `exc_info=True`.

- [x] `kiso/store.py`: `search_facts()` — `except Exception as exc: log.debug("FTS search failed, falling back: %s", exc, exc_info=True)`
- [x] `tests/test_store.py`: `test_fts5_fallback_logs_debug` — mock db that raises exception on FTS query; verifies `log.debug` is called

---

### 66g. Style — `import json` inside function + missing `encoding`

**Problem 1**: `_strip_llm_verbose` in `main.py` imports `json` inside the function body (`import json as _json`). Every call to `/status` executes an import statement.

**Problem 2**: `_load_env_file` uses `path.read_text()` without `encoding="utf-8"`. The rest of the codebase specifies encoding explicitly (e.g. `_init_kiso_dirs:69`).

**Fix**: `import json` at module level; `encoding="utf-8"` in `read_text`.

- [x] `kiso/main.py`: `import json` moved to module level; `path.read_text(encoding="utf-8")` in `_load_env_file`

---

### 66h. Performance — `check_deps` re-reads `kiso.toml`

**Problem**: `discover_skills()` parses `kiso.toml` for each skill and builds the dict. `check_deps()` receives that dict but reopens and re-parses the same `kiso.toml` to read `[kiso.deps]` — double I/O for the same information.

**Fix**: include `"deps"` in the dict returned by `discover_skills`; `check_deps` uses `skill.get("deps", {})` without I/O.

- [x] `kiso/skills.py`: `discover_skills()` — added `"deps": manifest.get("kiso", {}).get("deps", {})` to the skill dict
- [x] `kiso/skills.py`: `check_deps()` — uses `skill.get("deps", {}).get("bin", [])` without opening files; keeps fallback to file read if `"deps"` absent (compat with old dicts)
- [x] `tests/test_skills.py`: `test_check_deps_uses_skill_dict_no_io` — verifies `open` is not called when the dict already contains `"deps"`

---

### 66i. Type and style — `_PlanCtx` generics + long import

**Problem 1**: `_PlanCtx.deploy_secrets: dict`, `session_secrets: dict`, `installed_skills: list` use unparameterized types. Inconsistent with the rest of the codebase.

**Problem 2**: `from kiso.llm import ...` in `loop.py` is a 142-character line.

**Fix**: add type parameters; wrap the import over multiple lines.

- [x] `kiso/worker/loop.py`: `_PlanCtx` — `deploy_secrets: dict[str, str]`, `session_secrets: dict[str, str]`, `installed_skills: list[dict]`
- [x] `kiso/worker/loop.py`: `kiso.llm` import wrapped with parentheses over multiple lines

---

## Milestone 67: install.sh — simplified re-install menu

**Problem**: the menu that appeared when instances were already installed was conceptually wrong:
1. Three heterogeneous options: "Add a new instance", "Update Docker image", "Remove an existing instance" — mixing operations of different scope (global vs per-instance) and including a destructive operation ("remove") that has no place in an installer.
2. "Update Docker image" exposed the implementation detail (Docker) to the user, who doesn't know what a Docker image is.
3. "Remove instance" doesn't belong in an installer — it's a maintenance operation for the CLI wrapper.

**Correct principle**: an installer installs. Re-running it makes sense only for two things: installing another instance, or reinstalling an existing one. Reinstallation follows the same normal flow, with the additional option of a factory reset first.

**Fix**: menu reduced to two options; no reference to Docker; factory reset as the first question in the update flow.

- [x] `install.sh`: menu reduced to "Install another instance" / "Update instance 'X'" (or "Update an existing instance" if multiple)
- [x] `install.sh`: removed "Update Docker image (rebuild + restart all)" option
- [x] `install.sh`: removed "Remove an existing instance" option
- [x] `install.sh`: `update-instance` mode — first question: "Reset data? (wipes store.db, messages, learnings, facts — keeps config.toml and .env)"; then continues the normal flow (keep/overwrite config, keep/overwrite .env, rebuild container)
- [x] `install.sh`: in `update-instance` mode ports are read from `instances.json` instead of being re-detected (no port change on reinstall)
- [x] `docs/docker.md`: "Installation" section — re-install options updated

---

## ~~Milestone 68~~: `version` — show total line count inline ✓

**Problem**: `kiso version` printed only `kiso 0.2.0`. The line count was hidden behind `--stats`, a flag most users would never discover.

**Fix**: plain `kiso version` (and `-V`) appends the total on the same line: `kiso 0.2.0  (7742 lines)`. `-V --stats` shows the full per-area breakdown. `loc` renamed to `lines` for clarity; thousands separator removed.

- [x] `cli/__init__.py`: `_VersionAction` replaces `action="version"` — computes LOC at call time, supports `--stats` via `sys.argv` inspection
- [x] `cli/__init__.py`: `_fmt_loc` extracted to module level; plain int, no thousands separator; label `loc` → `lines`
- [x] `tests/test_cli.py`: `test_version_plain_shows_loc`, `test_version_stats_unchanged`, `test_version_flag_stats_shows_breakdown`, `test_version_flag_long_prints_version` updated

---

## ~~Milestone 69~~: session workspace — cartella `uploads/` per sessione ✓

**Problem**: ogni sessione aveva solo `pub/` (file serviti esternamente). Nessun posto dedicato per file che arrivano dall'esterno — allegati email, file Discord/Telegram, upload manuali — che i connettori e le feature future devono poter scrivere in un path stabile senza crearlo ogni volta.

**Fix**: `_session_workspace()` crea `uploads/` accanto a `pub/` ad ogni inizializzazione del workspace. Nessuna logica di upload per ora — solo la directory garantita.

- [x] `kiso/worker/utils.py`: `_session_workspace()` — `uploads_dir = workspace / "uploads"; uploads_dir.mkdir(exist_ok=True)`; chown incluso se `sandbox_uid` presente
- [x] `tests/test_worker.py`: `test_workspace_creates_uploads_dir`; `test_workspace_chown_chmod` aggiornato (chown count 2 → 3)
- [x] `docs/flow.md`: sezione "Uploads Directory" che spiega scopo e contratto della cartella
- [x] `docs/docker.md`: struttura directory aggiornata con `uploads/` sotto `sessions/{session}/`

---

## ~~Milestone 70~~: Autocomplete — shell completion fixes ✓

**Problem**: tre bug critici trovati durante i test manuali pre-release:

1. `_kiso_sessions` interrogava `kiso.db` ma il database si chiama `store.db` — la completion di sessioni non funzionava mai.
2. `kiso skill update/remove` e `kiso connector update/remove/run/stop/status` non completavano i nomi installati.
3. `kiso skill install` e `kiso connector install` mancavano di `--show-deps`.

**Fix**: riscrittura dei file di completion bash e zsh.

- [x] `kiso/completions/kiso.bash`, `completions/kiso.bash`: `_kiso_sessions` — `kiso.db` → `store.db`
- [x] `kiso/completions/kiso.bash`, `completions/kiso.bash`: estratto `_kiso_active_instance()` — elimina la duplicazione della logica di risoluzione istanza
- [x] `kiso/completions/kiso.bash`, `completions/kiso.bash`: aggiunti `_kiso_skill_names()` e `_kiso_connector_names()` via `docker exec ls`
- [x] `kiso/completions/kiso.bash`, `completions/kiso.bash`: `skill install` — aggiunto `--show-deps`; `skill update` — completa con `all` + nomi; `skill remove` — completa con nomi
- [x] `kiso/completions/kiso.bash`, `completions/kiso.bash`: `connector install` — aggiunto `--show-deps`; `connector update` — `all` + nomi; `connector remove/run/stop/status` — nomi
- [x] `kiso/completions/kiso.zsh`, `completions/kiso.zsh`: stesse fix equivalenti per zsh

---

## ~~Milestone 71~~: wrapper — porta interna vs porta esterna ✓

**Problem**: `kiso-host.sh` passava `--api "http://localhost:$SERVER_PORT"` ai comandi eseguiti via `docker exec`. `$SERVER_PORT` è la porta **esterna** dell'host (da `instances.json`, e.g. 8334). Ma il CLI gira dentro il container, dove il server ascolta sulla porta **interna** (da `config.toml`, sempre 8333 di default). Con la prima istanza funzionava per coincidenza (8333 == 8333). Dalla seconda in poi il CLI non riusciva a connettersi.

**Fix**: aggiunta `instance_internal_port()` che legge `[settings] port` da `config.toml` via regex. Tutti i `docker exec` usano ora la porta interna. `SERVER_PORT` rimane usato solo per operazioni lato host (health check, port mapping in `docker run`).

- [x] `kiso-host.sh`: `instance_internal_port()` — legge `port` da `~/.kiso/instances/{name}/config.toml`, fallback a 8333
- [x] `kiso-host.sh`: proxy (chat, msg, e tutti gli altri comandi) — `$SERVER_PORT` → `$INTERNAL_PORT`
- [x] `kiso-host.sh`: `stats` (singola istanza e `--all`) — `$SERVER_PORT` / `$_port` → `$INTERNAL_PORT` / `$_iport`
- [x] `kiso-host.sh`: `reset` — `$SERVER_PORT` → `$INTERNAL_PORT`
- [x] `kiso-host.sh`: help text — `kiso.db` → `store.db`

---

## Milestone 72: Planner + Reviewer — qualità comportamentale

Quattro problemi osservati durante i test manuali pre-release. Tre riguardano il prompt del planner, uno lo schema del reviewer.

### 72a. Planner: kiso management commands nel prompt

**Problem**: il planner non sa che `kiso skill install/update/remove`, `kiso connector install/update/remove/run/stop/status` e `kiso env set/delete/reload` sono exec primitivi disponibili. Quando l'utente chiede di installare qualcosa di disponibile come skill, il planner usa pip/apt invece del Kiso layer.

**Observed**: "installa aider" → piano con `pip install aider-chat` invece di `kiso skill install aider`.

**Fix**: sezione nel prompt del planner con tutti i kiso management commands e la loro sintassi esatta. Il planner può referenziarli in task `exec`.

- [x] `kiso/roles/planner.md`: aggiungere sezione "Kiso management commands" con `kiso skill install/update/remove <name>`, `kiso connector install/update/remove/run/stop/status <name>`, `kiso env set/get/delete/reload`

### 72b. Planner: risoluzione ambigua per installazioni

**Problem**: "installa aider" è ambiguo — potrebbe essere la skill kiso o il tool di sistema. Il planner deve chiedere se non è esplicito. Problema aggravato dal fatto che il planner generava il piano con `pip install` nello stesso piano del registry check, PRIMA di avere i risultati.

**Fix**: se il contesto è esplicito ("la skill X"), procedere direttamente con `kiso skill install`. Se ambiguo, fare il registry check via exec + `replan` (non includere l'install nello stesso piano del check). Pacchetti di sistema noti (git, curl, docker, ecc.) → sistema package manager direttamente.

- [x] `kiso/roles/planner.md`: regola aggiornata per richieste di installazione ambigue con replan obbligatorio dopo registry check

### 72c. Planner: lingua della risposta nel detail dei msg task

**Problem**: il messenger risponde nella lingua del task `detail` (scritto dal planner in inglese), non nella lingua dell'utente. Il messenger non vede la conversazione per design — non può rilevare la lingua da solo.

**Fix** (minima, zero schema change): il planner aggiunge `[Lang: it]` (o la lingua rilevata) come prima riga del `detail` dei task `msg`. Il messenger la legge e risponde in quella lingua. Overhead: ~5 token per msg task.

Alternative scartate: passare la conversazione al messenger (rompe il design); campo `language` nello schema (sproporzionato al beneficio).

- [x] `kiso/roles/planner.md`: regola — per ogni task `msg`, aggiungere `[Lang: xx]` (ISO 639-1) come prefisso del detail
- [x] `kiso/roles/messenger.md`: se il detail inizia con `[Lang: xx]`, rispondere in quella lingua

### 72d. Reviewer: `learn` → array di learning atomici

**Problem**: il reviewer emette un unico string di learning anche quando il task produce più fatti distinti di categorie diverse. Il curator riceve un blob invece di item separati, degradando la qualità della knowledge base.

**Observed**: `"The user speaks Italian. The system has a registry containing at least four skills (search, aider, browser, moltbook) and one connector (discord)."` — tre fatti distinti (`user`, `general`, `general`) come stringa unica.

**Fix**: `learn` passa da `string | null` a `string[] | null` (max 5 items). Ogni elemento = un fatto atomico. Il codice itera l'array e crea un entry in `store.learnings` per ogni item. Il curator non cambia — già valuta ogni learning come entry indipendente.

- [x] `kiso/brain.py`: schema reviewer — `"learn": {"type": ["array", "null"], "items": {"type": "string"}, "maxItems": 5}`
- [x] `kiso/worker/loop.py`: righe 452–455 — `for item in (review.get("learn") or []): await save_learning(db, item, session)`
- [x] `kiso/roles/reviewer.md`: istruzione a emettere item atomici separati, un fatto discreto per item, max 5
- [x] `tests/test_brain.py`: aggiornare test dello schema reviewer per array

---

## Milestone 73: User management via bot and CLI

**Context**: kiso users are defined statically in `config.toml`. There is no mechanism to add/remove users at runtime — neither via CLI nor via bot. An admin who wants to add a user must manually edit the file on the host and restart. This is a significant operational gap: the bot cannot manage users autonomously on the admin's behalf.

**Solution**: add `kiso user` as a CLI subcommand (runs inside the container like all other commands), a `POST /admin/reload-config` endpoint for hot-reload without restart, and update the planner to handle user management requests — including asking for connector aliases.

**Why it works from inside the container**: `config.toml` is a bind mount (`~/.kiso/instances/{name}/config.toml` on the host = `/root/.kiso/config.toml` in the container). Editing it from inside is equivalent to editing it from outside; changes persist across restarts.

**Why `--role` is required** (no default): admin and user have radically different permissions. A silent default would be a security risk. If the admin does not specify the role, the planner must ask before proceeding.

---

### 73a. `tomli-w` dependency + `reload-config` endpoint

**Problem**: `tomllib` (stdlib) is read-only. There is no way to safely rewrite `config.toml` without a TOML writing library. Without hot-reload, every config change requires a container restart.

**Note on `tomli_w` behaviour**: rewrites the entire file without preserving comments. Acceptable: comments are documentation only, values are preserved faithfully. Document in the bot's response that comments are removed on the first user management operation.

- [x] `pyproject.toml` + `uv.lock`: add `tomli-w>=1.0` as a runtime dependency
- [x] `kiso/main.py`: new `POST /admin/reload-config` endpoint — verifies `role == "admin"`, calls `reload_config()`, updates `app.state.config`, calls `invalidate_prompt_cache()` (bot_name or settings may have changed); returns `{"reloaded": True}`; on `ConfigError` returns 400 with the error message
- [x] `docs/api.md`: document the new endpoint
- [x] `tests/test_main.py`: 4 tests — reload ok (admin), 403 (non-admin), 400 (invalid config), cache invalidation called

---

### 73b. CLI `kiso user`

**Commands**:
```
kiso user list
kiso user add <username> --role admin|user [--skills "*"|s1,s2] [--alias connector:id ...]
kiso user remove <username>
kiso user alias <username> --connector <name> --id <platform_id>
kiso user alias <username> --connector <name> --remove
```

**Implementation details**:

- `list`: reads `config.toml` with `tomllib`, prints table of username / role / skills / aliases
- `add`:
  - `--role` required (no default); explicit error if omitted
  - if `role=user`: `--skills` required (at least one value or `"*"`)
  - if `role=admin`: `--skills` ignored (admins have no skill restrictions)
  - `--alias` repeatable, format `connector:platform_id` (e.g. `--alias discord:bob#1234`)
  - validates username against `NAME_RE` from `config.py`
  - error if username already exists
  - reads config with `tomllib`, adds entry, rewrites with `tomli_w`, calls `POST /admin/reload-config`
- `remove`:
  - error if username does not exist
  - error if it is the last remaining admin (prevents lockout)
  - removes entry, rewrites, reload
- `alias`:
  - adds or updates `aliases.{connector} = "{id}"` for an existing user
  - `--remove`: removes the alias for that connector
  - warns if the connector is not found in `instances.json` (non-blocking — connector may not be installed yet)
  - rewrites, reload

- [x] `cli/`: implement `user` subcommand with the four commands above
- [x] `kiso-host.sh`: the catch-all section already proxies everything via `docker exec` — no changes needed
- [x] `tests/test_cli_user.py`: unit tests for each command — add ok, add missing role, add invalid username, add existing user, remove ok, remove last admin, alias add/update/remove, list

---

### 73c. Planner: user management and alias workflow

**Problem**: the planner does not know about `kiso user`, does not know it should ask for active connector aliases, and has no rules to block user management for non-admins.

**Expected flow for "add user X"**:
1. If role not specified → `msg` task asking for role (and skills if role=user)
2. If role specified but no aliases and running connectors are present → `msg` task asking "what is X's ID on {connector}?" for each running connector (already visible in System Environment)
3. Once all info collected → `exec` with `kiso user add`
4. If `role=user` and skills not specified → ask before proceeding

**Protection rule**: if `Caller Role` is `user`, the planner must never generate `kiso user` tasks — respond with a msg explaining that user management requires admin access.

- [x] `kiso/roles/planner.md`: add to "Kiso management commands" section:
  ```
  - Users (admin only): `kiso user add <name> --role admin|user [--skills "*"|s1,s2] [--alias connector:id ...]`
                        `kiso user remove <name>`
                        `kiso user list`
                        `kiso user alias <name> --connector <conn> --id <id>`
                        `kiso user alias <name> --connector <conn> --remove`
  ```
- [x] `kiso/roles/planner.md`: rule — if `Caller Role` is `user`, never generate `kiso user` tasks; respond with a msg explaining the admin requirement
- [x] `kiso/roles/planner.md`: rule — for add-user requests: if role not specified, emit a msg task asking for role (and skills if user) before proceeding; if running connectors are present in System Environment, ask for aliases for each in the same msg task
- [x] `kiso/roles/planner.md`: rule — `kiso user add` with `--role admin` does not require `--skills`; with `--role user` `--skills` is required

---

## Milestone 74: Fix stale tests from M72d/M73 prompt compression

**Problem**: 14 tests broken by two recent milestones that didn't update all affected tests.

**Root causes**:
- **M73 prompt compression** (`planner.md`): phrases removed/reworded that tests matched literally — 6 planner tests fail.
- **M72d reviewer prompt** (`reviewer.md`): capitalization changed (`"sole criterion"` → `"Sole criterion"`); `"The plan context"` phrase removed — 2 reviewer tests fail.
- **M72d learn format** (`brain.py`, `loop.py`): `learn` changed from `string` to `array`; 3 worker tests still pass a string mock and 1 brain test asserts `== string` instead of `== array`.
- **M53 `count_loc`** (`_version.py`): `"tests"` key removed from return dict; 2 version tests use `stats["tests"]`.

**Fix strategy**: update the tests to match current behaviour. No source changes needed — the prompts, `count_loc`, and learn format are all correct.

- [x] `tests/test_brain.py` — planner prompt tests: update 6 literal-string assertions to match compressed prompt wording
- [x] `tests/test_brain.py` — reviewer prompt tests: `"sole criterion"` → `"Sole criterion"`; `"The plan context"` → `"Plan Context"`
- [x] `tests/test_brain.py` — `test_review_with_learning`: assert `review["learn"] == ["Uses Python 3.12"]` (array)
- [x] `tests/test_worker.py` — 3 learning tests: change string mocks to single-item arrays (`"Uses Flask"` → `["Uses Flask"]`, etc.)
- [x] `tests/test_version.py` — 2 tests: remove `stats["tests"]` assertions; update `test_total_equals_sum` (total = core + cli only)

---

## Milestone 75: Code review fixes (M73b follow-up)

**Problem**: code review of M73b found three bugs and four missing test cases.

**Bugs fixed**:
- **Empty skill segments not rejected**: `skills=","` produced `["", ""]` and slipped through; fix: filter with `s.strip()` then guard against empty list with `"no valid skill names"` error.
- **Connector name not validated**: `_user_alias` wrote the connector value directly to TOML without checking `NAME_RE`, allowing special chars to corrupt `config.toml`; fix: add `NAME_RE.match(connector)` gate before reading the file.
- **`load_config(CONFIG_PATH_DEFAULT)` inconsistency**: `_call_reload` passed an explicit path to `load_config` while `cli/env.py` calls it with no argument; fix: use `load_config()` (relies on env-var/default lookup, consistent with the rest of the CLI).

**New tests** (all in `tests/test_cli_user.py`):
- `TestUserAdd::test_add_empty_skills_segments_fails` — `skills=","` → exit 1, "no valid skill"
- `TestUserAdd::test_add_strips_whitespace_from_skills` — `" read , write "` → `["read", "write"]`
- `TestUserAlias::test_alias_invalid_connector_name_fails` — bad connector name → exit 1, "invalid connector"
- `TestRunUserCommandDispatch::test_no_subcommand_prints_usage` — `user_command=None` → exit 1, "usage"

- [x] `cli/user.py` — filter empty skill segments; add `NAME_RE` check for connector; use `load_config()` without args
- [x] `tests/test_cli_user.py` — add 4 new tests covering the fixed bugs and dispatch

---

## Upcoming milestones

### CLI quality
- ~~**M76**: `kiso user edit`~~ ✅ `cli/user.py` — `_user_edit` with last-admin guard, `_parse_skills`, argparse choices
- ~~**M77**: `kiso user list --json`~~ ✅ `cli/user.py` — `if getattr(args, "json", False): print(json.dumps(users))`

### Runtime robustness
- ~~**M78**: `--no-reload` flag~~ ✅ `cli/user.py` — flag on add/edit/remove/alias; `_maybe_reload(args)` helper
- ~~**M79**: loop error recovery~~ ✅ `kiso/worker/loop.py` — `isinstance(learn_raw, list/str)` + warning if string

### Tests / coverage
- ~~**M80**: integration tests for `cli/__init__.py`~~ ✅ `tests/test_cli_integration.py` — 14 end-to-end tests (list, list --json, add, edit, remove, alias) calling `main()` via patched `sys.argv`; only mocks: `require_admin`, `CONFIG_PATH_DEFAULT`, `_call_reload`
- ~~**M81**: coverage audit `kiso instance *`~~ ✅ `cli/instance.py` doesn't exist; audit found `cli/stats.py` at 0% → added `tests/test_cli_stats.py` with 23 tests; now at 100%

### Planner / brain
- ~~**M82**: planner test for ask-then-add workflow~~ ✅ `TestM82PlannerAskThenAdd` (3 tests): (1) `build_planner_messages` injects `## Caller Role\nuser`; (2) `run_planner` accepts a msg-only plan; (3) LLM receives caller role and request together
- ~~**M83**: schema validation for PLAN_SCHEMA and REVIEW_SCHEMA~~ ✅ added `jsonschema>=4` to dev deps; `TestM83PlanSchema` (15 tests) + `TestM83ReviewSchema` (9 tests) validating required fields, additionalProperties, enums, type constraints

---

## Code review — bugs, performance, maintainability (2026-03-02)

Full codebase review. Ordered by priority: definite crashes first, then defensive fixes, then performance, then maintainability.

### M84: Bug fixes

- [x] **84a** — `lifespan()` shutdown (`main.py`): first loop `for session, entry in _workers.items()` iterates without `list()` while cleanup callbacks can remove entries from the dict → `RuntimeError: dictionary changed size during iteration`; fix: `list(_workers.items())`

- [x] **84b** — `_retry_llm_with_validation()` (`brain.py` ~78): `json.JSONDecodeError` raises immediately instead of appending to `last_errors` and retrying like validation errors do; the model often produces valid JSON on a retry

- [x] **84c** — `review["reason"]` direct access (`worker/loop.py`, exec/skill/search handlers): should be `.get("reason")` to survive LLM responses that omit the field despite it being in the schema

- [x] **84d** — `float(item.get("confidence", 1.0))` (`brain.py`, `run_fact_consolidation` ~939): no `try/except ValueError`; crashes if the curator LLM returns `"confidence": "high"`; wrap with fallback to `1.0`

- [x] **84e** — config type validation at load time (`config.py`): `settings.update(settings_raw)` accepts any type without checking; `int(config.settings["max_plan_tasks"])` in `brain.py` crashes at runtime instead of at load; validate expected types for critical keys (`max_plan_tasks`, `max_validation_retries`, `context_messages`, etc.) in `load_config()`; also: `except` blocks in `_build_config` call `on_error()` without guaranteeing execution stops after

- [x] **84f** — `_apply_curator_result()` unsafe dict access (`worker/loop.py` ~1058): direct `ev["learning_id"]`, `ev["verdict"]`, `ev["fact"]`, `ev["question"]` accesses will `KeyError` if the curator LLM omits any field; switch to `.get()` with safe defaults; also: exceptions raised inside `_apply_curator_result()` are not caught by the surrounding curator `try/except`, so a malformed curator response can crash the entire consolidation loop

- [x] **84g** — `cli/__init__.py` unsafe JSON key access: `data["message_id"]` at lines 423 and 487 (send/msg response handling) and `info["session"]`, `info["message_count"]` at lines 984-985 (session info display) crash with `KeyError` on unexpected server responses; switch to `.get()` with graceful fallbacks

- [x] **84h** — `cli/user.py _read_raw()` unhandled `FileNotFoundError` (~line 38): if `config.toml` does not exist the function propagates `FileNotFoundError` instead of returning an empty dict; fix: catch `FileNotFoundError` and return `{}`

- [x] **84i** — prompt contradictions and validation gap:
  - `prompts/planner.md` line 21 says "Always start the detail with `[Lang: xx]`" (unconditional), but `prompts/messenger.md` line 4 says "If the task detail begins with `[Lang: xx]`" (conditional) → the messenger silently ignores the language directive when the planner omits it, producing English output regardless; align the two prompts: either make `[Lang: xx]` mandatory in both or conditional in both
  - `validate_plan()` (`brain.py`) enforces `expect=null` for `msg` tasks but does not enforce `skill=null` and `args=null`; a planner could accidentally include non-null `skill`/`args` and the worker would ignore them silently; add the checks

- [x] **84j** — exec deny list bypassable (`worker/exec.py` ~27): `shell=True` combined with a regex deny list is fragile — `_ROOT_TARGET` requires specific whitespace so `rm -rf/` (no space before `/`) passes; `:\(\)\s*\{.*\|.*&` requires literal fork-bomb syntax so obfuscated variants slip through; the deny list is easily circumvented by a creative LLM; fix: switch to an allowlist of permitted command prefixes, or parse the command into argv and use `shell=False` to eliminate shell metacharacter interpretation entirely

- [x] **84k** — subprocess environment leaks parent's `LD_PRELOAD` / `PYTHONPATH` (`worker/utils.py`, `_build_exec_env()`): the function builds a clean env but does not explicitly unset `LD_PRELOAD`, `LD_LIBRARY_PATH`, `PYTHONPATH`, and similar loader-override variables inherited from the parent process; a compromised parent environment could inject arbitrary libraries into every exec task child; fix: explicitly `env.pop(key, None)` for each dangerous variable before spawning

### M85: Performance

- [x] **85a** — cache `discover_skills()` (`brain.py` ~374): full filesystem scan + TOML parse on every planner call; add a TTL cache (e.g. 30 s) or explicit invalidation via `invalidate_skills_cache()`; the result also feeds `validate_plan()` and can be reused within the same request

- [x] **85b** — track used fact IDs (`brain.py` `build_planner_messages` + `worker/loop.py` ~1147, ~1397): on plan completion and on fast-path chat, `get_facts(all)` + `update_fact_usage(all IDs)` updates every fact in the DB; return the IDs of facts actually injected into context and update only those

- [x] **85c** — batch INSERT in fact consolidation (`worker/loop.py` ~249): `save_fact()` is called in a loop with one commit per row (N SQLite transactions); collect all values and execute a single multi-row INSERT + commit

- [x] **85d** — dict lookup for installed skills (`worker/loop.py` ~627): `next(s for s in ctx.installed_skills if s["name"] == skill_name)` is O(n) per skill task; build `{s["name"]: s for s in installed}` once when skills are discovered and store it in context

- [x] **85e** — missing DB index on `facts(category, session)` (`store.py`): `get_facts()` filters with `WHERE category != 'user' OR session = ?` but there is no index covering these columns; every planner, reviewer, and curator call does a full table scan; fix: add `CREATE INDEX IF NOT EXISTS idx_facts_cat_sess ON facts(category, session)` to the schema init in `create_tables()`

### M86: Maintainability

- [x] **86a** — named constants for task types and review statuses (`brain.py`): `"exec"`, `"msg"`, `"skill"`, `"search"`, `"replan"` and `"ok"`, `"replan"` appear as magic strings in the schema, `validate_plan`, `validate_review`, and `worker/loop.py`; define `TASK_TYPES`, `REVIEW_STATUSES`, `CURATOR_VERDICTS` as module-level constants

- [x] **86b** — shared CLI HTTP helper (`cli/user.py`, `cli/stats.py`, `cli/env.py`, `cli/session.py`): all four modules duplicate the pattern `load_config()` → token → `httpx.get(..., headers={"Authorization": "Bearer ..."})` with identical error handling; extract to `cli/_http.py` as `cli_get(args, path, params=None)` covering auth, `ConnectError`, and `HTTPStatusError` uniformly

- [x] **86c** — extract `_startup_recovery()` from `lifespan()` (`main.py`): the startup recovery block (~lines 155-194, ~40 lines) is self-contained logic that re-enqueues interrupted tasks; extracting it reduces `lifespan()` from ~87 to ~50 lines and makes it independently testable

- [x] **86d** — common pattern in `build_*_messages()` (`brain.py`): nine functions share the same `[system, user]` return literal; extracted `_build_messages(system_prompt, user_content)` private helper; all nine `return [{"role": "system", ...}, {"role": "user", ...}]` blocks replaced with a single call; no tests changed

- [x] **86e** — split `_render_plan_status()` (`cli/__init__.py`): ~209-line function that inlines rendering logic for all task types (msg, exec, skill, search, replan); extract per-type renderers (`_render_msg_task`, `_render_exec_task`, etc.) so each type can be modified and tested independently

- [x] **86f** — deduplicate `_skill_list()` / `_connector_list()` (`cli/skill.py:80-109`, `cli/connector.py:207-235`): the two functions are ~95% identical (~50 lines each), differing only in item type label and discover function; extract `_list_plugins(discover_fn, item_type: str)` in `cli/plugin_ops.py` and delegate from both

- [x] **86g** — test coverage gaps:
  - `cli/env.py`: `_read_lines()`, `_parse_key()`, `_parse_value()`, `_write_lines()` have no unit tests (only integration coverage); add cases for: file not found, empty lines, comments, quoted values with `=` inside, values with `#` that are not comments
  - `cli/render.py`: `render_step_usage()` and `render_cancel_done()` are untested; add unit tests

- [x] **86h** — `cli/connector.py` unused import: `import logging` and `log = logging.getLogger(__name__)` are defined but never referenced; remove both

- [x] **86i** — `cli/stats.py` redundant `getattr` defaults: `since = getattr(args, "since", 30)` etc. repeat the same defaults already declared in argparse (`add_argument("--since", default=30, ...)`); replace with direct attribute access `args.since`, `args.by`, `args.session`

- [x] **86j** — unnamed magic literals:
  - `kiso/webhook.py:127`: `timeout=10.0` → extract `_WEBHOOK_HTTP_TIMEOUT = 10.0`
  - `kiso/worker/loop.py:1339`: `min(extend, 3)` → extract `_MAX_EXTEND_REPLAN = 3`

- [x] **86k** — verbose per-item learning log (`worker/loop.py` ~463): `log.info("Learning saved: %s", item[:100])` fires for every fact item during consolidation; for sessions with many facts this floods the log; demote to `log.debug()`

- [x] **86l** — inconsistent error output destination in CLI: `cli/__init__.py` sends some errors to `sys.stderr` while all other CLI modules (`stats.py`, `env.py`, `session.py`, `user.py`) print errors to stdout; standardize on `sys.stderr` for error messages across all CLI modules

### M87: Robustness + Config safety + Test gaps

- [x] **87a** — unsafe JSON parse in CLI polling loop (`cli/__init__.py` ~763): `len(_json.loads(task["llm_calls"]))` is called without a try/except; a corrupted `llm_calls` value in the DB would crash the entire CLI render loop mid-session; wrap with `(ValueError, TypeError)` matching the same guard already used in `render_llm_calls()`
  - [x] `tests/test_cli.py`: `test_poll_status_corrupted_llm_calls_does_not_crash` — verifies that a non-JSON `llm_calls` value renders without raising

- [x] **87b** — unhandled file I/O in `_init_kiso_dirs()` (`kiso/main.py` ~56): `mkdir()`, `read_text()`, and `write_text()` can raise `OSError`/`PermissionError`; server startup would crash instead of logging a warning and continuing; wrap directory creation and per-file sync in error-handling blocks
  - [x] `tests/test_main.py`: 3 tests in `TestInitKisoDirs` — `mkdir` OSError, `importlib.resources` OSError, per-file read OSError

- [x] **87c** — bare `int()`/`float()` casts on config settings (`kiso/worker/loop.py`, `kiso/main.py`): all `int(config.settings["exec_timeout"])` etc. are type-checked at load time but not range-validated; out-of-range values (`exec_timeout=0`, `fact_decay_rate=99.0`) cause silent pathological behavior; add `setting_int(settings, key, *, lo, hi)` and `setting_float(settings, key, *, lo, hi)` helpers to `kiso/config.py` that clamp and warn, then replace all bare casts

- [x] **87d** — bare `except Exception` swallows `asyncio.CancelledError` (`kiso/worker/loop.py` ~218, 296, 304): three background coroutines (`_run_curator`, `_run_decay`, `_run_archive`) catch `Exception` without re-raising `CancelledError`; in Python 3.8+ `CancelledError` is a `BaseException` so it is not caught here, but adding an explicit `except asyncio.CancelledError: raise` before the broad handler makes the intent explicit and guards against future regressions
  - [x] `tests/test_worker.py`: `TestCancelledErrorPropagation` — 3 regression tests verifying propagation from `_run_decay`, `_run_archive`, `_run_curator`

- [x] **87e** — LLM consolidation result unbounded and unchecked (`kiso/brain.py` ~958): result list has no item-count cap (a runaway LLM could return thousands of items); `category` field accepts arbitrary strings without validation, allowing unknown or `null` values to bypass session-scoping logic in `loop.py`; add `_MAX_CONSOLIDATION_ITEMS = 200` cap with truncation warning, and a `_VALID_FACT_CATEGORIES` whitelist that normalizes unknown/null categories to `"general"`

- [x] **87f** — store test gaps (`tests/test_store.py`): six missing cases: (1) `decay_facts` with multiple independent facts — each decayed by correct amount; (2) `decay_facts` with `decay_rate=0.0` — rows matched but confidence unchanged; (3) `archive_low_confidence_facts` — all fields correctly preserved in `facts_archive`; (4) same — archive table stays empty when nothing is below threshold; (5) `save_facts_batch` with non-default `confidence` value persisted exactly; (6) `save_facts_batch` with mixed complete and minimal dicts in one call

---

## Milestone 88: Tech debt — dead code + missing constants

Two small items missed during M86 (maintainability). Zero behavior change, zero risk.

- [x] **88a** — unused `tasks: list` parameter in all task handlers (`kiso/worker/loop.py`): `_handle_msg_task`, `_handle_exec_task`, `_handle_skill_task`, `_handle_search_task`, `_handle_replan_task` all accept `tasks` but never read it; replace with `is_final: bool` in all five signatures; dispatch computes `is_final = i == len(tasks) - 1` before the handler call
  - [x] `tests/test_worker.py`: all 9 `_handle_*_task` calls updated to pass explicit `True`/`False` instead of a stale `tasks` list

- [x] **88b** — magic substatus string literals (`kiso/worker/loop.py`): `"translating"`, `"executing"`, `"reviewing"`, `"composing"` appear scattered across handlers and `_fast_path_chat`; extract as module-level constants `_SUBSTATUS_TRANSLATING`, `_SUBSTATUS_EXECUTING`, `_SUBSTATUS_REVIEWING`, `_SUBSTATUS_COMPOSING` analogous to the `TASK_TYPE_*` and `REVIEW_STATUS_*` constants added in M86a
  - [x] `tests/test_worker.py`: `TestSubstatusConstants` — verifies all four constant values and uniqueness

---

## Milestone 89: Performance — DB and async I/O

Five independent items that reduce latency without behavioral change. Ordered by fix complexity (easiest first).

- [x] **89a** — missing index on `messages(user)` (`store.py`): `get_sessions_for_user()` filters with `WHERE m.user = ?` on a JOIN but no index covers `messages(user)`; every `/sessions` request does a full table scan; add `CREATE INDEX IF NOT EXISTS idx_messages_user ON messages(user)` to `create_tables()`
  - [x] `tests/test_store.py`: `test_idx_messages_user_exists` — verifies index is present in `sqlite_master`

- [x] **89b** — N+1 queries in `create_session` / `upsert_session` (`store.py`): both functions called `get_session()` twice; replaced with `INSERT OR IGNORE` + single final SELECT; `upsert_session` uses `cur.rowcount == 1` to detect creation without an extra query
  - [x] `tests/test_store.py`: `test_upsert_session_creates_new`, `test_upsert_session_updates_existing`, `test_upsert_session_created_flag_idempotent`

- [x] **89c** — `get_facts()` without LIMIT in `build_messenger_messages` (`brain.py`): added `limit: int | None = None` parameter to `get_facts()` (uses `LIMIT -1` internally for no-cap, unifying 4 branches to 2); messenger call uses `_MAX_MESSENGER_FACTS = 50` constant — the consolidation loop in `loop.py` intentionally keeps no cap to compare against `knowledge_max_facts`
  - [x] `tests/test_store.py`: `test_get_facts_limit_caps_results`, `test_get_facts_limit_with_session`, `test_get_facts_limit_none_returns_all`
  - [x] `tests/test_brain.py`: `TestMessengerFactsConstant.test_max_messenger_facts_value` — verifies `_MAX_MESSENGER_FACTS == 50`

- [x] **89d** — blocking `write_text()` in worker async context (`worker/utils.py`): `_write_plan_outputs()` made `async`; `write_text` offloaded via `asyncio.get_running_loop().run_in_executor(None, path.write_text, content, "utf-8")`; all 3 call sites in `loop.py` updated with `await`; `TestWritePlanOutputs` and `TestCleanupPlanOutputs` tests updated to `async`
  - [x] `tests/test_worker.py`: `TestWritePlanOutputs.test_non_ascii_content_written_as_utf8` — verifies non-ASCII output is byte-for-byte correct UTF-8

- [x] **89e** — blocking `subprocess.run()` in `_ensure_sandbox_user()` (`worker/utils.py`): synchronous logic extracted to `_ensure_sandbox_user_sync()`; outer function made `async` dispatching via `run_in_executor`; call site in `loop.py` updated with `await`; `TestPerSessionSandbox` sandbox-user tests updated to `async`

---

## Milestone 90: Security hardening

Three independent items from the security review. Ordered by severity.

- [x] **90a** — custom `_RateLimiter` token-bucket (asyncio.Lock, no external deps) in `main.py`; limits: `/msg` 20/min per user, `/admin/*` 5/min per user, `/pub` 60/min per IP, `/sessions/{session}/cancel` 20/min per session; `_rate_limiter` singleton with `reset()` method; autouse fixture in conftest.py resets buckets between tests; `tests/test_rate_limiter.py` covers unit (allow/block/reset/isolation) + integration (429 on /msg after 20, per-user isolation)

- [x] **90b** — `_ENV_KEY_PREFIXES = ("KISO_", "OPENAI_", "ANTHROPIC_", "OLLAMA_")` allowlist + `_ENV_VALUE_MAX_LEN = 1024` in `main.py`; `post_reload_env` validates each key (prefix check) and value (no `\r`/`\n`, max 1024 chars), logs each applied key and summary; response changed from `keys_loaded` to `keys_applied`/`keys_skipped`; updated `cli/env.py` output, `docs/api.md`, all test assertions; tests cover: disallowed prefix, embedded CR (via mock), overlength value, `keys_skipped` field always present, rate limit 429

- [x] **90c** — `GET /status/{session}` now requires `user: str = Query(...)` param; admin bypasses ownership check; non-admin: `session_owned_by(db, session, username)` → `SELECT 1 FROM messages WHERE session=? AND user=? LIMIT 1` (O(log n) via new `idx_messages_session_user` index); 403 if not owned; `_poll_status` and `_slash_status` propagate `user` param; all existing `/status` test calls updated; new tests: 403 on unowned, 200 on owned, admin bypass, 422 on missing param; `session_owned_by` unit tests in test_store.py

---

## Milestone 91: Worker refactor — complexity reduction

Structural improvements to `worker/loop.py`. Higher risk than M88; each item should be implemented with a full test run after each sub-item.

- [x] **91a** — duplicated error recovery in `_process_message` (`loop.py` ~1477-1504): extracted `async def _handle_plan_error(db, config, session, msg_id, error_text)` and delegated from `asyncio.TimeoutError` and `PlanError` handlers; 2007 tests pass

- [x] **91b** — `_run_planning_loop` 235-line body with six termination cases: extracted `_handle_loop_success`, `_handle_loop_cancel`, `_handle_loop_failure(deliver_webhook=True)` — replan-timeout/PlanError paths use `deliver_webhook=False`; 2007 tests pass

- [x] **91c** — `ctx.plan_outputs` mutated in-place by handlers: added `plan_output: dict | None = None` to `_TaskHandlerResult`; extracted `_make_plan_output(index, task_type, detail, output, status)` helper replacing 4 inline dicts; all four handlers return `plan_output` in result; exec/search retry loops track output in `local_plan_output` and call `_write_plan_outputs` with `ctx.plan_outputs + [local_plan_output]`; `_execute_plan` appends `result.plan_output` after each handler; tests: `TestMakePlanOutput` (3), `TestHandlePlanError` (3), `TestHandleLoopFailure` (4), skill/exec success plan_output assertions; 2018 tests pass

---

## Milestone 92: Polish and gap-fill

Four independent items: two structural code smells, one latent bug, one new config setting.

- [x] **92a** — deferred local imports: `from kiso.sysenv import invalidate_cache` is buried inside `_process_message` (loop.py line 1584) while all other `kiso.sysenv` symbols are imported at module top-level (line 65); likewise `from datetime import ...` and `from kiso.stats import ...` are deferred inside `get_stats` in main.py. Move all to module-level. Tests: grep-based test asserting no `from kiso.sysenv import` inside a function body in loop.py.

- [x] **92b** — nested `_group_by_category` closure in brain.py (line 368): defined inside `build_planner_messages`, prevents direct unit testing; extract to module-level `_group_facts_by_category(fact_list, label_session=False)`. Tests: `TestGroupFactsByCategory` — all four categories, unknown category falls to `general`, `label_session=True` appends `[session:x]`, empty input.

- [x] **92c** — latent bug: pre-replan cancel check (loop.py lines 1351–1353) sets plan to `"cancelled"` and breaks without calling `_handle_loop_cancel`, so no cancel-summary message is saved, no webhook delivered, and `cancel_event` is never cleared — diverging from the main cancel branch. Fix: delegate to `_handle_loop_cancel`. Tests: `TestPreReplanCancelFix` — patches `save_message` to set cancel_event on the replan notification, asserts plan status `"cancelled"`, system message saved, `cancel_event` cleared.

- [x] **92d** — duplicate messenger timeout reads: `_msg_timeout = setting_int(config.settings, "planner_timeout")` appears at two call sites (loop.py lines 370 and 608) — the messenger borrows the planner timeout with no dedicated setting. Add `messenger_timeout` config key (defaulting to `planner_timeout` value for backward compatibility); read it once in `run_worker` alongside other timeouts and thread via `_PlanCtx.messenger_timeout`; `_fast_path_chat` and `_handle_msg_task` read from ctx/param instead of config. Tests: `TestMessengerTimeout` — slow `_msg_task` times out at 1s, task output records `"1s"`; `_handle_msg_task` respects `ctx.messenger_timeout`.

---

## Milestone 93: Correctness and dead code

Four independent items: two correctness bugs, one dead-code removal, one async-consistency fix.

- [x] **93a** — dead code: `get_unprocessed_messages` (store.py) is not called by any production code — only tests use it as a DB inspection helper (checking `len == 0`). Risk: future dev calling it instead of `get_unprocessed_trusted_messages` would silently re-enqueue untrusted messages. Fix: remove from store.py; replace test usages with direct SQL or `get_unprocessed_trusted_messages`. Tests: assert `not hasattr(kiso.store, "get_unprocessed_messages")`.

- [x] **93b** — `get_sessions_for_user` and `get_all_sessions` declare `list[SessionDict]` but SELECT only 4 of 7 columns (`webhook`, `summary`, `created_at` missing) — `row["webhook"]` would raise `KeyError` at runtime. Fix: `SELECT *` in both (consistent with every other store function). Tests: create session with webhook+description, assert returned dict contains all SessionDict keys.

- [x] **93c** — bug: `_handle_search_task` writes `update_task("done")` and audit-logs `"done"` *before* `_run_review_step`; during retries the task oscillates done→searching→done. Fix: defer `update_task("done")` and final audit log to after the reviewer's `break`; keep `_append_calls` per-iteration. Exec handler is the model: it writes status based on objective result (exit code); search has no exit code — reviewer verdict IS the final status. Tests: `TestHandleSearchTask` — assert task status is not `"done"` during retry (still "running"/"searching"), assert `"done"` only after reviewer returns ok.

- [x] **93d** — `_cleanup_plan_outputs` runs `exists()` + `unlink()` synchronously in the async event loop while sibling `_write_plan_outputs` uses `run_in_executor`. Fix: make `_cleanup_plan_outputs` async, offload unlink via executor; add `await` at all 5 call sites in loop.py. Tests: `assert asyncio.iscoroutinefunction(_cleanup_plan_outputs)`.

### Post-M93 simplify fixes

- [x] **timing bug** — `_handle_search_task` computed `task_duration_ms` inside the `while True` retry loop (`t0` reset each iteration), so `audit.log_task` on the happy path reported only the last iteration's duration. Fix: add `t0_total = time.perf_counter()` before the loop; compute `task_duration_ms = int((time.perf_counter() - t0_total) * 1000)` after the `break`. Tests: `test_search_audit_duration_spans_retries` — mocks `time.perf_counter` with controlled values across 1 retry, asserts `audit.log_task` receives the total span (2000 ms), not the last-iteration span (1000 ms).

- [x] **dead variable** — `original_run_review = None` was assigned but never read in `test_search_status_not_done_during_retry`. Removed.

- [x] **test helper** — 3 identical `SELECT COUNT(*) FROM messages WHERE processed = 0` + assert blocks in `TestRunWorker` extracted to `_assert_all_messages_processed(db)`.

- [x] **hardcoded key tuple** — `("session", "connector", "description", "updated_at", "webhook", "summary", "created_at")` duplicated in 2 tests; replaced with `SessionDict.__annotations__` so the tests stay in sync if new fields are added.

---

## Milestone 94: Dead code, missing constant, timeout gaps

Four independent items. 94a and 94b are zero-behavior-change; 94c and 94d fix real correctness bugs.

- [x] **94a** — missing `_SUBSTATUS_SEARCHING` constant (`worker/loop.py`): M88b extracted four substatus magic strings into named constants but `_handle_search_task` still uses the raw string `"searching"` — the only remaining magic substatus literal. Fix: define `_SUBSTATUS_SEARCHING = "searching"` alongside the other four; replace the literal at the single call site.
  - [x] `tests/test_worker.py`: import `_SUBSTATUS_SEARCHING`; extend `TestSubstatusConstants` with `test_substatus_searching_value` and include it in the distinctness check.

- [x] **94b** — dead parameters in `_process_message` (`worker/loop.py`): `queue: asyncio.Queue` and `idle_timeout: int` are declared but never referenced in the function body — the idle loop in `run_worker` owns them; `_process_message` does not. Fix: remove both from the signature and the single call site in `run_worker`.
  - [x] `tests/test_worker.py`: remove those arguments from any direct `_process_message` calls.

- [x] **94c** — no timeout on messenger in cancel/failure handlers (`worker/loop.py`): `_handle_loop_cancel` and `_handle_loop_failure` call `await _msg_task(...)` without `asyncio.wait_for`. If the messenger LLM hangs, `cancel_event` is never cleared and server shutdown blocks until force-cancellation. `messenger_timeout` exists everywhere else but was missing here. Fix: add `messenger_timeout: int` to both functions; wrap `_msg_task` with `wait_for`; fall back to raw detail text on `TimeoutError`; thread the value from `_run_planning_loop` (6 call sites).
  - [x] `tests/test_worker.py`: `TestHandleLoopFailure.test_failure_messenger_timeout_falls_back` and `TestHandleLoopCancel.test_cancel_messenger_timeout_falls_back` — mock `_msg_task` to raise `asyncio.TimeoutError`, assert fallback text is saved.

- [x] **94d** — summarizer receives unbounded message count (`worker/loop.py`): `_run_summarizer` calls `get_oldest_messages(db, session, limit=msg_count)` where `msg_count` is the total in the session — no cap. For sessions with 300+ messages this can overflow the LLM context window. `summarize_threshold` gates when to run but does not limit how many messages are sent. Fix: add `summarize_messages_limit = 100` to `SETTINGS_DEFAULTS` and `CONFIG_TEMPLATE`; use `min(msg_count, limit)` in `_run_summarizer`.
  - [x] `tests/test_worker.py`: `test_summarizer_respects_messages_limit` — 5 messages, limit=3, assert `get_oldest_messages` called with `limit=3`; `test_summarizer_uses_all_when_below_limit` — 2 messages, limit=5, assert called with `limit=2`.

### Post-M94 simplify fixes

- [x] **`_msg_task_with_fallback` extraction** — the identical `wait_for + except → fallback` block in `_handle_loop_cancel` and `_handle_loop_failure` extracted to `_msg_task_with_fallback(config, db, session, detail, goal, timeout)`. Signature uses `int | float` for timeout (matches `asyncio.wait_for`). Tests: `TestMsgTaskWithFallback` — 4 cases: success, `LLMError` fallback, `MessengerError` fallback, timeout fallback; each asserts the exact return value.

- [x] **Timeout test assertions strengthened** — `test_failure_messenger_timeout_falls_back` and `test_cancel_messenger_timeout_falls_back` changed from `assert rows` (presence-only) to content checks: `"The plan failed: goal"` and `"The user cancelled the plan: goal"` respectively, verifying the raw `_build_*_summary` text is saved, not an empty or wrong value.

- [x] **`test_cancel_saves_system_message`** added to `TestHandleLoopCancel` — happy-path parity with `TestHandleLoopFailure.test_saves_system_message`; asserts the LLM-generated cancel text is persisted as a system message.

- [x] **`timeout=0` → `timeout=0.001`** in timeout tests — avoids relying on event-loop scheduling detail for the cancellation to fire.

- [x] **`TestMsgTaskWithFallback` — removed unused `db` fixture** — `_msg_task` is fully patched in all 4 tests so no real DB is needed; pass `None` explicitly to eliminate per-test SQLite I/O.

- [x] **`asyncio.sleep(999)` → `asyncio.Event().wait()`** in the 3 new hanging-coroutine stubs — no timer-heap entry allocated; clearer intent ("wait forever for something that never arrives").

---

## Milestone 95: Verbose mode — input/output visual differentiation

**Problem**: with `/verbose-on`, input messages and LLM response are rendered in the same style inside a single panel — no visual hierarchy, impossible to tell at a glance where input ends and output begins.

**Fix**: in `render_llm_calls_verbose()` (`cli/render.py`), differentiate input from output using Rich markup:

- Input role labels (`[system]`, `[user]`) → `dim cyan`
- Input content → `dim` (lower visual weight)
- Separator line (`─── response ───…`) → `bold green` (clear visual break)
- Response content → normal weight (stands out against dimmed input)
- Escape all user content with `rich.markup.escape` to prevent accidental Rich tag interpretation

- [x] `cli/render.py`: update `render_llm_calls_verbose()` — add `rich.markup.escape`, dim input, bold green separator, normal response
- [x] `tests/test_render.py`: add `test_render_llm_calls_verbose_separator` (separator present in output) and `test_render_llm_calls_verbose_escapes_markup` (content with bracket patterns rendered literally)

---

## Milestone 96: Reviewer — warning vs error distinction

**Problem**: `reviewer.md` line 16 says: *"If output contains warnings about missing configuration (env vars, API keys, tokens), mark as replan even if the command succeeded."* This creates a retry spiral when `kiso skill install` succeeds (exit 0, "Skill installed successfully") but prints a warning about an optional env var. The M46 flow already handles env var collection before install — the warning is informational, not a failure. Observed: 3 wasted replan cycles (~10K tokens overhead) before timeout.

Root cause: the rule treats all config warnings as failures regardless of whether the task's `expect` was satisfied. This contradicts rule 10 ("Sole criterion is the task's `expect`").

**Fix**: remove the blanket warning rule (line 16) from `reviewer.md`. Replace with a nuanced rule:

> Warnings in stdout/stderr are informational. They do NOT override a successful exit code + satisfied `expect`. Mark replan for a warning ONLY if the task's `expect` explicitly requires that configuration to be present (e.g. expect says "no warnings").

- [x] `kiso/roles/reviewer.md`: replaced blanket warning rule (line 16) with nuanced rule: warnings are informational, replan only if `expect` explicitly requires absence of warnings
- [x] `tests/test_brain.py`: `TestReviewerWarningVsError` — 3 prompt assertion tests:
  1. `test_prompt_warnings_are_informational` — "informational" present in prompt
  2. `test_prompt_no_blanket_replan_on_warning` — old "mark as replan even if command succeeded" gone
  3. `test_prompt_explicit_expect_required_for_warning_replan` — new rule references "no warnings" in expect
- [x] `docs/llm-roles.md`: added **Warnings** rule to "Rules in the Default Prompt" section
- [x] `tests/live/test_roles.py`: 3 live tests in `TestReviewerLive`:
  1. `test_warning_with_satisfied_expect_returns_ok` — exit 0 + warning + expect satisfied → `ok`
  2. `test_warning_with_explicit_no_warnings_expect_returns_replan` — exit 0 + expect "no warnings" + warning → `replan`
  3. `test_nonzero_exit_with_warning_returns_replan` — exit 1 + warning → `replan`

---

## Milestone 99: `summary.sh` — verbose session compactor

**Purpose**: debug tool for analysing long verbose Kiso sessions. Parses `/verbose-on` output, deduplicates repeated LLM prompts via SHA1, redacts API keys, and produces compact JSON with compression stats.

**Features**:
- Text blocks and LLM call boxes (`╭─ role → model ╰─`) parsed and categorised
- SHA1 dedup: identical content stored once as a "def", referenced by ID in events
- Head/tail preview for long blocks (14 head + 8 tail lines)
- API key redaction (OpenAI `sk-`, GitHub `ghp_`, Slack `xox*-`, GitLab `glpat-`, Bearer tokens)
- `--pretty` / `--no-pretty` (auto: pretty on TTY, minified when piped)
- `--out FILE` to save output
- `--no-color` to disable terminal colors
- stdout = clean JSON only; decorations go to stderr (pipe-safe)
- Stats summary printed to stderr after output

- [x] `summary.sh`: script with embedded Python parser
- [x] `docs/testing.md`: "Debugging verbose sessions" section — usage, JSON structure, what to look for

---

## Milestone 97: Prompt compression — contextual rules, token reduction

**Problem**: the planner prompt is 61 lines (~2,500 chars) sent in full on every call. Many rules are contextual (kiso management commands, user management, plugin installation) but always included. In a typical flow the planner is called 2-4 times per user message → 5K-10K tokens of static prompt repeated. The trace shows planner calls at 2,678–3,866 input tokens each.

### 97a. Planner — contextual rule blocks

Split `planner.md` into a base template (~30 lines: core structure, general rules) and tagged appendix blocks injected only when relevant:

- `## Kiso Commands` (lines 38-43): inject when the user message or session context mentions kiso management (skill/connector/env/instance/user)
- `## User Management` (lines 45-48): inject when message contains user-related keywords
- `## Plugin Installation` (lines 50-58): inject when message contains install/plugin/skill/connector keywords

Detection: simple keyword match in `build_planner_messages()` on the user message string. False positives are harmless (extra context), false negatives degrade gracefully (planner has less guidance, may ask for clarification).

- [x] `kiso/roles/planner.md`: extracted lines 38-58 into 3 appendix files
- [x] `kiso/roles/planner-kiso-commands.md`: kiso management commands (skills, connectors, env, instance, users)
- [x] `kiso/roles/planner-user-mgmt.md`: user management rules (protection, --skills, ask-then-add)
- [x] `kiso/roles/planner-plugin-install.md`: plugin installation workflow (MANDATORY 7-step flow)
- [x] `kiso/brain.py`: `build_planner_messages()` — keyword-based injection via `_load_system_prompt()` for each appendix. Keywords: `{skill, connector, env, instance, kiso}` → commands; `{user, utente, admin, alias}` → user-mgmt; `{install, installa, plugin, add}` → plugin-install.
- [x] `tests/test_brain.py`: `TestPlannerContextualRules` — 5 tests: generic message has no appendix; skill/user/install keywords inject correct appendix; base prompt always present. Existing M45/M46/M73c tests updated to read from appendix files.

### 97b. Prompt wording tightening

Tighten role prompts for conciseness without losing meaning. Actual reduction:

- **planner.md**: 61→35 lines. Merged null-constraint rules (msg/replan/search) into one line; merged investigation + reference-doc rules; merged retrieval + pre-action rules. Base prompt: ~1125 tok (was ~1841 tok = **-39%**).
- **searcher.md**: 21→15 lines. Rules 16-20 consolidated into a single rule.
- **curator.md**: 21→14 lines. Merged MUST-have rules into one line; compacted action descriptions.

- [x] `kiso/roles/planner.md`: compressed general rules section
- [x] `kiso/roles/searcher.md`: consolidated rules 16-20
- [x] `kiso/roles/curator.md`: compacted descriptions and rules
- [x] `uv run pytest`: 2060 passed, 65 skipped

---

## Milestone 98: Verbose mode — thinking/reasoning content display

**Problem**: many models (DeepSeek, QwQ, Claude) return reasoning/thinking content, but the verbose panel shows it as raw text mixed with the response (for `<think>` tags) or drops it entirely (for API-level `reasoning_content` fields). The user asked to see thinking content in verbose mode to debug agent behavior.

### 98a. LLM layer — extract thinking/reasoning from API response

`llm.py:244` currently extracts only `content = data["choices"][0]["message"]["content"]`. Models may also return:

1. **`reasoning_content`** field: `data["choices"][0]["message"]["reasoning_content"]` (DeepSeek API, OpenRouter)
2. **`<think>`/`<thinking>` tags** embedded in `content` (DeepSeek via OpenRouter, some open models)

Fix: extract both and store separately in the usage entry.

- [x] `kiso/llm.py`: after line 244, extract `reasoning = data["choices"][0]["message"].get("reasoning_content", "")`. If empty, use `extract_thinking(content)` from render.py (move regex to a shared util). Store as `"thinking"` field in the usage entry (line 263-270).
- [x] `kiso/text.py`: move `_THINK_RE` and `extract_thinking()` from `cli/render.py` to a shared module; `render.py` re-exports from there.
- [x] `tests/test_llm.py`: `TestThinkingExtraction` — 5 tests: reasoning_content field, `<think>` tags, tag precedence, no thinking, thinking in get_usage_since.

### 98b. Verbose renderer — display thinking panel

`render_llm_calls_verbose()` currently shows `response` as-is. Add thinking display.

- [x] `cli/render.py`: in `render_llm_calls_verbose()`, thinking block rendered between separator and response, styled yellow. Label: `"🤔 reasoning"`.
- [x] `tests/test_render.py`: 3 tests — thinking panel present, absent, ASCII mode.

### 98c. Non-verbose mode — thinking for messenger output

Currently `render_msg_output()` (line 388-405) extracts thinking from `<think>` tags in the messenger response text. With 98a, thinking is already separated at the LLM layer. Update `_render_msg_task` to use the stored thinking field instead of regex extraction from content.

- [x] `cli/__init__.py`: in `_render_msg_task()`, extracts thinking from last LLM call and passes to renderer
- [x] `cli/render.py`: `render_msg_output()` — accepts optional `thinking` parameter; skips regex extraction when provided
- [x] `tests/test_render.py`: 3 new tests for stored thinking parameter

---

## Milestone 100: summary.sh — compact JSON by default ✅

**Problem**: `summary.sh` auto-detects TTY and defaults to pretty-printed JSON (`indent=2`) when output goes to the terminal. The user wants compact JSON by default in all cases — pretty only when explicitly requested with `--pretty`.

**Fix**: flip the auto-detection default. Compact is the new default; `--pretty` forces indented output.

- [x] `summary.sh`: change auto-detection block — remove `[[ -t 1 ]]` auto-pretty, set `PRETTY=0` as the unconditional default when no flag is given
- [x] `summary.sh`: update `--help` text to reflect new default ("minified by default, use --pretty for indented output")

---

## Milestone 101: Verbose mode — timestamps + deduplicated incremental panels ✅

### 101a. Timestamps in verbose panels

**Problem**: verbose panels show role, model, and tokens but no timing. Impossible to evaluate latency per LLM call or total duration.

**Fix**: record `time.time()` in each LLM usage entry, render it in the panel title.

- [x] `kiso/llm.py`: in `entries.append({...})`, add `"ts": time.time()` (`time` already imported)
- [x] `cli/render.py`: in `render_llm_calls_verbose()`, read `c.get("ts")` and format as `HH:MM:SS` appended to panel title — e.g. `" worker → deepseek-v3.2 (132→1) 14:32:05 "`; omit if `ts` absent (backward compat)

### 101b. Deduplicate incremental verbose panels

**Problem**: during incremental rendering (`_append_calls`), the CLI re-renders ALL LLM call panels for a task each time the call count changes. A typical exec task goes through 3 rendering cycles (translator appended, reviewer appended, task done), causing the translator box to appear **3 times** and the reviewer box **2 times** in the terminal output.

**Root cause**: `_render_plan_status` (cli/__init__.py) tracks task changes via `task_key = (status, review_verdict, substatus, llm_call_count)`. When `llm_call_count` changes, the "status unchanged" branch calls `render_llm_calls_verbose()` on the full `llm_calls` JSON. When status changes (e.g. running → done), `_render_other_task` also calls `render_llm_calls_verbose()` on the full set. Neither path tracks which calls were already printed.

**Fix**: track the number of already-rendered LLM calls per task in `_PollRenderState`. On incremental updates, render only new calls. On task completion, skip verbose re-render if all calls were already shown.

- [x] `cli/__init__.py`: add `verbose_shown: dict` to `_PollRenderState` (maps `task_id → number_of_verbose_calls_already_rendered`), initialized to `{}` at instantiation
- [x] `cli/__init__.py`: in the "status unchanged" branch, compute `already = state.verbose_shown.get(tid, 0)`, pass `skip=already` to `render_llm_calls_verbose()`, update `state.verbose_shown[tid] = llm_call_count`
- [x] `cli/__init__.py`: in `_render_other_task()`, same logic — pass `skip=already`, update `verbose_shown[tid]` after render
- [x] `cli/__init__.py`: clear `verbose_shown` on plan change (alongside `seen.clear()`)
- [x] `cli/render.py`: add `skip` parameter to `render_llm_calls_verbose(llm_calls_json, caps, skip=0)` — filters out the first `skip` verbose calls before rendering

---

## Milestone 102: Plugin discovery — cross-type search, planner prompt, completions ✅

### 102a. Planner prompt — enforce `curl` registry for ambiguous plugin requests ✅

**Problem**: user asks "installare funzionalità browser", planner generates `kiso connector search browser` — searches only connectors, misses the browser skill. The `planner-plugin-install.md` rule says to `curl <registry_url>` for ambiguous requests, but the planner (minimax-m2.5) ignored it and used `kiso connector search` instead.

**Contributing factor**: the first planner call (D00007 in the trace) also failed validation — produced a plan with only an exec task, violating "last task must be msg or replan". The retry (D00008) succeeded but still chose `kiso connector search` over `curl`.

**Fix**: make the rule unambiguous. Add a NEVER clause that explicitly prohibits using `kiso connector search` or `kiso skill search` for plugin discovery when the type is unknown.

- [x] `kiso/roles/planner-plugin-install.md`: rewrite rule 2 — "Ambiguous request without explicit 'skill' or 'connector': exec `curl <registry_url>` — NEVER use `kiso connector search` or `kiso skill search` for initial discovery (they only search one type and will miss results from the other). Then replan."
- [x] `tests/test_brain.py`: assert `planner-plugin-install.md` contains "NEVER" in context of `connector search` or `skill search`

### 102b. CLI — cross-type hint in empty search results ✅

**Problem**: `kiso connector search browser` → "No connectors found." with no hint that "browser" exists as a skill. The user has no guidance.

**Fix**: when search returns empty, also check the other type in the registry and suggest if found.

- [x] `cli/plugin_ops.py`: new function `cross_type_hint(registry: dict, current_type: str, query: str) -> str | None` — searches the other type ("skills" if current is "connectors" and vice versa), returns hint string or None
- [x] `cli/connector.py`: `_connector_search()` — if no results, fetch registry, call `cross_type_hint(registry, "connectors", query)`, print hint (e.g. `"No connectors found. Did you mean \`kiso skill search browser\`?"`)
- [x] `cli/skill.py`: `_skill_search()` — symmetric for connectors
- [x] `tests/test_cli_connector.py`: test cross-type hint shown / not shown
- [x] `tests/test_cli_skill.py`: symmetric tests

### 102c. Tab completion for `search` subcommand ✅

**Problem**: `kiso skill search <TAB>` offers no completions. The `search)` case is missing from the bash/zsh completion `case` statements for both `skill` and `connector`.

**Fix**: complete from locally installed names. Remote registry completion would add latency.

- [x] `kiso/completions/kiso.bash`: add `search)` case under both `skill` and `connector` — `COMPREPLY=($(compgen -W "$(_kiso_skill_names)" -- "$cur"))` / `$(_kiso_connector_names)`
- [x] `kiso/completions/kiso.zsh`: symmetric change
- [x] `completions/kiso.bash` + `completions/kiso.zsh`: sync top-level copies

---

## Milestone 103: Replan polling — race condition fix ✅

**Problem**: during a self-directed replan, the CLI exits the polling loop before the new plan starts. The user sees the prompt return silently — the new plan executes in the background with no terminal output.

**Root cause**: two-step race in `_run_planning_loop` (loop.py lines 1424-1433):

```
Step 1: await update_plan_status(db, current_plan_id, "done")   ← old plan finalized
Step 2: new_plan_id = await create_plan(db, session, ...)        ← new plan created
```

Between these two `await`s, the CLI polls via `/status`. `get_plan_for_session` returns the old plan (new plan doesn't exist yet). `_should_stop_polling` sees `plan.status == "done"` and `status not in ("running", "replanning")` → enters the `else` branch (line 559) and returns `True, 0` **immediately** — no stability window, unlike the `"failed"` case which has a 30-poll (~5s) buffer.

The "done" case was not protected because replans via reviewer-triggered replan set the old plan to "failed" (which has the buffer), but self-directed replans (the `replan` task type) set it to "done" — and "done" was assumed to always mean "genuinely finished".

**Fix** (two-pronged):

1. **Server-side** (primary): atomically transition from old plan to new plan. Create the new plan BEFORE finalizing the old plan's status, or use `"replanning"` as an intermediate status that persists until the new plan is ready.

2. **Client-side** (defense-in-depth): in `_should_stop_polling`, apply the same stability window to `"done" + worker_running` as already exists for `"failed" + worker_running`.

### 103a. Server-side — atomic replan transition ✅

- [x] `kiso/worker/loop.py`: in `_run_planning_loop`, reorder: (1) keep old plan at `"replanning"`, (2) `create_plan` + `_persist_plan_tasks`, (3) THEN `update_plan_status(old, "done"/"failed")`. This eliminates the window where no running/replanning plan exists.
- [x] `tests/test_worker.py`: test that old plan stays `"replanning"` until new plan is persisted (mock DB calls to verify ordering)

### 103b. Client-side — stability window for "done" + worker_running ✅

- [x] `cli/__init__.py`: in `_should_stop_polling` (line 548), change condition from `plan.get("status") == "failed" and worker_running` to `plan.get("status") in ("failed", "done") and worker_running` — applies the 30-poll stability window to both cases
- [x] `tests/test_cli.py`: test that "done" + worker_running does NOT cause immediate polling exit (5 new tests in `TestShouldStopPolling`)

## Milestone 104: Test suite performance — sub-second worker tests ✅

**Problem**: 75 test in `test_worker.py` impiegano >= 1s ciascuno, per un totale di ~65s su ~101s di wall time. La causa è `setting_int("worker_idle_timeout", lo=1)` che impone un minimo di 1 secondo: ogni test che chiama `run_worker()` paga 1s di attesa vuota quando la coda si svuota.

### 104a. `setting_float` in `config.py` ✅

`setting_float` già esistente (M87c). Cambiato `worker_idle_timeout` in `loop.py` da `setting_int(..., lo=1)` a `setting_float(..., lo=0.01)`.

- [x] `kiso/worker/loop.py`: `setting_float(config.settings, "worker_idle_timeout", lo=0.01)`

### 104b. Test config: idle timeout 0.05s ✅

- [x] `tests/test_worker.py`: `_make_config()` default da `1` a `0.05`
- [x] `test_idle_timeout_exits` con `0.1` ora funziona realmente (non troncato a int)

### 104c. Ridurre `exec_timeout` in `test_fact_consolidation_timeout` ✅

- [x] `tests/test_worker.py`: `exec_timeout: 5` → `exec_timeout: 1` — risparmio ~4s

**Risultato**: 2111 test, da ~101s a ~55s wall time (-45%).

## Milestone 105: Plan guardrails — search-for-plugins, translator cap, JSON repair

Observed in production: the planner ignores prompt rules (NEVER search for plugins), the translator wastes tokens, and the planner fails JSON validation 2/3 times.

### 105a. Server-side plan validation: reject search-for-plugins

**Problem**: planner generates `search` tasks to discover kiso plugins despite the NEVER rule in `planner-plugin-install.md`. The searcher (sonar) can't find private registry entries, wastes tokens, and triggers unnecessary replans.

**Fix**: add a post-validation step in the worker after the planner returns a valid plan. If any `search` task's `detail` matches a plugin-discovery pattern (regex: `skill|connector|plugin|registry` combined with `install|search|find|discover|browse`), reject the plan and retry with a specific hint: "Use `exec curl <registry_url>` to discover plugins, not web search."

- [x] `kiso/brain.py`: `_is_plugin_discovery_search()` + check in `validate_plan()` for search tasks
- [x] Pattern: reject only when the search is clearly about kiso plugin discovery, not general web search
- [x] On rejection: return validation error with hint, so the planner retries with guidance
- [x] Test: `TestIsPluginDiscoverySearch`, `TestValidatePlanPluginDiscovery` in `tests/test_brain.py`

### 105b. Translator `max_tokens` cap

**Problem**: translator produces 2,090 output tokens for a command that should be ~10 tokens. The system prompt says "Output ONLY the shell command" but the model ignores it.

**Fix**: add `max_tokens` parameter to the translator LLM call, capped at 500. No shell command needs more than 500 tokens. This is a hard limit that prevents token waste regardless of model behavior.

- [x] `kiso/brain.py`: `run_exec_translator()` passes `max_tokens=500` to `call_llm`
- [x] `kiso/llm.py`: `call_llm()` accepts and forwards `max_tokens` to API payload
- [x] Test: `TestExecTranslatorMaxTokens` in `tests/test_brain.py`, `TestMaxTokensParam` in `tests/test_llm.py`

### 105c. Planner JSON repair + improved retry hint

**Problem**: minimax-m2.5 fails JSON validation 2/3 times, costing ~6,300 extra input tokens per retry cycle. Common failures: trailing commas, unclosed brackets, markdown fences around JSON.

**Fix** (two layers):
1. **JSON repair**: before parsing, strip markdown fences (` ```json ... ``` `), fix trailing commas before `]` or `}`, close unclosed brackets. Light-touch, no heavy library dependency.
2. **Improved retry prompt**: on validation failure, include the specific parse error position and a reminder: "Return ONLY the JSON object. No markdown, no explanation, no trailing commas."

- [x] `kiso/brain.py`: `_repair_json()` in `_retry_llm_with_validation()` before `json.loads()`
- [x] Strip markdown code fences, trailing commas, leading/trailing whitespace
- [x] On parse failure: include line/col position in retry context
- [x] Test: `TestRepairJson`, `TestRetryJsonErrorPosition` in `tests/test_brain.py`

### 105d. Messenger chat context — include recent messages

**Problem**: in chat fast-path, the messenger has no conversation history and hallucmates prior exchanges.

**Fix**: add `include_recent` flag to `run_messenger()`. When True, load recent messages from DB and inject them (fenced) into the messenger context. `_fast_path_chat` in `loop.py` passes `include_recent=True`.

- [x] `kiso/brain.py`: `build_messenger_messages()` accepts `recent_messages`, `run_messenger()` accepts `include_recent`
- [x] `kiso/worker/loop.py`: `_msg_task()` forwards `include_recent`, `_fast_path_chat()` passes `True`
- [x] Test: `TestBuildMessengerMessagesRecent`, `TestRunMessengerIncludeRecent` in `tests/test_brain.py`

---

## Milestone 106: Prompt robustness — Kiso-native priority, exit code semantics, translator hardening

Production failure analysis: user asks "install a browser". The planner skips Kiso-native layer (skills/connectors), goes straight to OS-level `find / -name "*browser*"`, burns 3 replans + 6 retries (~197k tokens) on trivial "check if browsers exist" tasks, and times out. Root causes: planner ignores Kiso-native priority, reviewer treats "nothing found" as failure, translator generates fragile commands.

### 106a. Planner prompt — enforce Kiso-native-first with explicit step

**Problem**: planner.md says "Prefer Kiso-native solutions before OS-level ones" but this is too vague. The planner skips straight to OS-level `exec` tasks without first checking if a relevant skill or connector exists. In the browser example, the planner should first check installed skills/connectors and the plugin registry before attempting OS-level installation.

**Fix**: strengthen the Kiso-native rule in `planner.md` with an explicit mandatory step:

Add rule:
> When the user asks for a capability (browsing, email, file conversion, etc.), ALWAYS start by checking the Kiso layer:
> 1. Check the Skills section in context — is there already an installed skill that does this?
> 2. If not, plan an `exec curl <registry_url>` task to check the plugin registry for matching skills/connectors, followed by a `replan`.
> 3. Only if no Kiso plugin exists, fall back to OS-level installation.
> Never skip steps 1-2 and go directly to OS-level package installation.

- [x] `kiso/roles/planner.md`: add explicit Kiso-native-first rule with numbered steps (CRITICAL rule block with 3 numbered steps)
- [x] Test: 2144 passed — planner prompt loads correctly with new rule

### 106b. Reviewer prompt — informational tasks and exit code semantics

**Problem**: reviewer.md line 11 says "Non-zero exit code = strong failure indicator even if output looks correct. Zero exit is necessary but not sufficient." This is wrong for informational/verification tasks. Commands like `grep`, `which`, `find`, `dpkg -l | grep X` return exit code 1 when they find nothing — but "nothing found" IS a valid answer when the task asks to check/verify/list what exists.

The reviewer repeatedly marks `replan` for commands that successfully answered the question ("no browsers installed") because the exit code was non-zero. This causes a sterile loop: reviewer says replan → translator tries slightly different command → same exit code → same replan.

**Fix**: refine the exit code rule in `reviewer.md`:

Replace line 11 with:
> Non-zero exit code is a strong failure indicator for commands that MUST produce output (install, create, modify, download). However, for **verification/check/list** tasks (the expect asks "what is installed", "check if X exists", "list available Y"), a non-zero exit code with empty or minimal output often means "nothing found" — this IS a valid result. In this case, mark `ok` and record the negative finding in `learn` (e.g. "No browsers are preinstalled"). Only mark `replan` if the command itself errored (syntax error, permission denied, binary not found), not if it simply found no matches.

Add rule:
> If this is the second retry for the same task and the output is substantively the same, the failure is likely structural (exit code semantics, not a fixable error). Mark `ok` with learn capturing what was found, or mark `replan` with `retry_hint: null` to escalate immediately — do not suggest another similar command.

- [x] `kiso/roles/reviewer.md`: replaced exit code rule with task-type-aware version (action vs verification); added anti-loop rule for repeated retries
- [x] Test: 2144 passed — reviewer prompt loads correctly with new rules

### 106c. Reviewer context — richer exit code information

**Problem**: the reviewer sees only "FAILED (non-zero exit code)" with no distinction between `exit 1` (grep no match) and `exit 127` (command not found). This makes it impossible to distinguish "nothing found" from actual errors.

**Fix**: pass the actual exit code to the reviewer, with a contextual note for common codes.

- [x] `kiso/worker/utils.py`: `_run_subprocess()` returns 4-tuple `(stdout, stderr, success, exit_code)` — exit_code is raw returncode (-1 for timeout/OSError)
- [x] `kiso/worker/exec.py`: `_exec_task()` returns 4-tuple, propagates exit_code
- [x] `kiso/worker/skill.py`: `_skill_task()` returns 4-tuple for consistency
- [x] `kiso/brain.py`: `_EXIT_CODE_NOTES` dict maps common codes (1=no matches, 2=syntax, 126=not executable, 127=not found, -1=killed) to human-readable notes. `build_reviewer_messages()` and `run_reviewer()` accept `exit_code` param; when provided, Command Status shows `"Exit code: {code}"` + contextual note
- [x] `kiso/worker/loop.py`: `_handle_exec_task` and `_handle_skill_task` capture exit_code, store in task_row, `_review_task` forwards to `run_reviewer`
- [x] Test: 2144 passed — all existing tests updated for 4-tuple return, backward compat preserved (exit_code=None uses old format)

### 106d. Translator prompt — robust command generation

**Problem**: the translator generates fragile commands:
- `find / -type f -name "*browser*"` — overkill, slow, exits 1 if nothing found
- `which A && which B && which C` — first failure kills the chain
- No use of `|| true`, `command -v`, or other exit-safe patterns

**Fix**: add robustness rules to `worker.md`:

Add rules:
> - For verification/check tasks (checking if something exists, listing installed packages), ensure the command always exits 0 even when nothing is found. Use `command -v X || true`, `dpkg -l | grep X || true`, or `{ cmd; } 2>/dev/null; echo done`.
> - Prefer `;` over `&&` for independent checks (checking multiple binaries, multiple package managers). Use `&&` only when the second command depends on the first succeeding.
> - Never use `find /` for checking installed software. Use `command -v`, `which`, `dpkg -l`, or `apt list --installed` instead.

- [x] `kiso/roles/worker.md`: added 3 robustness rules — `|| true` for verification tasks, `;` over `&&` for independent checks, never `find /` for software checks
- [x] Test: 2144 passed — translator prompt loads correctly with new rules

### 106e. Default planner model — switch from MiniMax M2.5 to GLM 4.7

**Problem**: MiniMax M2.5 ($0.30/$1.20 per M tokens) was chosen for its "agentic" focus, but in practice it:
- Fails JSON validation 2/3 times (required M105c JSON repair workaround)
- Ignores prompt rules (NEVER search for plugins, Kiso-native first)
- GPQA Diamond only 62% — weak reasoning produces nonsensical plans
- AIME 45% — worst reasoning of all candidates

**Analysis** (March 2026 benchmarks via OpenRouter):

| Benchmark               | MiniMax M2.5 | DeepSeek V3.2 | GLM 4.7    | Qwen3.5 Plus |
|--------------------------|-------------|---------------|------------|-------------|
| IFEval (instruction following) | 87.5%  | 77.6%         | **88.0%**  | 76.5%*      |
| GPQA Diamond (reasoning)      | 62%    | 79.9%         | 85.7%      | **88.4%**   |
| AIME 2025 (reasoning depth)   | 45%    | 89.3%         | **95.7%**  | 91.3%       |
| Input $/M                     | $0.30  | $0.25         | $0.30      | $0.26       |
| Output $/M                    | $1.20  | $0.38         | $1.40      | $1.56       |

*IFBench, not IFEval — may not be directly comparable

**Decision**: GLM 4.7 wins on the two metrics that matter most for the planner role:
1. **IFEval 88.0%** (highest) — critical for following prompt rules
2. **GPQA 85.7% / AIME 95.7%** — strong reasoning for plan quality
Similar input cost to MiniMax, slightly higher output cost but justified by dramatically better instruction following and reasoning. DeepSeek V3.2 is cheaper but IFEval 77.6% is a risk for prompt compliance.

**Fix**: change the default planner model from `minimax/minimax-m2.5` to `z-ai/glm-4.7` in `config.py`. Users can override via `config.toml`.

- [x] `kiso/config.py`: `MODEL_DEFAULTS["planner"]` changed to `"z-ai/glm-4.7"`, `CONFIG_TEMPLATE` updated
- [x] `docs/config.md`: updated default model table and model routing example
- [x] Test: 2144 passed — config loads correctly with new default

**Superseded**: GLM 4.7 later replaced with `deepseek/deepseek-v3.2` across all roles for uniformity and cost (output $/M: $0.38 vs $1.40). Changes:
- [x] `kiso/config.py`: `MODEL_DEFAULTS["planner"]` → `"deepseek/deepseek-v3.2"`, `CONFIG_TEMPLATE` updated
- [x] `install.sh`: planner default in `ask_models()` → `"deepseek/deepseek-v3.2"`
- [x] `tests/test_config.py`: VALID fixture and `test_missing_model_role` → deepseek
- [x] `kiso/llm.py`: docstring example → deepseek
- [x] `docs/config.md`, `docs/llm-roles.md`: already referenced deepseek, no changes needed
---

### M107 — CLI verbose fixes, planner timeout for reasoning models ✅

**Problems identified during first GLM 4.7 deploy:**

#### M107a — Verbose timestamp format
**Problem**: verbose LLM call timestamp shows only `HH:MM:SS`, no date. When scrolling terminal history, can't tell which day a call happened.
**Fix**: change strftime from `%H:%M:%S` to `%m-%d %H:%M:%S`.

- [x] `cli/render.py`: timestamp format updated

#### M107b — Replan verbose output missing
**Problem**: when a replan occurs, the verbose LLM call panel (the expandable box with system/user/response) is not shown for the new plan's LLM calls. Root cause: `state.shown_plan_llm_id` is not reset when a replan is detected in `_render_plan_status()`, so the condition to render plan-level LLM calls fails.
**Fix**: add `state.shown_plan_llm_id = None` alongside the other state resets on replan detection.

- [x] `cli/__init__.py`: reset `shown_plan_llm_id` on replan

#### M107c — Planner HTTP timeout ignores planner_timeout
**Problem**: `call_llm()` in `kiso/llm.py` uses `exec_timeout` (120s) as the HTTP timeout for ALL LLM calls, including planner. Even if `planner_timeout` is set higher, the HTTP request dies at 120s. GLM 4.7 (reasoning model, 358B MoE) frequently exceeds 120s because it generates thinking tokens before responding.
**Fix**: `call_llm()` now selects the correct timeout based on role: `planner_timeout` for planner, `messenger_timeout` for messenger, `exec_timeout` for everything else. Default `planner_timeout` raised from 120s to 300s.

- [x] `kiso/llm.py`: role-based HTTP timeout selection
- [x] `kiso/config.py`: `planner_timeout` default raised to 300s (SETTINGS_DEFAULTS + CONFIG_TEMPLATE)
- [x] `docs/config.md`: updated default value and description
- [x] `install.sh`: updated planner_timeout default

---

### M108 — Elapsed timer on CLI spinners ✅

**Problem**: when the planner or a task takes a long time (e.g. GLM 4.7 reasoning), the spinner shows `◆ Planning... ⠴` with no indication of how long it's been. User can't tell if it's working or stuck.

**Fix**: add elapsed seconds to all spinner states. Shows `(Ns)` after 2+ seconds:
- Planning: `◆ Planning... (45s) ⠴`
- Task running: `▶ [1/3] exec: ... running (12s) ⠋`

Implementation:
- `_PollRenderState.spinner_start` tracks when current spinner began (monotonic clock)
- Reset on spinner transition (new task, planning phase start)
- `render_planner_spinner()` and `render_task_header()` accept `elapsed` param
- Threshold: hidden for first 2s to avoid flicker on fast operations

- [x] `cli/__init__.py`: spinner_start tracking, elapsed computation, passed to render functions
- [x] `cli/render.py`: `render_planner_spinner(elapsed=)`, `render_task_header(elapsed=)`
- [x] Test: 2165 passed

### M109 — Worker responsiveness, phase visibility, and inflight LLM calls ✅

**Problem**: ^C doesn't drain the worker queue (180s wait), `_post_plan_knowledge()` blocks the worker, CLI can't distinguish worker phases, and verbose mode only shows LLM calls after the response arrives.

#### M109a — Queue drain on cancel
- `kiso/store.py`: `mark_messages_processed()` batch helper
- `kiso/main.py`: `post_cancel()` drains `entry.queue` via `get_nowait()` loop, marks drained messages as processed
- Response includes `drained` count

#### M109b — Non-blocking post-plan knowledge
- `kiso/worker/loop.py`: `_spawn_knowledge_task()` helper wraps `_post_plan_knowledge` in `asyncio.create_task` with its own LLM budget
- `run_worker()` tracks and awaits `_pending_knowledge_task` before next message and on shutdown
- Exceptions logged, not propagated

#### M109c — Worker phase visibility + inflight LLM call
**Phase labels**: `_worker_phases` dict in main.py, `set_phase` callback passed to `run_worker` → `_process_message`, phases: classifying → planning/executing → idle. Exposed as `worker_phase` in `/status`. CLI renders phase-specific spinner text with timer reset on transition.

**Inflight call**: `_inflight_calls` dict in llm.py, populated before HTTP request, cleared in `finally`. Exposed as `inflight_call` in `/status` (messages stripped for non-verbose). CLI renders Rich Panel with "waiting for response..." in verbose mode, deduped by timestamp.

#### M109d — Cancel in-flight LLM calls

- [x] `kiso/store.py`: `mark_messages_processed()` batch helper
- [x] `kiso/main.py`: queue drain in `post_cancel()`, `_worker_phases` + `_set_worker_phase()`, `worker_phase` + `inflight_call` in `/status`
- [x] `kiso/llm.py`: `_inflight_calls` tracking, `get_inflight_call()`, `stripped_messages` dedup
- [x] `kiso/worker/loop.py`: `_spawn_knowledge_task()`, phase callback, background task management
- [x] `cli/__init__.py`: phase tracking, spinner phase label, inflight call rendering
- [x] `cli/render.py`: phase-specific `render_planner_spinner()`, `render_inflight_call()`, shared helpers (`_make_rich_console`, `_rich_buf_to_str`, `_short_model`, `_build_message_parts`, `_verbose_title`, `_labeled_sep`)
- [x] `kiso/brain.py`: `WORKER_PHASE_CLASSIFYING/PLANNING/EXECUTING/IDLE` + `WORKER_PHASES` frozenset (simplify: stringly-typed → named constants)
- [x] Simplify: `_parse_llm_calls` reuse, `Callable` import, `_phase()` wrapper in `run_worker`, lazy `stripped_messages`
- [x] Tests: 39 new tests across 7 files (test_llm, test_sessions, test_cli, test_cli_render, test_worker, test_status, test_brain). 2204 passed

---

### M110 — CLI polish, model upgrade, and planner token optimization ✅

#### M110a — Phase completion timers
- [x] `cli/render.py`: `render_phase_done()`, `_PHASE_DONE_LABELS` map
- [x] `cli/__init__.py`: `phase_start` in `_PollRenderState`, emit completion lines on phase transitions and plan completion

#### M110b — Fix verbose call ordering (P1 + P3)
- [x] `cli/__init__.py`: `shown_plan_llm_count` for incremental plan-level LLM rendering (classifier appears immediately)
- [x] `cli/__init__.py`: summary line only on plan completion, deduplicated review re-renders

#### M110c — Planner model upgrade
- [x] `kiso/config.py`: `MODEL_DEFAULTS["planner"]` → `"deepseek/deepseek-v3.2"` (planner_timeout stays 300s)

#### M110d — Planner token optimization
- [x] `kiso/worker/utils.py`: aggressive replan output truncation (200/1000 chars), 12k char budget
- [x] `kiso/config.py`: `context_messages` 7→5
- [x] `kiso/brain.py`: fact text capped at 200 chars

#### M110e — Fix replan phase label
- [x] `kiso/worker/loop.py`: `set_phase(PLANNING)` before replan, `set_phase(EXECUTING)` after
- [x] Tests: 2211 passed

---

### M111 — Session quality fixes (timers, verbose dedup, reviewer hardening) ✅

**Problem**: Per-task elapsed timers vanish from final output, verbose mode duplicates input messages, reviewer hallucinates learnings on empty output, reviewer returns `reason: null` on replan.

#### M111a — Per-task `duration_ms` storage
- [x] `kiso/store.py`: `duration_ms INTEGER DEFAULT NULL` in tasks DDL + `TaskDict`, PRAGMA-guarded migration in `init_db()`, `duration_ms` param in `update_task()`
- [x] `kiso/worker/loop.py`: threaded `duration_ms=task_duration_ms` into all 8 `update_task()` completion calls; fixed `_handle_skill_task` to compute duration before DB write

#### M111b — Per-task elapsed in CLI completion line
- [x] `cli/render.py`: `render_task_header()` appends `(Ns)` suffix for done/failed tasks with `duration_ms >= 1000`

#### M111c — Verbose box deduplication
- [x] `cli/render.py`: `shown_inflight_ts: set | None` param in `render_llm_calls_verbose()`, skips input when ts already shown
- [x] `cli/__init__.py`: `_PollRenderState.shown_inflight_ts` changed from `float | None` to `set`, removed clear-tracking clause, passed to both verbose call sites

#### M111d — Reviewer hardening
- [x] `kiso/roles/reviewer.md`: reason required for replan (null for ok), empty-output learn guard
- [x] `kiso/worker/loop.py`: server-side guard in `_review_task()` discards learnings when output+stderr are empty/whitespace

- [x] Tests: 2241 passed (14 new tests across test_store, test_worker, test_cli_render, test_brain)

---

### M112 — Live session fixes (validation, caching, verbose, elapsed, anti-hallucination) ✅

Various fixes discovered during live testing sessions.

- [x] Session validation, caching, verbose rendering, elapsed display, anti-hallucination improvements
- [x] Tests: 2254 passed

---

### M113 — Split verbose LLM panels into separate Input/Output panels ✅

Replace single combined verbose panel per LLM call with two distinct panels: Input (messages sent) and Output (response received), with summary line between.

- [x] `cli/render.py`: `render_llm_calls_verbose()` renders Input panel (dim cyan border) + summary line + Output panel (dim green border) per call; `_verbose_title()` accepts `direction` param
- [x] Tests: 2254 passed

---

### M114 — Inflight indicator integration (verbose input panel during waiting) ✅

During live polling, show Input panel immediately when an LLM call is inflight, then show Output panel when response arrives. No duplication.

- [x] `cli/__init__.py`: `inflight_input_shown: set` in `_PollRenderState`, shared `_print_verbose_panels()` helper for both `_emit_verbose_calls` and plan-level rendering
- [x] Tests: 2262 passed

---

### M115 — discover_skills() diagnostic logging ✅

Add warning logs when `discover_skills()` returns empty to help debug skill discovery failures in Docker.

- [x] `kiso/skills.py`: log resolved dir path and `exists` status when dir not found
- [x] `kiso/brain.py`: log when planner receives empty skill list
- [x] Tests: 2264 passed

---

### M116 — Planner rules to prevent infinite investigation loops ✅

Add "Skills efficiency" rules to planner prompt: use listed skills directly, don't reinstall, don't re-verify confirmed facts, only ask for declared env vars, place msg tasks after dependent tasks.

- [x] `kiso/roles/planner.md`: 4 new rules in "Skills efficiency" section
- [x] Tests: 2272 passed

---

### M117 — Messenger anti-hallucination for absent information ✅

Messenger must not fabricate entries when task outputs don't contain the requested data.

- [x] `kiso/roles/messenger.md`: rule to state nothing found instead of fabricating
- [x] Tests: 2272 passed

---

### M118 — Reviewer substance-over-format for check tasks ✅

Reviewer accepts verification results that demonstrate the condition is met, regardless of output format matching the literal `expect` wording.

- [x] `kiso/roles/reviewer.md`: "Verification substance over format" rule
- [x] Tests: 2272 passed

---

### M119 — Idempotent plugin install (exit 0 when already installed) ✅

`kiso skill install` and `kiso connector install` now return successfully (exit 0) with a notice when the plugin is already installed, instead of exiting 1 with an error.

- [x] `cli/plugin_ops.py`: `_plugin_install()` returns with notice instead of `sys.exit(1)` when plugin dir exists
- [x] Tests: 2272 passed (both skill and connector install-already-installed tests updated)

---

### M120 — Shell script ERR trap fix — `[[ ]] &&` short-circuit with `set -e` ✅

**Problem**: `install.sh` and `kiso-host.sh` use `set -euo pipefail` with an ERR trap. Patterns like `[[ -n "$VAR" ]] && cmd` cause false ERR trap activation when the condition is false, because the entire statement returns exit 1. This caused `install.sh` to show `Error: command failed (exit 1): exit 0` after a successful installation.

**Fix**: Add `|| true` to all `[[ ]] && cmd` short-circuit patterns that can legitimately be false, or convert to `if/then/fi`. Affected locations:

- [x] `install.sh`: backup cleanup (lines 66-67, 806-807), lib guard (line 69), source mode (line 364), instance count echo (line 867)
- [x] `kiso-host.sh`: TTY detection (line 186), `--yes` flag scan (line 432), empty NAME check (line 274)
- [x] Tests: 2272 passed, `KISO_INSTALL_LIB=1 source install.sh` works

---

### M121 — Ctrl+C handling in install.sh and kiso-host.sh ✅

**Problem**: Ctrl+C during interactive prompts (e.g., "Overwrite config.toml? [y/N]") in `install.sh` does not exit the script. The user is trapped — Ctrl+C has no effect. Root cause: the cleanup trap handles both EXIT and INT but doesn't call `exit`, and the ERR trap fires for signal-killed commands (exit > 128), printing confusing error messages instead of exiting cleanly.

**Fix**:
- [x] `install.sh`: separate INT trap (`_on_interrupt`) that calls cleanup + `exit 130`; ERR trap skips signal exits (`$? > 128`)
- [x] `kiso-host.sh`: ERR trap skips signal exits (`$_ec > 128`)
- [x] Tests: 2272 passed, source mode works

---

## Post-mortem: guidance.studio session (2026-03-05)

User asked: "vorrei navigare su internet, che aprissi guidance.studio, mi dicessi di cosa si occupa l'azienda, e mi lasciassi uno screenshot". The system spent ~5 minutes going in circles — 6 replans, 4 redundant registry fetches, repeated "skill not installed" validation failures — without ever installing the browser skill or giving the user a meaningful response. User gave up and typed `/exit`.

Root causes identified (in priority order):

1. **Replan output truncation**: `_REPLAN_OUTPUT_LIMIT = 200` chars per exec task — registry JSON (800+ chars) and kiso.toml (1200+ chars) are truncated beyond usefulness. Planner can't see the browser skill in the output, so it re-fetches.
2. **Plugin-install appendix not injected**: keyword gate (`{"install", "installa", "plugin", "add"}`) checks the original user message, which says "navigare" not "install". Even during replan, the enriched message contains "installed" (from validation error) but not "install" (exact word). So the planner never gets `planner-plugin-install.md` instructions.
3. **Validation error gives no remedy**: "skill 'browser' is not installed. Available skills: none" — tells the planner what's wrong but not how to fix it. The planner (deepseek-v3.2) can't infer `kiso skill install browser` on its own.
4. **No total job wall-clock timeout**: per-call timeouts exist (exec=120s, planner=300s, messenger=120s) but nothing caps the entire message processing. With `extend_replan` accumulating (+3 per replan, uncapped), theoretical max is 12+ replans × 300s = 60+ minutes.
5. **Silent failure**: when the planner validation loop exhausts `max_validation_retries`, the `PlanError` triggers `_handle_loop_failure` which generates a generic messenger response. But the user sees nothing until then — no progress, no intermediate "I'm stuck" message.
6. **extend_replan accumulation is unbounded**: planner can request `extend_replan=3` on every replan cycle. The code adds it to `max_replan_depth` without a global cap, turning 3 replans into 12+.
7. **Verbose OUT panels missing**: plan-level verbose rendering uses `shown_plan_llm_count = len(plan_calls)` (all calls) to slice `verbose_calls` (only calls with messages). Counter mismatch skips OUT panels. Also, validation retry LLM calls inside `_json_retry_loop` may not propagate their panels correctly.

---

### M122 — Raise replan output limits and add smart truncation ✅

**Problem**: `_REPLAN_OUTPUT_LIMIT = 200` chars per exec task is too aggressive. A `curl` of registry.json (800 chars) or kiso.toml (1200 chars) gets truncated to uselessness. The planner can't see the data it already fetched, so it re-fetches it.

**Fix**: Raise exec output limit and add content-aware truncation that preserves structured data (JSON, TOML).

- [x] `kiso/worker/utils.py`: raise `_REPLAN_OUTPUT_LIMIT` from 200 to 2000 chars, `_REPLAN_SEARCH_OUTPUT_LIMIT` to 2000
- [x] `kiso/worker/utils.py`: raise `_REPLAN_CONTEXT_CHAR_BUDGET` from 12000 to 20000 (~5000 tokens)
- [x] `kiso/worker/utils.py`: add `_smart_truncate()` — truncates at newline boundary with "... (truncated)" marker
- [x] Tests: 2274 passed — updated existing limit tests, added smart truncation + newline boundary tests

---

### M123 — Inject plugin-install appendix during replan when skill is missing ✅

**Problem**: `planner-plugin-install.md` is only injected when `{"install", "installa", "plugin", "add"}` appears in the user message. When the planner discovers mid-plan that a skill needs installing (via replan), the original message doesn't contain these keywords, so the install instructions are never injected.

**Fix**: Extend plugin-install detection with substring matching for "not installed" and "registry".

- [x] `kiso/brain.py`: added `or "not installed" in msg_lower or "registry" in msg_lower` to plugin-install detection
- [x] Tests: 2276 passed — added tests for "not installed" replan context and "registry" keyword

---

### M124 — Validation error suggests the fix for missing skills ✅

**Problem**: Plan validation says `"skill 'browser' is not installed. Available skills: none"` but doesn't tell the planner how to install it.

**Fix**: Enrich the validation error message with the remedy.

- [x] `kiso/brain.py`: error now includes `"Use an exec task with 'kiso skill install {name}' to install it first, then replan to use it."`
- [x] Tests: 2277 passed — added `test_skill_not_installed_suggests_fix`

---

### M125 — Total job wall-clock timeout ✅

**Problem**: No cap on total message processing time. The full replan loop can run indefinitely.

**Fix**: Add `job_timeout` setting (default 600s) checked at the top of each loop iteration.

- [x] `kiso/config.py`: added `job_timeout = 600` to SETTINGS_DEFAULTS and TOML template
- [x] `kiso/worker/loop.py`: check `elapsed > job_timeout` at top of each `_run_planning_loop` iteration; calls `_handle_loop_failure` on timeout
- [x] Tests: 2278 passed — mocked `time.monotonic` to verify job timeout triggers failure

---

### M126 — Cap cumulative extend_replan

**Problem**: `extend_replan` is added to `max_replan_depth` on every replan cycle without a global cap. The planner can request +3 every time, turning a 3-replan limit into 12+.

**Fix**: Cap the total accumulated extensions.

- [x] `kiso/worker/loop.py` (`_run_planning_loop`): track `total_extensions` and cap at `_MAX_EXTEND_REPLAN` (3) total, not per-cycle
- [x] `kiso/worker/loop.py`: log when extension request is denied due to cap
- [x] Tests: verify that after 3 total extensions, further extend_replan requests are ignored

---

### M127 — Intermediate "I'm stuck" message after repeated replan failures

**Problem**: During long replan loops, the user sees only "Investigating... (N/M)" or "Replanning (attempt N/M): ..." but no substantive information. If the planner is going in circles, there's no escalation to the user.

**Fix**: Detect circular replanning and escalate to user.

- [x] `kiso/worker/loop.py` (`_run_planning_loop`): after 2 consecutive replans with similar failure reasons (e.g., same "skill not installed" error), generate a msg task asking the user for help instead of silently retrying
- [x] Similarity detection: compare last 2 entries in `replan_history` — if `failure` strings share >60% of words, consider it a loop
- [x] The escalation msg task should explain what was tried and what's blocking, in the user's language
- [x] Tests: verify loop detection triggers after 2 similar failures and produces a user-facing message

---

### M128 — Fix verbose OUT panel counter mismatch

**Problem**: In `cli/__init__.py`, plan-level verbose rendering uses `shown_plan_llm_count = len(plan_calls)` (count of ALL plan calls) to slice `verbose_calls` (only calls with `messages`). If some calls lack messages, the counter advances past actual verbose calls, skipping their OUT panels.

**Fix**: Track verbose calls separately from total calls.

- [x] `cli/__init__.py` (`_PollRenderState`): add `shown_plan_verbose_count` field (default 0) separate from `shown_plan_llm_count`
- [x] `cli/__init__.py` (`_render_plan_status`): use `shown_plan_verbose_count` to slice `verbose_calls`, and `shown_plan_llm_count` for the summary line gate
- [x] Reset both counters on plan change
- [x] Tests: construct mock plan data with mixed verbose/non-verbose calls; verify all OUT panels are rendered

---

### M129 — Planner prompt: always include install instructions when no skills are installed

**Problem**: The planner-plugin-install appendix is keyword-gated. But when `discover_skills()` returns empty (no skills installed), the planner may need install instructions regardless of what the user asked — any non-trivial task will likely require a skill.

**Fix**: Auto-inject plugin-install appendix when no skills are available.

- [x] `kiso/brain.py` (`build_planner_messages`): after `discover_skills()`, if `installed_names` is empty, always append `planner-plugin-install.md` to the system prompt
- [x] This complements M123 (replan detection) — M129 handles the initial plan, M123 handles replans
- [x] Tests: verify plugin-install appendix is injected when no skills are installed, even without keyword match

---

### M130 — Replan history includes task outputs in Previous Replan Attempts section

**Problem**: `replan_history` entries in `_build_replan_context` only show `"Tried: [exec] curl ..., [replan] Investigate..."` — task descriptions but NOT their outputs. The planner can't see what the registry returned in a previous replan cycle.

**Fix**: Include summarized outputs in replan history entries.

- [x] `kiso/worker/loop.py` (`_run_planning_loop`): when building `replan_history` entries, include a `"key_outputs"` field with the first 500 chars of each completed task's output
- [x] `kiso/worker/utils.py` (`_build_replan_context`): in the "Previous Replan Attempts" section, render key_outputs alongside each attempt
- [x] Add a separate budget for replan history outputs to avoid bloating the context (3000 chars)
- [x] Tests: verify replan history entries include task outputs

---

### M131 — Planner anti-loop: don't re-verify facts already confirmed in plan_outputs

**Problem**: The planner prompt says "if a previous plan already confirmed a fact, do not re-verify it" (planner.md line 37), but this is just a prompt instruction. With weak models and truncated outputs (pre-M122), the planner can't follow this rule because it can't see the confirmed facts.

**Fix**: After M122 raises the output limit and M130 adds outputs to replan history, add explicit "Confirmed Facts" section to replan context.

- [x] `kiso/worker/utils.py`: add `_extract_confirmed_facts(completed)` helper that scans completed task outputs for patterns like: registry JSON → extract skill/connector names; command output with "installed"/"available" → extract status lines; short outputs → first line
- [x] `kiso/worker/utils.py` (`_build_replan_context`): add `## Confirmed Facts` section before Completed Tasks, listing extracted facts as bullet points (includes facts from history key_outputs)
- [x] This is a best-effort extraction — JSON outputs get skill names, status lines get extracted, other outputs get first line. Capped at 10 facts.
- [x] Tests: verify confirmed facts extraction from registry JSON (single + array), install output, history key_outputs, and empty outputs

---

### M132 — Configurable classifier timeout (was hardcoded 15s) ✅

**Problem**: Classifier LLM call timeout was hardcoded to 15s in `_process_message`. With slower models or high-latency providers, 15s may not be enough, causing spurious fallbacks to the planner.

**Fix**: Add `classifier_timeout` setting (default 30s), use it in the classifier `asyncio.wait_for`.

- [x] `kiso/config.py`: add `classifier_timeout = 30` to SETTINGS_DEFAULTS and TOML template
- [x] `kiso/worker/loop.py`: read `classifier_timeout` from settings, pass to `_process_message`, use instead of hardcoded 15
- [x] Tests: 2272 passed

---

### M133 — Dedicated classifier model role + early plan creation for classifier OUT panel ✅

**Problem**: (1) The classifier uses the `worker` model (deepseek-v3.2), which is a large reasoning model overkill for a "plan or chat" classification — takes 30s. (2) The classifier OUT panel is never shown in verbose mode because the plan record isn't created until after the planner finishes.

**Fix**: Add required `classifier` model role (no fallback — kiso requires all models to be configured). Create plan record before the planner runs so classifier usage is immediately visible to CLI.

- [x] `kiso/brain.py` (`classify_message`): use `classifier` model role directly (no fallback)
- [x] `kiso/config.py`: add `classifier` to `MODEL_DEFAULTS` and `CONFIG_TEMPLATE`
- [x] `kiso/worker/loop.py` (`_process_message`): create plan early (before planner), store classifier usage immediately
- [x] `kiso/worker/loop.py` (`_handle_plan_error`): accept optional `plan_id` to reuse existing plan on error
- [x] Tests: 2291 passed

---

### M134 — Unify model config: installer reads MODEL_DEFAULTS from Python ✅

**Problem**: `install.sh` hardcoded model roles/defaults/descriptions separately from `kiso/config.py`. Adding a new role (like `classifier` in M133) required updating both files — easy to forget and caused a real bug.

**Fix**: Single source of truth. `install.sh` reads `MODEL_DEFAULTS` and `MODEL_DESCRIPTIONS` from Python at install time, with a static FALLBACK for curl-pipe installs where Python isn't available. Added `MODEL_DESCRIPTIONS` dict to `config.py`. Added drift-detection tests.

- [x] `kiso/config.py`: add `MODEL_DESCRIPTIONS` dict (role → human description)
- [x] `install.sh` (`ask_models`): read roles from Python via `PYTHONPATH` import, FALLBACK heredoc for offline installs
- [x] `tests/test_config.py`: 3 alignment tests (descriptions ↔ defaults, CONFIG_TEMPLATE ↔ defaults)
- [x] Tests: 2293 passed

---

### M135 — Fix verbose panel ordering and Ctrl+C in installer ✅

**Problem 1**: In verbose mode, the classifier IN panel was shown twice and the plan header appeared between them. Root causes: (a) plan was created after classifier finished, so inflight panel rendered before the plan header existed; (b) inflight `ts` and completed call `ts` differed (`time.time()` called at start vs end), so the dedup check failed.

**Problem 2**: Ctrl+C during interactive installer prompts silently killed the script instead of showing "Interrupted." because all `ask_*` functions ran inside `$(...)` subshells which don't inherit the parent's INT trap.

**Fix 1**: Move `create_plan()` before the classifier call. Pass `plan_id` to `_fast_path_chat()` so it reuses the existing plan. Share a single `call_ts` in `llm.py` between inflight tracking and usage logging.

**Fix 2**: Convert `ask_username`, `ask_provider_name`, `ask_base_url`, `ask_api_key`, `ask_models` from echo-returning to setting globals (`KISO_USER`, `PROVIDER_NAME`, `BASE_URL`, `API_KEY`, `MODELS_SECTION`), eliminating subshells during interactive prompts.

- [x] `kiso/llm.py`: single `call_ts` shared between inflight and usage entry
- [x] `kiso/worker/loop.py`: `create_plan()` before classifier; `_fast_path_chat(plan_id=...)` reuses existing plan
- [x] `install.sh`: all interactive functions use globals instead of echo + `$()`
- [x] Tests: 2294 passed

---

### M136 — Fix double `.kiso` path in exec subprocess (HOME override)

**Problem**: `kiso/worker/utils.py:_build_exec_env()` sets `HOME = str(KISO_DIR)` (= `/root/.kiso` inside Docker). When an exec task runs `kiso skill install browser`, the subprocess CLI code computes `KISO_DIR = Path.home() / ".kiso"` = `/root/.kiso/.kiso` — double `.kiso`. Skills get installed to `/root/.kiso/.kiso/skills/` but the server's `discover_skills()` looks at `/root/.kiso/skills/`. They never see each other.

This is the root cause of the infinite install → validation failure → replan loop observed in the guidance.studio session: the planner tries `type: "skill"` with browser, validation rejects it ("Available skills: none"), planner falls back to exec install + replan, but the skill is still invisible. Loops 4+ times until circular replan detection fires.

**Evidence**: exec output says `Skill 'browser' is already installed at /root/.kiso/.kiso/skills/browser`.

**Fix**: Change `env["HOME"] = str(KISO_DIR)` to `env["HOME"] = str(Path.home())` so the subprocess HOME matches the real home directory (`/root`), and `Path.home() / ".kiso"` resolves to `/root/.kiso` as intended. The `sys/bin` PATH prepend and other `KISO_DIR`-relative paths remain correct.

- [x] `kiso/worker/utils.py` (`_build_exec_env`): `env["HOME"] = str(Path.home())`
- [x] Test: exec subprocess env has correct HOME
- [ ] Manual verification: `kiso skill install` installs to `/root/.kiso/skills/`, `discover_skills()` finds it

---

### M137 — Planner: don't put msg tasks before investigation tasks

**Problem**: The planner creates plans with a msg task as task [1/N] that says "I'll check capabilities first" — before the exec/search tasks that gather information. The messenger has no data yet and hallucinates an answer ("I can't browse the web"), which is shown to the user even though subsequent tasks discover the browser skill IS installed.

**Evidence**: Plan D00008 has `[msg, exec, exec, replan]` — msg fires first, messenger invents "you need to use a browser directly." Then exec discovers browser skill exists.

**Fix**: Add rule to planner prompt (`kiso/roles/planner.md`): "Never place a msg task before exec/search/skill tasks in the same plan. Msg tasks communicate results — they must come after the tasks that produce those results. If you need to inform the user mid-plan, place the msg after the relevant exec/search tasks."

- [x] `kiso/roles/planner.md`: add task ordering rule (msg after data-gathering tasks)
- [x] `kiso/brain.py`: structural validation — msg before last exec/search/skill rejected
- [x] Tests: 5 tests for msg ordering validation (before exec, before search, after all, msg-only, msg+replan)

---

### M138 — Messenger: never invent CLI commands or code

**Problem**: The messenger fabricates CLI commands that don't exist: `kiso browser goto https://guidance.studio`, `kiso browser navigate --url https://www.guidance.studio`. These aren't real — the browser skill uses JSON args via `type: "skill"` tasks. Users see fake commands and get confused.

**Evidence**: D00013 tells user to "use a browser directly", D00080 invents `kiso browser navigate --url`, D00063 invents `kiso browser goto`.

**Fix**: Add rule to messenger prompt (`kiso/roles/messenger.md`): "Never invent CLI commands, code snippets, or technical syntax. Only reproduce commands that appear verbatim in preceding task outputs. If no commands are available, describe the action in natural language without fabricating syntax."

- [ ] `kiso/roles/messenger.md`: add anti-hallucination rule for commands/code
- [ ] Verify existing anti-hallucination rules and strengthen if needed

---

### M139 — Use `bash` instead of `/bin/sh` for exec subprocesses

**Problem**: `_run_subprocess` uses `create_subprocess_shell(cmd)` which invokes `/bin/sh -c`. On Ubuntu/Debian, `/bin/sh` is `dash`, which doesn't support bashisms like `<<<` (here-strings), `[[ ]]` double brackets, process substitution `<()`, etc. The worker LLM (DeepSeek) frequently generates bash-specific syntax, causing `Syntax error: redirection unexpected` (exit 2). This triggers unnecessary replans that waste LLM calls and time.

**Evidence**: D00026/D00033 in guidance.studio session — worker generates `sed -n '...' <<<"..."` twice, both fail with exit 2 on dash. The replan context doesn't help because the retry still uses bashisms.

**Fix**: Switch shell execution from `/bin/sh` to `bash`:
- In `_run_subprocess`, when `shell=True`, use `asyncio.create_subprocess_exec("bash", "-c", cmd, **kwargs)` instead of `create_subprocess_shell(cmd)`.
- Add worker prompt rule: "The shell is bash. Prefer POSIX constructs, but bash features (arrays, `<<<`, `[[ ]]`) are available."
- Update the worker prompt's "Executed by /bin/sh" to "Executed by bash".

- [ ] `kiso/worker/utils.py` (`_run_subprocess`): use `bash -c` instead of `create_subprocess_shell`
- [ ] `kiso/roles/worker.md`: update shell reference from `/bin/sh` to `bash`
- [ ] Test: verify subprocess uses bash (here-string test)

---

### M140 — Save large task outputs to workspace files

**Problem**: Task outputs are passed to subsequent tasks via `plan_outputs`, truncated to `_REPLAN_OUTPUT_LIMIT = 2000` chars. When task 1 fetches a 20KB HTML page, the truncation cuts off the `<meta name="description">` tag. All subsequent tasks that try to extract the description from the truncated output fail, creating an **unbreakable replan loop** — 5+ iterations all failing identically because the data they need was destroyed by truncation.

**Evidence**: D00040 shows the planner embedding truncated HTML ending at `"Specialized in bespoke bus'"` — the description attribute is cut off mid-word. D00048, D00054, D00072, D00083 all fail with "description missing" because it literally isn't in the truncated text.

**Fix**: When a task's stdout exceeds a threshold (e.g. 4KB), automatically save it to a workspace file and replace the inline output with a file reference:
1. In `_exec_task` (or the output handling after subprocess): if `len(stdout) > _LARGE_OUTPUT_THRESHOLD`, write stdout to `{workspace}/.kiso/task_outputs/task_{index}.txt` and replace the output in plan_outputs with `[Output saved to {path} ({len} bytes). First 500 chars:]\n{head}`.
2. The worker prompt already knows the workspace path — it can `cat` or `grep` the file.
3. Keep `_REPLAN_OUTPUT_LIMIT` for the replan context (LLM prompt), but the file is always available for the next task to read.

- [ ] `kiso/worker/loop.py`: auto-save large outputs to workspace file, replace inline output with path + head
- [ ] `kiso/roles/worker.md`: add note about large output files in workspace
- [ ] `kiso/worker/utils.py`: add `_LARGE_OUTPUT_THRESHOLD = 4096`
- [ ] Test: large output saved to file, path passed in plan_outputs

---

### M141 — Worker prompt: always use `curl -L` for HTTP requests

**Problem**: The worker generates `curl -s URL` without `-L` (follow redirects). guidance.studio returns 301 → worker gets redirect HTML → reviewer says "use -L" → retry works. But on the **next replan**, the worker generates `curl -s` again without `-L` because it doesn't carry forward the retry hint. This 301→retry→replan→301 cycle repeats across 3 replans (D00062, D00097).

**Evidence**: D00012 `curl -s`, D00019 `curl -sL` (retry), D00062 `curl -s` (replan 2, forgets -L), D00069 `curl -L -s` (retry), D00097 `curl -s` (replan 4, forgets again).

**Fix**: Add worker prompt rule: "Always use `curl -L` (follow redirects) unless the task explicitly asks for redirect-only behavior."

- [ ] `kiso/roles/worker.md`: add `curl -L` default rule
- [ ] Optionally: add to blocked commands or lint check

---

### M142 — Planner: use file-based data flow for multi-step tasks

**Problem**: The planner creates plans where task 2 depends on task 1's output, but the only data path is inline `plan_outputs` (truncated). The planner should instruct task 1 to save output to a file and task 2 to read from that file. Instead, the planner embeds raw (truncated) HTML in the task detail itself (D00040 — 3000+ char detail field), creating quoting nightmares and data loss.

**Evidence**: D00040 detail field is 3788 chars, mostly embedded HTML. D00076 detail is 2876 chars. Worker tries to echo the embedded HTML → quoting breaks → grep fails.

**Fix**: Add planner prompt rule: "When a task produces large output (HTML, JSON, logs) that later tasks need, instruct task 1 to save output to a file in the workspace (e.g. `curl -sL URL > page.html`). Subsequent tasks should read from that file (`grep ... page.html`). Never embed large data in task details."

- [ ] `kiso/roles/planner.md`: add file-based data flow rule for multi-step tasks
- [ ] Test: prompt-only rule, verify in planner prompt text

---

### M143 — Planner: diversify strategy after repeated failures

**Problem**: Across 5 replans, the planner generates essentially the same plan: `exec(grep/sed HTML) → msg → exec(registry) → replan`. It never tries fundamentally different approaches (web search, Python one-liner, `lynx -dump`, saving to file). The "DO NOT repeat these approaches" section in replan context is ignored.

**Evidence**: Plans D00008, D00040, D00058, D00076, D00093 — all use the same grep/sed approach on HTML. The planner keeps trying minor variations (different grep patterns) instead of changing strategy.

**Fix**: Add planner prompt rule: "If the replan history shows 2+ failures with the same approach (e.g. grep/sed on HTML), you MUST try a fundamentally different strategy. Examples: use `search` task instead of `exec curl`; use `python3 -c` for parsing; use `lynx -dump` for text extraction; save to file then process. Never submit the same approach a third time."

- [ ] `kiso/roles/planner.md`: add strategy diversification rule after repeated failures
- [ ] Test: prompt-only rule, verify in planner prompt text

---

### M144 — Planner: detail field must be natural language, not shell commands

**Problem**: The planner writes shell commands directly in the `detail` field: `"Command: echo '<!DOCTYPE html>...' | grep ..."`. The detail is supposed to be natural language that the worker translates to a shell command. Embedding commands causes: (a) the worker copies the command verbatim, even if it's broken; (b) quoting/escaping breaks when the detail contains raw HTML.

**Evidence**: D00040, D00076, D00093 — detail fields contain explicit `Command: ...` with embedded HTML and complex grep/sed pipelines.

**Fix**: Add planner prompt rule: "The `detail` field must be natural language describing WHAT to accomplish, not HOW. Never include shell commands, code, or raw data in detail. The worker translates intent to commands."

- [ ] `kiso/roles/planner.md`: add rule forbidding shell commands in detail field
- [ ] `kiso/brain.py`: optionally add validation — reject tasks where detail contains obvious shell syntax (pipes, `&&`, backticks)
- [ ] Test: validate detail field doesn't contain shell commands

---

### M145 — Reviewer: carry forward retry hints to replan context

**Problem**: The reviewer provides correct retry hints (e.g. "use curl -L to follow redirects"), and these work for the immediate retry. But when the task escalates to a full replan, the retry hint is lost — the next plan's worker starts from scratch and makes the same mistake.

**Evidence**: D00016 retry_hint "use -L" → immediate retry works. But replan 2 (D00062) and replan 4 (D00097) repeat `curl -s` without `-L` because the hint wasn't carried forward.

**Fix**: Include accumulated retry hints from all completed tasks in the replan context's "Confirmed Facts" section. In `_build_replan_context`, extract `retry_hint` values from completed tasks and add them as "Learned corrections: ..." in the confirmed facts.

- [ ] `kiso/worker/utils.py` (`_build_replan_context`): extract retry_hints from completed tasks, add to confirmed facts
- [ ] Test: replan context includes retry hints from prior attempts

---

### M146 — Reviewer: `summary` field for intelligent output condensation

**Problem**: Task outputs are truncated mechanically to 2000 chars before being passed to the planner via replan context. This destroys critical data (e.g. `<meta name="description">` tag cut off mid-word in a 20KB HTML page). Meanwhile, the reviewer already reads the full output and extracts `learn` facts — but those go to the memory system, not to the replan context. The planner never benefits from the reviewer's understanding of the output.

**Evidence**: D00022 — reviewer reads full HTML and extracts 5 detailed facts about guidance.studio's business. But the replan context (D00038, D00056, D00074, D00091) shows the same truncated HTML every time. The reviewer's intelligence is wasted; the planner sees garbage.

**Relationship to M140**: M140 saves raw output to files so the *worker* can access full data. M146 gives the *planner* an intelligent summary so it can make better replan decisions. They complement each other:
- M140 = raw data access for workers (file-based)
- M146 = intelligent context for planner (LLM-summarized)

**Fix**: Add a `summary` field to the reviewer JSON schema. The reviewer already processes the full output — asking it to produce a short summary costs negligible extra tokens. Then use `summary` (when present) instead of truncated raw output in replan context.

1. **Reviewer schema** (`kiso/brain.py`): add optional `summary` field to reviewer response format.
   ```json
   {
     "status": "ok",
     "reason": null,
     "learn": ["..."],
     "summary": "Title: 'Guidance | Navigate your business'. Description: 'Accelerating growth for organizations by unlocking digital potential.' Site built with Webflow, uses openresty.",
     "retry_hint": null
   }
   ```

2. **Reviewer prompt** (`kiso/roles/reviewer.md`): add rule — "Produce a `summary`: a concise (max 500 chars) extraction of the key data from the task output that would be useful to a planner deciding next steps. Include specific values (names, versions, paths, URLs, extracted text) rather than vague descriptions. If the output is short or trivial, summary can be null."

3. **Replan context** (`kiso/worker/utils.py`): in `_build_replan_context`, when a completed task has a reviewer `summary`, use it instead of the truncated raw output. Fall back to truncated output if summary is null.

4. **Task result storage**: store reviewer `summary` alongside task output in the completed task dict, so `_build_replan_context` can access it.

- [ ] `kiso/brain.py`: add `summary` field to reviewer JSON schema
- [ ] `kiso/roles/reviewer.md`: add summary extraction rule
- [ ] `kiso/worker/loop.py`: store reviewer `summary` in completed task dict
- [ ] `kiso/worker/utils.py` (`_build_replan_context`): prefer reviewer summary over truncated output
- [ ] Test: reviewer summary used in replan context when available
- [ ] Test: falls back to truncated output when summary is null
