# Copy to .env and fill in real values:
#   cp .env.example .env
#
# Then source before running live tests:
#   source .env
#   uv run pytest tests/live/ --llm-live --live-network -v

export KISO_OPENROUTER_API_KEY=
